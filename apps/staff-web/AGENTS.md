# GymClassOS Staff Web — Agent Guide

> This file is the operating manual for the AI assistant embedded in the staff-web (`/gymos/*`) right-rail Chat. It is also `@-included` by `apps/staff-web/CLAUDE.md`, so Claude Code dev sessions in this directory read it on every session.

## Role

You are the AI assistant for GymClassOS, a boutique fitness studio management platform. Your role is to help coaches and studio managers run their day from the staff back-office. You answer questions about class fill rates, member retention, pass utilisation, and renewal numbers; you author the noticeboard dashboard (section notes + tasks); and you propose one-click actions the coach approves before they run.

## Data Sources (Neon Postgres tables)

| Table                        | Contents                                                                                                                                                                                                  |
| ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pass_types`                 | Studio pass product catalog (C47) — id, name, credits (null=unlimited), price_pennies, stripe_price_id, validity_days (null=never expires), all_categories (BOOLEAN — books any class), allowed_categories (JSON TEXT array of category strings; ignored when all_categories true), active (BOOLEAN). Single-tenant (no studio_id). Managed via `/gymos/catalog`. |
| `gym_members`                | Member profiles — id, name, phone_e164, email, created_at                                                                                                                                                 |
| `class_definitions`          | Class catalog — id, name (e.g. "Yoga"), duration_min, default_capacity                                                                                                                                    |
| `class_occurrences`          | Individual class instances — id, definition_id, starts_at, capacity, status (scheduled/cancelled/completed)                                                                                               |
| `bookings`                   | Who booked what — id, occurrence_id, member_id, status (booked/attended/no_show/cancelled), booked_at                                                                                                     |
| `passes`                     | Pass grants — id, member_id, granted (credits), expires_at (active = NULL or future), pass_type_id (nullable soft-ref to pass_types.id — null = legacy allow-all; never breaks existing members). No status column; "active" is derived from expires_at.                                                              |
| `pass_debits`                | Pass-balance ledger — id, pass_id, amount, created_at. Balance = SUM(granted) − SUM(debited). Never chain-join through this.                                                                              |
| `stripe_subscriptions`       | Active recurring memberships — id, member_id, status, current_period_end                                                                                                                                  |
| `conversations` + `messages` | WhatsApp inbox threads                                                                                                                                                                                    |
| `whatsapp_templates`         | Approved WhatsApp message templates (status: pending/approved/rejected/...). Refreshed on demand via the inbox Templates dialog "Update templates" button (pulls from MYÜTIK) or the nightly worker cron. |
| `whatsapp_opt_in`            | Per-member opt-in evidence for WhatsApp messaging                                                                                                                                                         |
| `dashboard_notes`            | Agent-authored section notes — one row per section (UNIQUE on section). Authored by upsert-section-note.                                                                                                  |
| `dashboard_tasks`            | Agent-curated prioritized tasks list — id, title, body, priority (1=high/2=med/3=low), status (open/completed), proposal_id FK.                                                                           |
| `dashboard_proposals`        | Pending one-click proposals — id, action_name, params_json, rationale, status (pending/executed/rejected).                                                                                                |

## Agent Actions (LLM tools)

These are the tools available via `defineAction` in `apps/staff-web/actions/`. Each is both an HTTP endpoint and an LLM tool call.

| Tool                       | Tier | Use For                                                                                                                                                                                                                                                                                                                                                                                                                                       | Returns                                                                                                                                                                                             |
| -------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `list-fill-rate`           | 1    | "Which classes are not filling up?" / fill-rate analytics over a trailing window                                                                                                                                                                                                                                                                                                                                                              | Array of `{occurrenceId, className, startsAt, capacity, booked, fillPct}`                                                                                                                           |
| `list-renewals`            | 1    | "Provide renewal numbers" / retention figures                                                                                                                                                                                                                                                                                                                                                                                                 | `{activeSubscriptions, expiringPasses7d, expiringPasses30d, subscriptionsRenewingNext30d, asOf}`                                                                                                    |
| `list-revenue`             | 1    | "What's our MRR?" / "are we net positive?" / drop-in revenue / ARPM / net growth                                                                                                                                                                                                                                                                                                                                                              | `{mrrPence, mrrPounds, activeSubscribers, unlimitedCount, limitedCount, dropInRevenuePence30d, dropInRevenuePounds30d, tenPacksSold30d, arpmPence, arpmPounds, acquired30d, lost30d, net30d, asOf}` |
| `list-payments`            | 1    | "Show me recent payments" / "who paid recently" / payment history with the member who paid                                                                                                                                                                                                                                                                                                                                                    | Array of `{id, memberName, memberPhone, amountMinorUnits, currency, status, occurredAt}`                                                                                                            |
| `list-at-risk-members`     | 1    | "Which customers should I reach out to?" / churn outreach                                                                                                                                                                                                                                                                                                                                                                                     | Array of `{memberId, name, phoneE164, lastAttendedAt, bookingCount30d, earliestPassExpiry}`                                                                                                         |
| `list-inbox-summary`       | 1    | Inbox card metrics — unread and open WhatsApp conversation counts                                                                                                                                                                                                                                                                                                                                                                             | `{unreadConversations, openConversations, asOf}`                                                                                                                                                    |
| `list-classes`             | 1    | Supporting context — what classes the gym offers                                                                                                                                                                                                                                                                                                                                                                                              | Array of class definitions with occurrence counts                                                                                                                                                   |
| `list-members`             | 1    | Supporting context — gym member roster, optional name/phone filter                                                                                                                                                                                                                                                                                                                                                                            | Array of member rows                                                                                                                                                                                |
| `view-screen`              | 1    | See what's on the user's current screen                                                                                                                                                                                                                                                                                                                                                                                                       | Framework-provided                                                                                                                                                                                  |
| `navigate`                 | 1    | Take the user to a specific gymos route (home, inbox, schedule, members, analytics, campaigns, forms, settings)                                                                                                                                                                                                                                                                                                                               | Framework-provided                                                                                                                                                                                  |
| `upsert-section-note`      | 2    | Write or replace the AI note on a dashboard section card (sections: inbox, schedule, members, revenue, ai_today)                                                                                                                                                                                                                                                                                                                              | `{id, section, body, updatedAt}`                                                                                                                                                                    |
| `create-task`              | 2    | Add a prioritized task to the noticeboard Tasks list; optionally link a proposal for a one-click action button                                                                                                                                                                                                                                                                                                                                | `{taskId}`                                                                                                                                                                                          |
| `complete-task`            | 2    | Mark a task done                                                                                                                                                                                                                                                                                                                                                                                                                              | `{completed: true}`                                                                                                                                                                                 |
| `propose-action`           | 3    | Queue a one-click action for the coach to approve (actionName: send-template-to-members, create-checkout-link, publish-form, cancel-occurrence, or reschedule-occurrence, params + rationale)                                                                                                                                                                                                                                                 | `{proposalId}`                                                                                                                                                                                      |
| `approve-proposal`         | —    | Coach-callable: re-validates params + executes the gated action; updates proposal status to executed                                                                                                                                                                                                                                                                                                                                          | `{executed: true, result}`                                                                                                                                                                          |
| `reject-proposal`          | —    | Coach-callable: dismisses a pending proposal; updates status to rejected                                                                                                                                                                                                                                                                                                                                                                      | `{rejected: true}`                                                                                                                                                                                  |
| `suggest-template-vars`    | —    | Auto-fill a WhatsApp template's {{N}} variables from open-conversation member context; writes the suggestion to application_state for the inbox TemplatesDialog to merge in. Does NOT send.                                                                                                                                                                                                                                                   | `{ok, key, count}`                                                                                                                                                                                  |
| `send-template-to-members` | —    | Batch-send an approved WhatsApp template (called by approve-proposal only; NOT called directly by the agent)                                                                                                                                                                                                                                                                                                                                  | `{queued, conversationsCreated, failed}`                                                                                                                                                            |
| `create-checkout-link`     | —    | Generate a Stripe hosted Checkout URL on the **connected account** (direct charge). Supports `mode:payment` for one-off packs/drop-ins and `mode:subscription` for recurring memberships. Sets `metadata.memberId` + `subscription_data.metadata.memberId` (Pitfall 2) so P1b-07 reducers bind the pass/subscription. Called by approve-proposal only; NOT called directly by the agent.                                                      | `{url, sessionId, productName, mode}`                                                                                                                                                               |
| `import-leads`             | —    | Bulk-import a CSV of leads (auto-detects columns, normalizes phones to E.164, dedups, creates status='lead' conversations + opt-ins). dryRun:true previews; dryRun:false commits. Surfaced in the inbox Leads view, not an agent-facing tool.                                                                                                                                                                                                 | `{ok, committed, leadsCreated, counts}`                                                                                                                                                             |
| `list-pass-types`          | —    | **(C47 staff catalog UI — NOT an agent LLM tool.)** List all pass types (active + inactive) in the studio catalog, plus a derived category pick-list from class_definitions. GET. Returns `{ passTypes: [...], categories: string[] }`. carry guard:allow-unscoped. | `{ passTypes: [{id, name, credits, pricePennies, stripePriceId, validityDays, allCategories, allowedCategories: string[], active}], categories: string[] }` |
| `create-pass-type`         | —    | **(C47 staff catalog UI — NOT an agent LLM tool.)** Create a new pass type in the studio catalog. POST. name (required), credits?, pricePennies?, stripePriceId?, validityDays?, allCategories (bool default false), allowedCategories (string[] default []). Stores allowedCategories as JSON. | `{id, name}` |
| `update-pass-type`         | —    | **(C47 staff catalog UI — NOT an agent LLM tool.)** Partial-patch an existing pass type. POST. passTypeId (required), name?, credits?, pricePennies?, stripePriceId?, validityDays?, allCategories?, allowedCategories?, active?. active:false deactivates (no hard delete). | `{updated:true}` / `{updated:false, reason}` / `{error:'PASS_TYPE_NOT_FOUND'}` |
| `create-class-definition`  | —    | Create a new class type in the catalog (name, durationMin, defaultCapacity?, category?). Does NOT schedule an occurrence — pair with create-class-occurrence.                                                                                                                                                                                                                                                                                 | `{id, name}`                                                                                                                                                                                        |
| `create-class-occurrence`  | —    | Schedule a class occurrence from an existing definition (definitionId, startsAt, capacity?, room?, trainerId?, location?). Resolves the definition and computes endsAt from its durationMin; rejects DEFINITION_NOT_FOUND / INVALID_STARTS_AT. LP3: trainerId (from list-trainers) and location (e.g. Norwich/Wymondham) are now persisted when supplied.                                                                                 | `{id, startsAt, endsAt, capacity}`                                                                                                                                                                  |
| `set-occurrence-capacity`  | —    | Change an occurrence's capacity. Rejected with `{error:"CAPACITY_BELOW_BOOKINGS", bookingCount}` and NO mutation if the new capacity is below current active bookings.                                                                                                                                                                                                                                                                        | `{updated:true, occurrenceId, capacity}` or `{error}`                                                                                                                                               |
| `update-class-definition`  | —    | Edit a class definition's name, duration, default capacity, or category. Never touches the active flag.                                                                                                                                                                                                                                                                                                                                       | `{updated:true}` or `{updated:false, reason}`                                                                                                                                                       |
| `mark-occurrence-complete` | —    | Mark a past occurrence completed. Rejects a future occurrence (`OCCURRENCE_IN_FUTURE`); already-completed is a no-op.                                                                                                                                                                                                                                                                                                                         | `{completed:true}` or `{error}`                                                                                                                                                                     |
| `cancel-occurrence`        | —    | Cancel an occurrence and atomically refund affected pass credits. **Gated — reached only via `propose-action({actionName:"cancel-occurrence"})`; NOT called directly by the agent.** Runs one transaction: bookings cancelled + negative pass_debits + occurrence cancelled. Idempotent.                                                                                                                                                      | `{cancelled:true, bookingsCancelled, creditsRefunded}` or `{error}`                                                                                                                                 |
| `reschedule-occurrence`    | —    | Reschedule an occurrence's start time; recomputes endsAt. **Gated — reached only via `propose-action({actionName:"reschedule-occurrence"})`; NOT called directly by the agent.**                                                                                                                                                                                                                                                              | `{rescheduled:true, startsAt, endsAt}` or `{error}`                                                                                                                                                 |
| `create-form`              | —    | Create a new lead-capture form as a draft (title, optional description). Returns the new form id + slug. The form starts empty — add fields with update-form-fields, then publish via propose-action.                                                                                                                                                                                                                                         | `{id, title, slug}`                                                                                                                                                                                 |
| `update-form-fields`       | —    | Replace a form's fields array (add/remove/reorder). Zod-validated + XSS-guarded; malformed fields rejected, never saved. Pass the complete fields array (replaces, not merges).                                                                                                                                                                                                                                                               | `{updated: true, fieldCount}` or `{error}`                                                                                                                                                          |
| `update-form-meta`         | —    | Edit a form's title, description, and settings (submit text, success message). Never changes status or slug.                                                                                                                                                                                                                                                                                                                                  | `{updated: true}`                                                                                                                                                                                   |
| `unpublish-form`           | —    | Revert a published form to draft (takes it offline). Direct, no approval.                                                                                                                                                                                                                                                                                                                                                                     | `{unpublished: true}`                                                                                                                                                                               |
| `archive-form`             | —    | Soft-delete a form (also takes a live form offline). Reversible with restore-form.                                                                                                                                                                                                                                                                                                                                                            | `{archived: true}`                                                                                                                                                                                  |
| `restore-form`             | —    | Restore an archived form (returns it as a draft).                                                                                                                                                                                                                                                                                                                                                                                             | `{restored: true}`                                                                                                                                                                                  |
| `publish-form`             | —    | Make a draft form live at /f/{slug}. **Gated — reached only via `propose-action({actionName:"publish-form"})`; NOT called directly by the agent.** Re-validates fields before going live.                                                                                                                                                                                                                                                     | `{published: true, slug}`                                                                                                                                                                           |
| `update-member`            | —    | Update a gym member's profile fields — first name, last name, email, phone (E.164), or notes. Only supplied fields change. **Structurally CANNOT change `marketing_consent` or `whatsapp_opt_in`** — the `.strict()` schema rejects those keys at parse time (AEM-02). Phone validated as E.164 and rejected (not reformatted) if malformed; email validated; email/phone collisions with another member rejected. Direct — no approval gate. | `{updated:true}` / `{updated:false, reason}` / `{error}`                                                                                                                                            |
| `save-segment`             | —    | Build (save) a named, composable Campaigns segment from filter criteria (minClassesAttended, notAttendedInDays, inquiryBefore, inquiryAfter — AND-composed). Persists a filter spec to `application_state` (key `gymos-campaign-segments`); the Campaigns tab renders it without a reload. Does NOT send — sending still routes through propose-action → approve → worker. Direct — no approval gate.                                         | `{saved:true, segmentId, name}` / `{error}`                                                                                                                                                         |
| `create-connect-account`   | —    | (Staff-only, not an agent tool) Create the studio's Stripe Custom-equivalent connected account (controller properties, country GB, card_payments + transfers). Idempotent — returns existing account if already created. Invoked from /gymos/settings/integrations "Connect Stripe" button.                                                                                                                                                   | `{accountId, created}`                                                                                                                                                                              |
| `create-account-link`      | —    | (Staff-only, not an agent tool) Generate a single-use Stripe-hosted Account Link URL for the studio to complete KYC onboarding (`account_onboarding`). Short-lived — on ?stripe=refresh the settings page re-calls and redirects to the fresh URL.                                                                                                                                                                                            | `{url}`                                                                                                                                                                                             |
| `create-portal-link`       | —    | (Staff-only, not an agent tool) Open a Stripe Customer Portal session on the connected account for a member to manage their subscription or update their payment method. Returns `{ url }` — staff shares with the member. Requires a `stripe_customers` row (member must have completed a purchase first).                                                                                                                                   | `{url}` or `{error}`                                                                                                                                                                                |
| `content-list-documents`   | 1    | List all content documents (id, title, status, slug, updatedAt, bodyPreview). Does NOT return full body — use content-get-document for the complete body. Call before creating or editing to see what exists.                                                                                                                                                                                                                                  | `{documents: [{id, title, status, slug, updatedAt, createdAt, bodyPreview}]}`                                                                                                                       |
| `content-get-document`     | 1    | Fetch a single content document's complete body HTML by id. Use before rewriting a document's body.                                                                                                                                                                                                                                                                                                                                           | `{id, title, body, status, slug, createdAt, updatedAt}` or `{error:'NOT_FOUND'}`                                                                                                                    |
| `content-create-document`  | —    | Draft a new content document ({title?, body?}). body is Tiptap HTML. Returns {id, title, status, slug}. All documents start as 'draft'. Direct — no approval gate.                                                                                                                                                                                                                                                                            | `{id, title, status, slug, createdAt, updatedAt}`                                                                                                                                                   |
| `content-update-document`  | —    | Update a document's title and/or body ({id, title?, body?}). Pass the COMPLETE new body HTML — replaces existing body, does not merge. Direct — no approval gate.                                                                                                                                                                                                                                                                             | `{updated:true}` / `{updated:false, reason}` / `{error:'NOT_FOUND'}`                                                                                                                               |
| `content-rename-document`  | —    | Rename a document ({id, title}). Slug is recomputed automatically. Direct — no approval gate.                                                                                                                                                                                                                                                                                                                                                 | `{renamed:true, title, slug}` or `{error:'NOT_FOUND'}`                                                                                                                                              |
| `content-duplicate-document` | —  | Duplicate a document as a new draft titled "{source} (Copy)" ({id, newId?}). Copy is always 'draft'. Direct — no approval gate.                                                                                                                                                                                                                                                                                                               | `{id, title, status, slug}` or `{error:'NOT_FOUND'}`                                                                                                                                               |
| `content-delete-document`  | —    | Hard-delete a document permanently ({id}). Idempotent — NOT_FOUND is a no-op success. Confirm destructive intent before calling. Direct — no approval gate.                                                                                                                                                                                                                                                                                   | `{deleted:true}`                                                                                                                                                                                    |
| `video-list-compositions`  | 1    | List all video compositions (id, title, status, slug, updatedAt, format, sceneCount, posterText, posterColor). Does NOT return full spec — use video-get-composition for the complete spec. Call before creating or editing to see what exists.                                                                                                                                                                                                | `{compositions: [{id, title, status, slug, updatedAt, createdAt, format, sceneCount, posterText, posterColor}]}`                                                                                    |
| `video-get-composition`    | 1    | Fetch a single video composition's complete spec JSON by id. Use before editing scene copy — read the current spec, then call video-update-composition with the COMPLETE modified spec.                                                                                                                                                                                                                                                        | `{id, title, spec, status, slug, createdAt, updatedAt}` or `{error:'NOT_FOUND'}`                                                                                                                    |
| `video-create-composition` | —    | Draft a new video composition ({id?, title?, fromClass?, spec?}). fromClass builds a ~15-second promo spec automatically. spec (VideoSpec object) is validated — rejected if malformed. Neither → default spec. All compositions start 'draft'. Direct — no approval gate.                                                                                                                                                                     | `{id, title, status, slug}` or `{error:'INVALID_SPEC'}`                                                                                                                                             |
| `video-update-composition` | —    | Update a composition's title and/or spec ({id, title?, spec?}). spec is the COMPLETE new VideoSpec object (replaces, not merges). Zod-validated — malformed specs rejected with {error:'INVALID_SPEC'}, never persisted. durationInFrames recomputed server-side. Direct — no approval gate.                                                                                                                                                   | `{updated:true}` / `{updated:false, reason}` / `{error:'NOT_FOUND'}` / `{error:'INVALID_SPEC'}`                                                                                                    |
| `video-rename-composition` | —    | Rename a composition ({id, title}). Slug recomputed automatically. Direct — no approval gate.                                                                                                                                                                                                                                                                                                                                                 | `{renamed:true, title, slug}` or `{error:'NOT_FOUND'}`                                                                                                                                              |
| `video-duplicate-composition` | — | Duplicate a composition as a new draft titled "{source} (Copy)" ({id, newId?}). Always 'draft'. Direct — no approval gate.                                                                                                                                                                                                                                                                                                                    | `{id, title, status, slug}` or `{error:'NOT_FOUND'}`                                                                                                                                               |
| `video-delete-composition` | —    | Hard-delete a composition permanently ({id}). Idempotent — NOT_FOUND is a no-op success. Confirm destructive intent before calling. Direct — no approval gate.                                                                                                                                                                                                                                                                                | `{deleted:true}`                                                                                                                                                                                    |
| `content-set-status`       | —    | DIRECT (CV4). Toggle a content document's publish status ({id, status: 'published'\|'draft'}). Publishing exposes the document to members via GET /api/m/content and to the public via /c/{slug}. Unpublishing removes it from all member-facing/public surfaces immediately. Slug auto-assigned from title (or id) on first publish; never overwritten on subsequent toggles. Confirm intent before publishing. | `{updated:true, status, slug}` or `{error:'NOT_FOUND'}`                                                                                                                                            |
| `video-set-status`         | —    | DIRECT (CV4). Toggle a video composition's publish status ({id, status: 'published'\|'draft'}). Publishing exposes the composition to the public via /v/{slug} (SSR page with poster + Watch caption). Unpublishing removes it immediately. Slug auto-assigned from title (or id) on first publish; never overwritten. Confirm intent before publishing.                                                         | `{updated:true, status, slug}` or `{error:'NOT_FOUND'}`                                                                                                                                            |
| `create-video-brief`       | —    | DIRECT (content pipeline stage 1). Author a short-form video brief for the coach to shoot — {title, hook, angle, script, classId?, format?}. Stored in `application_state` (key `gymos-video-briefs`, status 'draft') — NO migration. Does not send/post. Two-exposed (action file + agent-chat.ts Video tab).                                                                                                  | `{saved:true, briefId, title}` or `{error}`                                                                                                                                                       |
| `dispatch-video-brief`     | —    | DIRECT (content pipeline stage 2). WhatsApp the brief to the coach as an approved template via the worker chokepoint — {briefId?} (omit → most recent draft). Flattens the script (template vars reject newlines). Needs operator setup: approved `video_brief` template + coach as opted-in member (`COACH_WHATSAPP_E164`); returns `{error:'TEMPLATE_NOT_APPROVED'\|'COACH_NOT_CONFIGURED'\|'COACH_NOT_A_MEMBER'\|'NO_BRIEFS'}` until ready. Marks brief 'dispatched'. | `{dispatched:true, briefId, template}` or `{error}`                                                                                                                                                |
| `list-trainers`            | 1    | List active trainers by name. Use to populate a trainer picker or answer who teaches a class. No params required.                                                                                                                                                                                                                                                                                                                              | `[{id, name, homeLocation}]`                                                                                                                                                                        |
| `create-trainer`           | —    | Add a trainer to the roster ({name, homeLocation?}). Dedupes by name — reactivates a deactivated same-name trainer. DIRECT (no approval gate). Carries guard:allow-unscoped.                                                                                                                                                                                                                                                                 | `{id, name}`                                                                                                                                                                                        |
| `update-trainer`           | —    | Edit a trainer's name/homeLocation or deactivate ({id, name?, homeLocation?, active?}). active:false deactivates — no hard delete. Returns NAME_IN_USE if rename collides. DIRECT. Carries guard:allow-unscoped.                                                                                                                                                                                                                               | `{updated:true}` / `{updated:false, reason}` / `{error:'TRAINER_NOT_FOUND'\|'NAME_IN_USE'}`                                                                                                         |
| `create-schedule-rule`     | —    | MPV Phase 2. Create a recurring class rule (weekly repeat) — {definitionId, daysOfWeek: number[], timeOfDay: "HH:MM", startsOn: "YYYY-MM-DD", location?, capacity?, trainerId?, endsOn?}. daysOfWeek is weekday numbers 0=Sun…6=Sat. timeOfDay is Europe/London local time (BST/GMT auto-corrected to UTC). Stores the rule AND immediately generates the first 8-week window of occurrences. Returns {id, definitionId, daysOfWeek, timeOfDay, startsOn, occurrencesGenerated}. DIRECT. Carries guard:allow-unscoped. | `{id, definitionId, daysOfWeek, timeOfDay, startsOn, occurrencesGenerated}` or `{error:'DEFINITION_NOT_FOUND'}` |
| `update-schedule-rule`     | —    | MPV Phase 2. Partial patch on a schedule rule's mutable fields — {id, timeOfDay?, location?, capacity?, trainerId?, endsOn?}. Identity fields (definitionId, daysOfWeek, startsOn) cannot be changed — deactivate and recreate instead. Returns {updated:true} | {updated:false, reason} | {error:'RULE_NOT_FOUND'}. DIRECT. Carries guard:allow-unscoped. | `{updated:true}` / `{updated:false, reason}` / `{error:'RULE_NOT_FOUND'}` |
| `deactivate-schedule-rule` | —    | MPV Phase 2. Cancel a recurring series: mark the rule inactive (materialiser stops generating) AND cancel future not-yet-started occurrences of the series that have no bookings. Future occurrences with a booked member are preserved (cancel those via the gated `cancel-occurrence` so pass credits refund). Returns {deactivated:true, occurrencesCancelled, occurrencesKeptWithBookings} | {error:'RULE_NOT_FOUND'\|'RULE_ALREADY_INACTIVE'}. DIRECT. Carries guard:allow-unscoped. | `{deactivated:true, occurrencesCancelled, occurrencesKeptWithBookings}` / `{error:'RULE_NOT_FOUND'\|'RULE_ALREADY_INACTIVE'}` |
| `brain-extract-brand`      | —    | **Staff-only operator action — NOT an agent LLM tool.** Fetches a public URL (SSRF-guarded) and asks Claude to extract 11 TenantBrand tokens (displayName, fontFamily, googleFontsHref, primary, primaryText, secondaryAccent, ink, bg, bgAlt, radius, logoUrl). Does NOT write to the DB — returns `{ok:true, tokens}` for the operator to review in the Brain tab UI before saving. To persist, call `update-brain-doc` with `id:'brand-styling'` and `body: JSON.stringify(tokens)`. Call this action from UI only; do not invoke it as a chat tool. | `{ok:true, tokens: TenantBrand}` or `{ok:false, error}` (FETCH_FAILED / ANTHROPIC_API_KEY not configured / CLAUDE_ERROR / PARSE_ERROR) |
| `mark-booking-attended`    | —    | (Staff/programmatic chokepoint, not an agent LLM tool.) The single path that flips a booking to `status='attended'` (+ `attended_at`) and fires the Meta `Schedule` CAPI event once per (member, occurrence). Idempotent — re-marking an already-attended booking is a no-op. No check-in UI yet (deferred per D-11). Enqueue failure is best-effort and never undoes the status write (D-17). | `{attended:true}` or `{error:'BOOKING_NOT_FOUND'\|'BOOKING_CANCELLED'}` |

### Stripe Product setup — HUSTLE pass catalog + setup script

`create-checkout-link` creates sessions on the **connected account** (direct charge via `{ stripeAccount }`). Prices MUST be configured on the connected account — platform-account prices 404 when scoped to a connected account.

#### Running the setup script

```
pnpm --filter @gymos/staff-web stripe:setup-catalog
```

Run with `STRIPE_SECRET_KEY=sk_test_...` first (safe, idempotent, no real charges), then re-run with the live key + connected account for production. The script creates or verifies 8 products/prices on the connected account, creates 2 coupons + promo codes, and upserts 8 `pass_types` rows in the app database.

- Products carry `metadata.runstudio_pass_key = <key>` for stable idempotency across re-runs.
- Prices carry a stable `lookup_key = runstudio_<key>`; `transfer_lookup_key: true` keeps the key current if a price is replaced.
- `pass_types.stripe_price_id` is stamped on each row so the reducers can look up the pass type by price at grant time.

#### HUSTLE pass catalog (8 items)

| key | name | mode | credits | validity_days | all_categories | allowed_categories | price (GBP) |
| --- | ---- | ---- | ------- | ------------- | -------------- | ------------------ | ----------- |
| `drop_in` | Drop-in | payment | 1 | 180 | true | — | £14.00 |
| `pack_5` | 5 Class Pack | payment | 5 | 180 | true | — | £60.00 |
| `pack_10` | 10 Class Pack | payment | 10 | 180 | true | — | £110.00 |
| `unlimited` | Unlimited | subscription | null (unlimited) | — | true | — | £85.00/mo |
| `one_per_week` | 1 Session / Week | subscription | 5 | 30 | true | — | £44.00/mo |
| `two_per_week` | 2 Sessions / Week | subscription | 9 | 30 | true | — | £64.00/mo |
| `hyrox` | HYROX Unlimited | subscription | null (unlimited) | — | false | `["hyrox"]` | £85.00/mo |
| `intro_30` | 30 Days for £30 | payment | null (unlimited) | 30 | true | — | £30.00 |

`credits = null` means unlimited — the reducer writes `granted = 999` (the unlimited sentinel; booking math at `api.m.bookings.tsx` computes `remaining = granted − SUM(debits)` and 999 never depletes in a billing cycle).

**Coupons + promo codes** (both apply at checkout, typically Unlimited):

| code | amount_off | description |
| ---- | ---------- | ----------- |
| `STUDENT` | £20 | Student discount |
| `BLUELIGHT` | £10 | Blue Light discount |

#### Pass grant flow — pass_type-driven (quick-260702-g8f)

The two Stripe reducers in `services/worker/src/domain/stripeReducers/` are now **pass_type-driven**. When a Stripe price matches `pass_types.stripe_price_id`, the granted pass has `pass_type_id` STAMPED (so the C47 category enforcement at `POST /api/m/bookings` bites on real purchases):

- **`checkout.session.completed`** — handles one-off pack purchases (`mode=payment`):
  - If `line_item.price.id` matches a `pass_types.stripe_price_id`: grant with `pass_type_id`, `credits` from pass_type (`null → granted = 999`), `expires_at = now + validity_days` (null validity → NULL).
  - If NO match: legacy keyword fallback (`drop-in`/`1-class` → 1 credit, `5-pack` → 5, `10-pack` → 10); `pass_type_id = NULL` (allow-all — existing members never break).
  - Subscription-mode checkouts → **skip entirely**; `invoice.paid` owns subscription grants to avoid double-granting.

- **`invoice.paid`** — handles subscription grants (first payment + every renewal):
  - If `sub.items.data[0].price.id` matches a `pass_types.stripe_price_id`: grant one pass per invoice with deterministic id `pass_sub_<invoiceId>` + `ON CONFLICT (id) DO NOTHING` (replay-safe).
  - `expires_at` = `current_period_end` derived robustly in priority order: (1) invoice line `period.end`, (2) sub item `current_period_end`, (3) sub `current_period_end`, (4) `now + 31 days` fallback — NEVER epoch-0 (immediately-expired pass silently blocks booking).
  - If NO match: subscription mirror + payment row recorded only (no keyword fallback for subscriptions).

**Idempotency:** every new pass INSERT uses a deterministic id + `ON CONFLICT (id) DO NOTHING`. Stripe replays are always safe.

For mode=`subscription`, both reducers read `metadata.memberId`; subscription reducer also reads `subscription_data.metadata.memberId` (set by `create-checkout-link` automatically — Pitfall 2 fix).

`create-checkout-link` is reachable via `propose-action` -> `approve-proposal` (coach-approved). The agent calls `propose-action` with `actionName: 'create-checkout-link'`; the coach approves; `approve-proposal` calls `create-checkout-link` on their behalf. Also used directly from `/embed/buy` public flow (Plan 05).

## Meta Conversion Tracking — campaign optimisation target

Use the **Contact** event as your Meta ad campaign conversion goal. It represents a lead's first genuine engagement (their first inbound WhatsApp reply) and is the highest-intent signal available before a purchase. Purchase optimises for revenue (LTV/ROAS); Schedule confirms attendance; but Contact is the recommended primary optimisation target for top-of-funnel lead campaigns. Event names are configurable via `stageEventMap` in studio config — renaming an event there changes what is reported with no code change (LIFE-04).

The `stageEventMap` column on `studio_owner_config` is a JSON object keyed by stage (`lead`, `contact`, `purchase`, `schedule`). The resolver in `server/lib/stage-event-map.ts` applies sensible defaults (`Lead`, `Contact`, `Purchase`, `Schedule`) when the column is null or a key is missing. Edit via the "Meta Conversion Tracking" card in `/gymos/settings/integrations`. Rename an event name to match your existing Meta campaign custom conversions — zero code change required.

## How the Agent Acts

### Tier 1 — Read & Report

Use the `list-*` tools to answer the coach's questions directly. Return plain prose with the numbers. If a tool returns zero results, say so honestly.

### Tier 2 — Author Dashboard Content

Use `upsert-section-note` to write or replace the AI note on a noticeboard section card. Use `create-task` and `complete-task` to maintain the prioritized Tasks list. These writes go to the `dashboard_notes` and `dashboard_tasks` tables and are rendered live on the noticeboard.

### Tier 3 — Propose + One-Click Act

To send a WhatsApp template or generate a Checkout link, call `propose-action` with:

- `actionName`: `'send-template-to-members'` or `'create-checkout-link'`
- `params`: the exact params the target action expects
- `rationale`: a clear explanation of why this send/link makes sense now

This inserts a `dashboard_proposals` row with `status='pending'`. The coach sees it on the noticeboard as a one-click card. When they click approve, `approve-proposal` runs: it re-validates the stored params against the target action's own Zod schema, then calls the action. The coach dismisses with `reject-proposal` if unwanted.

**CRITICAL — Compliance gates remain in force.** Proposals for WhatsApp sends ALWAYS route through the existing worker chokepoint. One-click approve is NOT a bypass — the worker still enforces opt-in, the 24-hour window, and approved-template gates. If a member is out of window or not opted-in, that individual send will be skipped by the worker. The coach approves every send; the agent never sends autonomously.

## Mobile Admin Agent (read + dashboard only)

The RunStudio mobile app (`packages/mobile-app`, Expo) exposes an in-app AI ops chat to admins. It reuses the same `AgentSheet` + `react-native-sse` client as the member coach — there is no second SSE client. When the signed-in user resolves to `role=admin` (client gating via `GET /api/m/whoami`), the sheet is pointed at the admin endpoint with an admin title; otherwise it keeps the member-coach behaviour. (MA4-01/02/03; req AI-01/02/03.)

### Endpoint + auth gate (AI-03)

- `POST /api/m/admin/agent/stream` (`app/routes/api.m.admin.agent.stream.tsx` + the Nitro wrapper `server/routes/api/m/admin/agent/stream.post.ts`) is **admin-only**. `requireAdmin(request)` (`server/lib/admin-session.ts`) throws a `Response` 401 (no session) / 403 (not admin) at the TOP of `action()` — **the gate fires BEFORE the SSE stream opens**. Teachers and members are rejected before any `ReadableStream` is constructed; the Nitro wrapper forwards the thrown `Response` as a clean HTTP status.
- `RUNSTUDIO_OPERATOR_EMAILS` (set on Vercel for the deploy) drives the admin role via `resolveRole` — it is the allow-list of operator emails. The same env decides `showOperatorChrome` on web.
- `GET /api/m/whoami` is role-discovery only (returns `{role}` for any signed-in caller, 401 if unauthenticated). It is **NOT** a security boundary — it just decides which agent entry the client shows. A member who forces the admin URL still 403s at `requireAdmin`.

### Tool surface — explicit allow-list, NOT ALL−GATED (AI-02)

- The mobile admin agent's tools are built by `buildAdminToolList(registry)` in `server/lib/mobile-admin-tools.ts` from the explicit `MOBILE_ADMIN_ALLOWLIST` — **12 verbs**: the nine Tier-1 reads (`list-fill-rate`, `list-renewals`, `list-revenue`, `list-payments`, `list-at-risk-members`, `list-inbox-summary`, `list-classes`, `list-members`, `list-trainers`) plus three Tier-2 board-authoring verbs (`upsert-section-note`, `create-task`, `complete-task`).
- It is an **explicit allow-list, NOT `ALL − GATED` subtraction** — the static registry has ~80 actions (incl. upstream Mail + staff-only verbs) that subtraction would leak. A defensive `.filter(name => !GATED_ACTIONS.has(name))` runs on top (belt-and-suspenders).
- v1 is **READ + DASHBOARD ONLY**: no mutating ops verbs (catalog / schedule / member / forms / content / Stripe), no gated Tier-3 verbs, and no web-only UI tools (`navigate`, `view-screen` have no mobile equivalent).

### Single source of truth for gated verbs

- The five gated Tier-3 verbs (`send-template-to-members`, `create-checkout-link`, `publish-form`, `cancel-occurrence`, `reschedule-occurrence`) now live in ONE file: `server/lib/gated-actions.ts` (`GATED_ACTION_LIST` tuple + `GATED_ACTIONS` set). It is re-imported by `approve-proposal.ts` (`ACTION_ALLOWLIST`), `propose-action.ts` (Zod enum), AND `mobile-admin-tools.ts` (defensive filter). **This replaces the old "update both files" gate-atomicity rule (2026-06-18)** — the lists can no longer drift. Gated verbs remain reachable on web only via `propose-action` → `approve-proposal`.

### Unit-test proof (AI-02)

- `server/lib/mobile-admin-tools.test.ts` (vitest, `vitest.unit.config.ts`) asserts every gated verb is structurally absent from the built tool list and that the exposed set contains no mutating verb. `gated-actions.ts` and `mobile-admin-tools.ts` are kept pure (no `@agent-native/core` import) so they remain importable under the unit runner (BD4-01 ESM/CJS caveat).

### Sanctioned server-side-LLM divergence

- Like the member coach agent, the mobile admin agent calls the LLM **server-side** (a manual Anthropic tool loop under `runWithRequestContext({ userEmail: admin.email })`), because the Expo binary cannot use `sendToAgentChat`. This is an **intentional, sanctioned exception** to the root `delegate-to-agent` rule — not a bug to "fix". Tool results invalidate the client react-query caches so results reflect in app state (the four-area application-state contract).

## System Prompt

The system prompt for the agent chat is defined in `apps/staff-web/server/plugins/agent-chat.ts`. The prompt establishes the gym domain, the available tools, and the suggest-and-act posture. Do not duplicate it here — edit the plugin if you change it.

## Forbidden Vocabulary

This deploy is a gym product. The agent must never use:

- "email", "Gmail", "inbox" (in the email sense), "thread" (in the email sense)
- "Starred", "Important", "Archive", "Drafts"
- "labels" (in the Gmail sense), "mail filters"

The word "Inbox" in this product refers to the WhatsApp conversations list (`/gymos/inbox`), not email.

## Member API — Calorie Counter Endpoints

In addition to the staff agent actions above, the member calorie counter exposes several `/api/m/*` endpoints consumed by `packages/mobile-app`. All are gated by `requireDemoMember(request)` (demo-mode header check).

| Endpoint                    | Method | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/api/m/me`                 | GET    | Role surface (no member row required) — `{ role, userId, email, trainerId }` for any authenticated caller (member/teacher/admin); 401 if unauthenticated. `role` from `resolveRole` (RUNSTUDIO_TEACHER_EMAILS / RUNSTUDIO_OPERATOR_EMAILS); teacher `trainerId` resolved from `trainers.user_id`. The mobile client branches UI by role (MA3 / TCH-01, TCH-03).                                                                                                                                                                                                                                                                                                                                                                                            |
| `/api/m/profile`            | GET    | Member profile + today's macro targets and progress                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `/api/m/food-entries`       | GET    | List food diary entries for a date (`?date=YYYY-MM-DD`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `/api/m/food-entries`       | POST   | Log a food entry; request `{ foodItem: { name, kcalPer100g, proteinPer100g?, carbsPer100g?, fatPer100g?, source?, barcode? }, quantityG, mealType, entrySource? }`. `foodItem.source` accepts `"openfoodfacts" \| "custom" \| "llm_estimate"`. `entrySource` accepts `"manual" \| "barcode" \| "search" \| "favourite" \| "agent"` (defaults to `"barcode"` if barcode present, else `"search"`).                                                                                                                                                                                                                                                                                                                                                                     |
| `/api/m/foods/search`       | GET    | Search Open Food Facts proxy (`?q=`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/api/m/foods/barcode/:ean` | GET    | Barcode lookup via Open Food Facts proxy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `/api/m/foods/analyze`      | POST   | **AI calorie + macro estimate** — calls Claude (`claude-sonnet-4-6`) with a vision block (if `image` provided) and/or text description. Request: `{ image?: string (bare base64 or data URL), description?: string, mealHint?: string }` — at least one of `image`/`description` required. Returns `{ ok: true, estimate: { foodName, kcalPer100g, proteinPer100gG, carbsPer100gG, fatPer100gG, suggestedQuantityG, confidence: "low"\|"medium"\|"high", note } }` or `{ ok: false, error }`. Consumed by `packages/mobile-app/app/food-ai.tsx` (reached from the Food tab "+" → "AI estimate"). Logging reuses `POST /api/m/food-entries` with `foodItem.source:"llm_estimate"` + `entrySource:"agent"` — no DB migration required (both enum values already exist). |
| `/api/m/schedule`           | GET    | Upcoming class occurrences. **MEM-01 anonymous read branch:** resolved via `getOptionalMember` (never 401) so anyone can browse without a Bearer. An anonymous caller gets the browse-only schedule (occurrences + counts), every `isBookedByMe:false`, and NO member-scoped data — the per-member booked-set query is skipped. A signed-in member additionally gets their booked flags. This honors the bearer-gate rule because an anonymous read asserts no identity and returns no member-scoped data; all WRITE endpoints keep `requireMember`/`requireMemberOrDemo`.                                                                                                                                                                                                                                                                                                                                                       |
| `/api/m/bookings`           | POST   | Book a class occurrence — **MEM-03 atomic transaction:** ONE `db.transaction` does an idempotency pre-check + `FOR UPDATE` occurrence lock + capacity check + FIFO active-pass pick + booking insert (`pass_id` set) + a `+1` `pass_debits` row (`reason:'class_booking'`). **C47 category gate:** FIFO pick now enforces pass_type category compatibility — `null pass_type_id` = legacy allow-all (existing members never break); `all_categories=true` = any class; otherwise `allowed_categories` JSON array must include the occurrence's class category (or class has no category → compatible). Errors: 402 `{error:'NO_PASS'}` (no active pass with credit), **403 `{error:'NO_COMPATIBLE_PASS'}`** (has credit but no pass type allows this class category), 409 `{error:'CAPACITY_FULL'}`, 409 `{error:'OCCURRENCE_UNAVAILABLE'}`, 404 `{error:'OCCURRENCE_NOT_FOUND'}`. Pass is debited **on booking**, never on purchase; refunds via `cancel-occurrence` (`-1`) reconcile against the same `pass_id`.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `/api/m/purchase`           | POST   | Initiate a Stripe checkout for a product                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `/api/m/teacher/schedule`   | GET    | **Teacher (TCH-01)** — occurrences assigned to the requesting teacher (`class_occurrences.trainer_id === their trainerId`), next 7 days, status `scheduled`, with class metadata. Gated by `requireTeacher` (401/403 non-teachers). An unlinked teacher (`trainerId` null) or one with no upcoming sessions gets HTTP 200 `{ items: [], trainerLinked }` — an empty state, NOT an error.                                                                                                                                                                                                                                                                                                                                                              |
| `/api/m/teacher/roster`     | GET    | **Teacher (TCH-01)** — `?occurrenceId=` returns `booked`+`attended` bookings joined to member name (`{bookingId, memberId, firstName, lastName, status}`), ONLY for an occurrence the teacher owns. Ownership-gated by `trainer_id` (403 for a foreign occurrence; a null `trainerId` always 403s); 400 without `occurrenceId`; 404 unknown occurrence.                                                                                                                                                                                                                                                                                                                                                                                              |
| `/api/m/teacher/check-in`   | POST   | **Teacher (TCH-02)** — `{ bookingId }` marks attendance ONLY by **calling** the existing `mark-booking-attended` chokepoint (`mod.default.schema` + `mod.default.run`). There is NO new attendance write path here — the status flip and the Meta `Schedule` CAPI event both fire inside the chokepoint (single write path preserved). Ownership-gated by `trainer_id` (booking → occurrence → trainer): a teacher can only check in members for sessions they own (403 otherwise; null `trainerId` always 403s). No new agent LLM tool is added (teachers have no AI surface — TCH-03), so the four-area "Actions" obligation is satisfied by documentation only.                                                                                       |

| `/api/m/checkin`            | POST   | **Member self-check-in (DE6-01)** -- `{ occurrenceId }` (extracted client-side from the kiosk QR `runstudio-checkin:<id>`). Gated by `requireMember`. Validates occurrence is `scheduled` + inside temporal window `[startsAt-45m, endsAt+15m]` + member has a booked/attended booking (else 409 NOT_BOOKED / CHECKIN_WINDOW_CLOSED / OCCURRENCE_UNAVAILABLE). Already attended: idempotent 200 without re-calling chokepoint. Otherwise calls mark-booking-attended.run (the sole attendance writer). Returns `{ attended: true, className, startsAt }`. |
| `/api/m/admin/check-in`     | POST   | **Admin tap-to-check-in (DE6-02)** -- `{ bookingId }`. Gated by requireAdmin (lives under the public /api/m path but self-gates inside, same pattern as /api/m/admin/agent/stream). NO trainer-ownership check -- admin can check in any member for any class. Calls mark-booking-attended.run (the sole attendance writer). Returns `{ attended: true }` or `{ error: BOOKING_NOT_FOUND|BOOKING_CANCELLED }`. Powers the /gymos/kiosk roster tap. |

> **DE6 single-attendance-writer invariant.** Both /api/m/checkin (member self-scan) and /api/m/admin/check-in (kiosk roster tap) are pure callers of mark-booking-attended.run. There is still **exactly one attendance write path**: actions/mark-booking-attended.ts. Neither new route writes bookings.status directly. The /gymos/kiosk page is the admin-gated tablet check-in surface (per-class QR + tap roster); it is agent-navigable via navigate({view:"kiosk"}). The mobile app consumes /api/m/checkin as a regular API endpoint -- not bound by the four-area contract.

## Adding a New Gym Action

1. Create `apps/staff-web/actions/<action-name>.ts` using `defineAction` from `@agent-native/core`.
2. Set `http: { method: "GET" }` for read actions, no `http` key for mutations.
3. Mutations on ownable resources MUST use `accessFilter` / `assertAccess` per root AGENTS.md. Gym domain tables (`gym_members`, `bookings`, `passes`, etc.) do NOT use `ownableColumns()` — they are single-tenant by design, so add a `// guard:allow-unscoped` comment instead.
4. Restart the dev server so `.generated/actions-registry.js` picks up the new action.
5. Document the action in this file's Agent Actions table.
6. If the action should be callable by the agent, add it to the system prompt tool list in `agent-chat.ts`.

> **Two-exposure rule — AE3 members + campaigns actions.** `update-member` (Members tab) and `save-segment` (Campaigns tab) are exposed to the agent: action files are in `actions/` (auto-registered into `.generated/actions-registry.ts`) AND named in the `agent-chat.ts` system prompt Members + Campaigns sections. Both are DIRECT (no propose-action gate). `update-member` structurally excludes consent/opt-in via a `.strict()` schema — the agent can never flip `marketing_consent` or `whatsapp_opt_in`; the system prompt also instructs it to refuse any such request.

> **Two-exposure rule — AE2 schedule actions.** The create path (`create-class-definition`, `create-class-occurrence`) and the new schedule actions (`set-occurrence-capacity`, `update-class-definition`, `mark-occurrence-complete`, and the gated `cancel-occurrence` / `reschedule-occurrence`) are exposed to the agent: action files are in `actions/` and entered in `.generated/actions-registry.ts`, AND named in the `agent-chat.ts` system prompt Schedule section. `cancel-occurrence` and `reschedule-occurrence` are reachable only through `propose-action` — never as direct tools.

> **Two-exposure rule — LP3 trainer actions.** `list-trainers` (Tier 1 read), `create-trainer`, and `update-trainer` are exposed to the agent: action files in `actions/` (auto-registered into `.generated/actions-registry.ts`) AND named in the `agent-chat.ts` system prompt Schedule section AND documented in the Agent Actions table above. All three are DIRECT (no propose-action gate). All gym-table queries carry `// guard:allow-unscoped — single-tenant gym tables`. `create-class-occurrence` now also accepts and persists optional `trainerId` and `location` fields (LP3 v24/v25 columns) — the AGENTS.md table row has been updated accordingly.

> **Two-exposure rule — MPV Phase 2 schedule-rule actions.** `create-schedule-rule`, `update-schedule-rule`, and `deactivate-schedule-rule` are exposed to the agent: action files in `actions/` (auto-registered into `.generated/actions-registry.ts`) AND named in the `agent-chat.ts` system prompt Schedule section AND documented in the Agent Actions table above. All three are DIRECT (no propose-action gate). DST-correct time conversion uses `server/lib/recurrence-generator.ts` (pure, zero-dep, BST/GMT aware). `class_schedule_rules` table added in migrations v27-v30; `class_occurrences.rule_id` column added in v28; partial unique index in v29 backs idempotent `ON CONFLICT DO NOTHING` materialiser inserts. Nightly materialiser (`class-materialize` queue, 04:00 UTC) is registered in `services/worker`.

> **MC2 LIFE-03 attendance chokepoint — not a two-exposure action.** `mark-booking-attended` (MC2 LIFE-03) is the attendance chokepoint. It is registered as an action file but intentionally NOT added to the `agent-chat.ts` system prompt (D-11: minimal backend transition, not an agent surface). It carries `guard:allow-unscoped` on gym/attribution tables and enqueues the Schedule CAPI event best-effort (D-17).

> **Two-exposure rule — AE1 forms actions.** The seven forms actions above (`create-form`, `update-form-fields`, `update-form-meta`, `unpublish-form`, `archive-form`, `restore-form`, and the gated `publish-form`) are exposed to the agent: action files are in `actions/` (auto-registered) AND named in the `agent-chat.ts` system prompt Forms section. `publish-form` is reachable only through `propose-action` — never as a direct tool.

> **Two-exposure rule — CV2 content actions.** The five mutation content actions (`content-create-document`, `content-update-document`, `content-rename-document`, `content-duplicate-document`, `content-delete-document`) plus the two read actions (`content-list-documents`, `content-get-document`) are exposed to the agent: action files are in `actions/` (auto-registered into `.generated/actions-registry.ts`) AND named in the `agent-chat.ts` system prompt Content tab section AND documented in the Agent Actions table above. All content actions are DIRECT (no propose-action gate) — staff-only authoring, like `update-member`. CV4 adds `content-set-status` — also DIRECT, two-exposed in the same way — which makes status mutable to `'published'` (the old "stays 'draft' until CV4" caveat is now realized). Publishing exposes the document via /api/m/content (member API) and /c/{slug} (public SSR); only published items reach members.

> **Two-exposure rule — CV3 video actions.** The five mutation video actions (`video-create-composition`, `video-update-composition`, `video-rename-composition`, `video-duplicate-composition`, `video-delete-composition`) plus the two read actions (`video-list-compositions`, `video-get-composition`) are exposed to the agent: action files are in `actions/` (auto-registered into `.generated/actions-registry.ts`) AND named in the `agent-chat.ts` system prompt Video tab section AND documented in the Agent Actions table above. All video actions are DIRECT (no propose-action gate) — staff-only authoring. `spec` is a structured JSON object validated by `VideoSpecSchema` from `server/lib/video-spec.ts` before every write — malformed specs are rejected (`{error:'INVALID_SPEC'}`) and never persisted. CV4 adds `video-set-status` — also DIRECT, two-exposed — which makes status mutable to `'published'` (the old "deferred to CV4" caveat is now realized). Publishing exposes the composition via /v/{slug} (public SSR); only published compositions appear there. The agent uses `fromClass` to build promo specs from class details, and always passes the COMPLETE spec to `video-update-composition` (replaces, not merges).

> **Two-exposure rule — CV4 publish pipeline actions.** `content-set-status` and `video-set-status` follow the same two-exposure pattern: action files in `actions/` (auto-registered) AND named in `agent-chat.ts` Content/Video tab sections AND documented in the Agent Actions table above. Both are DIRECT (no approval gate). No `http` key (POST-only mutations). Both carry `// guard:allow-unscoped — single-tenant`. Only `status='published'` items are ever exposed by /api/m/content, /c/:slug, or /v/:slug — every surface filters on published.

## Data Sources

| Table | Contents |
|-------|----------|
| `video_compositions` | In-browser video compositions — id (text PK), title (text), spec (JSON TEXT — VideoSpec: format/fps/durationInFrames/scenes), status (draft/published), slug (text), created_at / updated_at (text). Single-tenant (no studio_id, no ownableColumns). Reads carry `// guard:allow-unscoped — single-tenant video`. CV4: status is mutable to 'published' via video-set-status; published compositions appear at /v/{slug}. |
| `content_documents` | Content documents — id (text PK), title (text), body (Tiptap HTML), status (draft/published), slug (text nullable), created_at / updated_at (text). Single-tenant. CV4: status is mutable to 'published' via content-set-status; published documents exposed via /api/m/content (member API) and /c/{slug} (public SSR). |
| `studio_brain_docs` (row id=`brand-styling`) | **Brand & Styling source of truth.** The `brand-styling` row in `studio_brain_docs` stores the studio's visual identity as a JSON body matching the `TenantBrand` interface (`server/lib/tenant-brand.ts`). Fields: displayName, fontFamily, googleFontsHref, primary, primaryText, secondaryAccent, ink, bg, bgAlt, radius, logoUrl. Seeded by `brain-init` with defaults from `DEFAULT_TENANT_BRAND`. Resolved at SSR time by `getTenantBrand()` (`server/lib/tenant-brand-resolver.ts`) with a 30s in-process cache; falls back to `DEFAULT_TENANT_BRAND` on any error. Written only via `update-brain-doc` with `id:'brand-styling'`; cache is invalidated on every write. Never write this row directly via SQL — always go through the action so the cache is invalidated. |

> **C47 Pass-type catalog (staff UI only — not agent LLM tools).** `list-pass-types`, `create-pass-type`, and `update-pass-type` are staff catalog UI actions, NOT agent tools. They are registered in `actions/` (auto-registered into `.generated/actions-registry.ts`) but are NOT named in `agent-chat.ts` and do NOT appear in the mobile admin allow-list. The catalog surface is `/gymos/catalog` (admin-only tab). Category-based compatibility model: each `pass_types` row carries `all_categories` (BOOLEAN — books any class) or an `allowed_categories` JSON string array (matched against `class_definitions.category`). A `passes` row with `pass_type_id = null` is a legacy allow-all pass — never restricts booking. Booking enforces this inside the atomic transaction: funded-but-incompatible → 403 `NO_COMPATIBLE_PASS`; no-credit → 402 `NO_PASS`. Agent nav: `navigate({view:'catalog'})` routes to `/gymos/catalog` via GymosNavBridge.

## Conventions Inherited from Root AGENTS.md

- `defineAction` is the only path for new operations (Six Rules #3)
- shadcn/ui primitives are mandatory; no custom modals or browser dialogs
- Tabler icons only (no emojis as icons)
- TypeScript everywhere (`.ts`/`.tsx`)
- Optimistic UI on every mutation
- No breaking database changes — strictly additive only
- No unscoped queries on ownable resources

Read the root `AGENTS.md` at the workspace root for the full conventions list.
