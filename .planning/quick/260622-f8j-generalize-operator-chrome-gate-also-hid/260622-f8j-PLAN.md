---
phase: quick-260622-f8j
plan: 01
type: execute
wave: 1
depends_on: [quick-260622-e4a]
files_modified:
  - packages/core/src/client/AgentPanel.tsx
  - packages/core/src/client/AssistantChat.tsx
  - apps/staff-web/app/components/layout/AppLayout.tsx
  - .changeset/260622-f8j-operator-chrome-gate.md
autonomous: true
requirements: [QUICK-260622-f8j]
must_haves:
  truths:
    - "A non-operator (session email not in operatorEmails) sees NO settings gear, NO Workspace button, NO Feedback button, and NO model picker in the /gymos agent sidebar"
    - "A non-operator can still send messages (server uses App Default Model), still sees the Act mode picker, the attachment +, the mic, and send"
    - "A non-operator cannot reach Workspace/resources mode via any back door (no Workspace button; no event handler exists for it)"
    - "An operator (Patrick) sees all four chrome elements exactly as before"
    - "Upstream agent-native apps and the non-gymos (email) AgentSidebar mount are unaffected — chrome stays visible (default true)"
  artifacts:
    - path: "packages/core/src/client/AgentPanel.tsx"
      provides: "showOperatorChrome prop gating gear + Workspace button + FeedbackButton + back-door guards; threads flag to MultiTabAssistantChat"
      contains: "showOperatorChrome"
    - path: "packages/core/src/client/AssistantChat.tsx"
      provides: "showOperatorChrome prop on AssistantChatProps; composer passes undefined model props when false (mirrors PromptComposer showModelSelector=false)"
      contains: "showOperatorChrome"
    - path: "apps/staff-web/app/components/layout/AppLayout.tsx"
      provides: "showOperatorChrome={isOperator} on the /gymos AgentSidebar mount"
      contains: "showOperatorChrome={isOperator}"
    - path: ".changeset/260622-f8j-operator-chrome-gate.md"
      provides: "@agent-native/core patch changeset"
      contains: "@agent-native/core"
  key_links:
    - from: "AppLayout.tsx /gymos AgentSidebar"
      to: "AgentPanel showOperatorChrome"
      via: "prop pass-through (AgentSidebar -> AgentPanel @ ~2374)"
      pattern: "showOperatorChrome"
    - from: "AgentPanel MultiTabAssistantChat render (~1354)"
      to: "AssistantChat composer model-picker gate"
      via: "showOperatorChrome flows through ...props -> AssistantChat -> AssistantChatInner"
      pattern: "showOperatorChrome"
---

<objective>
Generalize the existing operator-only `showSettingsGear` gate (shipped in quick task 260622-e4a) into a single broader flag `showOperatorChrome` that, when `false`, ALSO hides three more operator-only chrome elements in the agent sidebar:

1. The settings gear (already gated — keep behavior, just rename the flag).
2. The **Workspace** button in the AgentPanel header (`IconLayoutGrid`, `switchMode("resources")`).
3. The **FeedbackButton** (`variant="icon"`) in the header actions row.
4. The **model picker** ("Sonnet 4.6" dropdown) in the composer — rendered by `TiptapComposer`'s `<ModelSelector>` inside `AssistantChatInner`'s composer block.

KEEP VISIBLE for everyone: the "Act" mode picker (`ModeSelector` — `execMode`/`onExecModeChange`), the attachment "+", the mic, and send. Only the MODEL dropdown is hidden, never the mode picker.

Default `showOperatorChrome` is `true` so upstream agent-native apps and the non-gymos (email) AgentSidebar mount are unaffected. The only incoming value comes from `AppLayout.tsx`, which feeds its existing `isOperator` (unchanged: session email ∈ `operatorEmails`, default `[patrickalexanderross@outlook.com]`).

Purpose: White-label the /gymos agent sidebar for gym staff (non-operators) so they never see framework-infra chrome (settings, workspace files/agents/skills/tasks, feedback, model selection) — only the gym-relevant chat surface.
Output: One renamed/broadened core prop wired AppLayout -> AgentSidebar -> AgentPanel -> MultiTabAssistantChat -> AssistantChat -> composer, plus a `@agent-native/core` patch changeset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260622-e4a-revert-settings-panel-trim-gate-sidebar-/260622-e4a-SUMMARY.md

# Constraints (from root + staff-web AGENTS.md)
- `@agent-native/core` source change → REQUIRES a `.changeset/*.md` ("@agent-native/core": patch). staff-web is private (no changeset).
- TypeScript only. Tabler icons only (no change to icons here — only show/hide existing ones).
- **Shared-composer rule (CRITICAL):** Do NOT build a one-off composer, textarea, model picker, or selector. Only toggle EXISTING props on the existing `TiptapComposer` by passing `undefined` for model props — exactly mirroring `PromptComposer`'s `showModelSelector=false` path. The `<ModelSelector>` render condition in `TiptapComposer` is `selectedModel && availableModels && onModelChange` (TiptapComposer.tsx:1860) — passing `undefined` for any of those hides it WITHOUT touching TiptapComposer. Do NOT modify TiptapComposer.
- Additive / default-true. Preserve upstream behavior.
- Do NOT create or switch git branches.
- `packages/core/dist/client/*.d.ts` is gitignored; e4a patched it locally for the staff-web typecheck against pre-built dist. If `apps/staff-web` typecheck fails resolving the new prop on the dist `.d.ts`, patch the local dist declaration the same way e4a did (Vercel regenerates dist from source on build).

<interfaces>
<!-- Exact lines verified in the files on 2026-06-22. The ~refs in the task brief were approximate; these are confirmed. -->

packages/core/src/client/AgentPanel.tsx — current `showSettingsGear` occurrences (ALL become `showOperatorChrome`):
- 383: `showSettingsGear?: boolean;`  (AgentPanelProps interface; JSDoc @ 382)
- 495: `showSettingsGear = true,`  (AgentPanelInner destructure)
- 540: `(saved === "settings" && showSettingsGear)`  (initial-mode restore guard — already blocks restoring "settings")
- 582: `if (!showSettingsGear) return;`  (open-settings event back-door guard)
- 597: `}, [switchMode, showSettingsGear]);`  (open-settings useEffect deps)
- 800: `{showSettingsGear && (`  (gear <Tooltip> wrapper)
- 822: `[codeAccessEnabled, codeUnavailableDescription, showCliMode, showSettingsGear],`  (renderModeButtons useMemo deps)
- 1993: `showSettingsGear?: boolean;`  (AgentSidebarProps interface; JSDoc @ 1992)
- 2011: `showSettingsGear,`  (AgentSidebar destructure)
- 2374: `showSettingsGear={showSettingsGear}`  (AgentSidebar -> AgentPanel pass-through)

Workspace button (NOT yet gated) — AgentPanel.tsx:779-799:
```
779  <Tooltip>
780    <TooltipTrigger asChild>
781      <button
782        onClick={() => switchMode("resources")}
783        aria-label="Workspace files, agents, skills, and tasks"
...
792        <IconLayoutGrid size={14} />
793        Workspace
794      </button>
795    </TooltipTrigger>
796    <TooltipContent>Workspace files, agents, skills, and tasks</TooltipContent>
797  </Tooltip>
```
This whole <Tooltip> (779-799) must become conditional on showOperatorChrome.

Initial-mode restore guard — AgentPanel.tsx:533-545 (must NOT restore into "resources" when chrome hidden):
```
536  if (
537    saved === "chat" ||
538    saved === "cli" ||
539    saved === "resources" ||
540    (saved === "settings" && showSettingsGear)   // -> showOperatorChrome
541  )
542    return saved;
```
Line 539 `saved === "resources"` must also be gated so a persisted "resources" mode is NOT restored for non-operators.

NO `agent-panel:open-resources` event handler exists (grep confirmed only `agent-panel:open-settings`). The only resources entry point is the Workspace button + persisted-mode restore — both handled above. No extra event guard needed.

FeedbackButton (NOT yet gated) — AgentPanel.tsx:825-865 `renderHeaderActions`:
- 833: `<FeedbackButton variant="icon" side="bottom" align="end" />`  (wrap in `{showOperatorChrome && (...)}`)
- 864: `[onCollapse, canUseCodeTools, onToggleFullscreen, isFullscreen],`  (renderHeaderActions useMemo deps — ADD showOperatorChrome)

MultiTabAssistantChat render in AgentPanel — AgentPanel.tsx:1354-1369 (thread the flag here):
```
1354  <MultiTabAssistantChat
1355    apiUrl={apiUrl}
...
1366    storageKey={storageKey}
1367    scope={scope}
1368    browserTabId={browserTabId}
1369  />
```
Add `showOperatorChrome={showOperatorChrome}`. (MultiTabAssistantChatProps = Omit<AssistantChatProps,...> & {...}, so once the prop is on AssistantChatProps it is accepted here and flows through `...props` to <AssistantChat> with NO change needed in MultiTabAssistantChat.tsx.)

packages/core/src/client/AssistantChat.tsx — model picker source:
- AssistantChatProps interface @ 3159; model props @ 3217-3234 (selectedModel/defaultModel/selectedEngine/selectedEffort/availableModels/onModelChange/onEffortChange). ADD `showOperatorChrome?: boolean;` here.
- `AssistantChatInner` destructure @ 3293-3323 (this component renders the composer). ADD `showOperatorChrome = true,` to the destructure.
- Composer block @ 4855-4948 renders `<TiptapComposer>`. Current model-prop passing @ 4886-4890:
```
4886  selectedModel={selectedModel ?? defaultModel}
4887  selectedEffort={selectedEffort}
4888  availableModels={availableModels}
4889  onModelChange={onModelChange}
4890  onEffortChange={onEffortChange}
```
  Note 4882-4883 pass `execMode`/`onExecModeChange` (the Act mode picker) — DO NOT touch these.
- Outer `AssistantChat` forwardRef (@ 4957) spreads `{...props}` into `<AssistantChatInner>` (@ 5017-5023), so the flag flows automatically; no change needed at the outer component beyond the interface.

PromptComposer mirror pattern — PromptComposer.tsx:468-485 (when showModelSelector is false → pass `undefined` for composerModel/Effort/Groups + handlers). Mirror this for the model props above.

TiptapComposer ModelSelector condition (DO NOT EDIT this file) — TiptapComposer.tsx:1858-1878:
```
1860  {selectedModel && availableModels && onModelChange && (
1861    <ModelSelector ... />          // MODEL picker — hidden when any prop is undefined
1868  )}
1869  {execMode && onExecModeChange && (
1870    <ModeSelector ... />            // ACT mode picker — stays visible (separate condition)
1876  )}
```

apps/staff-web/app/components/layout/AppLayout.tsx:158-175 — /gymos AgentSidebar mount:
- 170: `showSettingsGear={isOperator}`  → becomes `showOperatorChrome={isOperator}`
- `isOperator` computed @ 131-138 (unchanged). The non-gymos mount (@ ~184+) does NOT pass the prop → stays default true → unaffected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename showSettingsGear -> showOperatorChrome in AgentPanel + gate Workspace button & FeedbackButton & resources back-door, thread to MultiTabAssistantChat</name>
  <files>packages/core/src/client/AgentPanel.tsx</files>
  <action>
In packages/core/src/client/AgentPanel.tsx make these edits (verified line numbers — confirm exact surrounding text before each edit):

A. RENAME the existing flag everywhere `showSettingsGear` appears to `showOperatorChrome`. The 10 sites (lines 383, 495, 540, 582, 597, 800, 822, 1993, 2011, 2374). Update the two JSDoc comments (@ 382 and @ 1992) to describe the broader meaning, e.g.:
   "Show operator-only chrome (settings gear, Workspace button, Feedback button, model picker) in the agent sidebar. Default: true (upstream behavior). Set false to hide framework/operator chrome for white-labelled deploys (e.g. gym staff)."
   Keep `= true` default at 495 and the optional `?` on both interface decls (383, 1993).

B. GATE THE WORKSPACE BUTTON. Wrap the entire Workspace `<Tooltip>...</Tooltip>` block (currently AgentPanel.tsx:779-799, the one with `onClick={() => switchMode("resources")}`, `IconLayoutGrid`, and text "Workspace") in `{showOperatorChrome && ( ... )}`. This is in the same `renderModeButtons` useMemo whose deps array (822) already lists showOperatorChrome after the rename — no deps change needed for this one.

C. GATE THE RESOURCES BACK DOOR in the initial-mode restore (current lines 536-541). Change line 539 from `saved === "resources" ||` to `(saved === "resources" && showOperatorChrome) ||` so a persisted "resources" mode is NOT restored for non-operators (mirrors the existing `(saved === "settings" && showOperatorChrome)` guard on the next line). Do NOT add any new event listener — grep confirms there is NO `agent-panel:open-resources` handler; the Workspace button (gated in B) and this restore are the only resources entry points.

D. GATE THE FEEDBACK BUTTON. In `renderHeaderActions` (starts ~825), wrap the `<FeedbackButton variant="icon" side="bottom" align="end" />` (currently line 833) in `{showOperatorChrome && (...)}`. Then ADD `showOperatorChrome` to the `renderHeaderActions` useMemo deps array (currently line 864: `[onCollapse, canUseCodeTools, onToggleFullscreen, isFullscreen]`) so the memo recomputes when the flag changes.

E. THREAD THE FLAG TO THE COMPOSER. In the `<MultiTabAssistantChat ... />` render (currently lines 1354-1369), add a new prop line `showOperatorChrome={showOperatorChrome}` (e.g. after `browserTabId={browserTabId}` on line 1368). This is accepted because MultiTabAssistantChatProps extends AssistantChatProps (Omit), and the prop is added to AssistantChatProps in Task 2. No edits to MultiTabAssistantChat.tsx are required — it already spreads `...props` to <AssistantChat>.

Do NOT touch the Act mode picker, attachment, mic, send, CLI button, or any other control. Run prettier on the file after editing.
  </action>
  <verify>
    <automated>cd packages/core && npx prettier --write src/client/AgentPanel.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>tsc exits 0. `showSettingsGear` no longer appears in AgentPanel.tsx; `showOperatorChrome` appears at all 10 former sites plus the Workspace gate, the resources-restore gate, the FeedbackButton gate + its deps, and the MultiTabAssistantChat pass-through. Act mode picker / attachment / mic / send / CLI untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add showOperatorChrome to AssistantChat; hide the model picker by passing undefined model props (mirror PromptComposer showModelSelector=false)</name>
  <files>packages/core/src/client/AssistantChat.tsx</files>
  <action>
In packages/core/src/client/AssistantChat.tsx:

A. ADD THE PROP TO THE INTERFACE. In `AssistantChatProps` (interface starts @ 3159), near the other composer/model props (3217-3234), add:
   `/** Show operator-only chrome — the composer model picker. Default: true. When false, the model selector is hidden (server falls back to the App Default Model) and the Act mode picker stays visible. Mirrors PromptComposer showModelSelector=false. */`
   `showOperatorChrome?: boolean;`

B. DESTRUCTURE IT in `AssistantChatInner` (the inner forwardRef whose destructure block is 3293-3323 — the component that renders the composer). Add `showOperatorChrome = true,` to that destructure (e.g. after `onForkChat,` on line 3322, before the closing `},` on 3323). The outer `AssistantChat` (@ 4957) already spreads `{...props}` into `<AssistantChatInner>` (5017-5023), so no change is needed there beyond the interface.

C. HIDE THE MODEL PICKER at the composer block. The current `<TiptapComposer>` model props are lines 4886-4890:
```
selectedModel={selectedModel ?? defaultModel}
selectedEffort={selectedEffort}
availableModels={availableModels}
onModelChange={onModelChange}
onEffortChange={onEffortChange}
```
   Replace these five prop values so that when `showOperatorChrome` is false they all pass `undefined` (mirroring PromptComposer.tsx:468-485). Prefer computing five locals just above the `return (...)`/composer JSX for readability, then using them in the props — e.g.:
```
const composerModel = showOperatorChrome ? (selectedModel ?? defaultModel) : undefined;
const composerEffort = showOperatorChrome ? selectedEffort : undefined;
const composerAvailableModels = showOperatorChrome ? availableModels : undefined;
const composerOnModelChange = showOperatorChrome ? onModelChange : undefined;
const composerOnEffortChange = showOperatorChrome ? onEffortChange : undefined;
```
   then set:
```
selectedModel={composerModel}
selectedEffort={composerEffort}
availableModels={composerAvailableModels}
onModelChange={composerOnModelChange}
onEffortChange={composerOnEffortChange}
```
   (Place the const declarations in component-body scope, not inside the JSX. If a closer in-scope spot is cleaner, inline the ternaries directly in the five props instead — either is fine as long as all five become undefined when the flag is false.)
   TiptapComposer's `<ModelSelector>` then hides via its existing condition `selectedModel && availableModels && onModelChange` (TiptapComposer.tsx:1860) — DO NOT edit TiptapComposer. Leave `execMode`/`onExecModeChange` (4882-4883) and everything else in the composer block untouched, so the Act mode picker, attachment, mic, and send stay visible. Message sending still works — with no model override the server uses the App Default Model (same as PromptComposer's hidden path).

Run prettier on the file after editing.
  </action>
  <verify>
    <automated>cd packages/core && npx prettier --write src/client/AssistantChat.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>tsc exits 0. AssistantChatProps has `showOperatorChrome?: boolean`; AssistantChatInner destructures it with default true; the five `<TiptapComposer>` model props pass `undefined` when the flag is false. `execMode`/`onExecModeChange` and all other composer controls are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Rename the AppLayout prop to showOperatorChrome={isOperator}; add changeset; dual typecheck</name>
  <files>apps/staff-web/app/components/layout/AppLayout.tsx, .changeset/260622-f8j-operator-chrome-gate.md</files>
  <action>
A. AppLayout. In apps/staff-web/app/components/layout/AppLayout.tsx, on the /gymos `<AgentSidebar>` mount (currently line 170), rename `showSettingsGear={isOperator}` to `showOperatorChrome={isOperator}`. Do NOT change the `isOperator` computation (131-138) or the non-gymos AgentSidebar mount (which passes no such prop and must stay default-true). Update the nearby comment block (128-130 / 144-154) if it specifically names "settings gear" to instead say "operator chrome (settings, Workspace, Feedback, model picker)". Run prettier on the file.

B. Changeset. Create .changeset/260622-f8j-operator-chrome-gate.md with:
```
---
"@agent-native/core": patch
---

Broaden the operator-chrome gate: rename the `showSettingsGear` prop on AgentSidebar/AgentPanel to `showOperatorChrome` (default true, upstream-safe). When false it now hides not just the settings gear but also the Workspace button, the Feedback button, and the composer model picker (the Act mode picker, attachment, mic, and send stay visible; the server falls back to the App Default Model). Used by white-labelled deploys (e.g. RunStudio gym staff) to show non-operators a clean chat surface.
```
(staff-web is private — no changeset for it.)

C. dist .d.ts fallback (only if needed). If step D's staff-web typecheck fails because the pre-built `packages/core/dist/client/AgentPanel.d.ts` (gitignored) still declares the old `showSettingsGear` / lacks `showOperatorChrome`, patch the local dist `.d.ts` the same way e4a did: rename/add the prop in `dist/client/AgentPanel.d.ts` so the staff-web typecheck resolves it. This is a local-only patch (Vercel regenerates dist from source on build). Note it in the summary if done.

D. Dual typecheck (the gate for this whole plan).
  </action>
  <verify>
    <automated>cd packages/core && npx tsc --noEmit && cd ../../apps/staff-web && pnpm typecheck</automated>
  </verify>
  <done>Both typechecks exit 0. AppLayout passes `showOperatorChrome={isOperator}` on the /gymos mount only; the non-gymos mount is unchanged. `.changeset/260622-f8j-operator-chrome-gate.md` exists with "@agent-native/core": patch. No `showSettingsGear` references remain anywhere in packages/core/src or apps/staff-web/app.</done>
</task>

</tasks>

<verification>
- `grep -rn "showSettingsGear" packages/core/src apps/staff-web/app` returns ZERO matches (fully renamed).
- `grep -rn "showOperatorChrome" packages/core/src apps/staff-web/app` shows it in AgentPanel.tsx (interfaces, destructures, gates, pass-throughs), AssistantChat.tsx (interface + destructure + composer gate), and AppLayout.tsx (one /gymos mount).
- TiptapComposer.tsx is UNCHANGED (model-picker condition at 1860 untouched; relied on, not edited).
- MultiTabAssistantChat.tsx is UNCHANGED (prop flows through Omit<AssistantChatProps> + `...props`).
- The Act mode picker (`execMode`/`onExecModeChange`) is never gated — visible for everyone.
- `cd packages/core && npx tsc --noEmit` → exit 0.
- `cd apps/staff-web && pnpm typecheck` → exit 0.
</verification>

<success_criteria>
- One incoming prop from AppLayout (`showOperatorChrome={isOperator}`) gates all four operator-only chrome elements (gear, Workspace, Feedback, model picker) plus the resources back-door.
- Default true everywhere → upstream agent-native apps and the email AgentSidebar mount unaffected.
- Non-operators keep a working chat: Act mode picker, attachment, mic, send all visible; sending uses the App Default Model.
- No one-off composer/selector introduced; only existing props toggled (shared-composer rule honored).
- `@agent-native/core` patch changeset present; staff-web (private) has none.
- Both typechecks green. No git branch created/switched.
</success_criteria>

<output>
After completion, create `.planning/quick/260622-f8j-generalize-operator-chrome-gate-also-hid/260622-f8j-SUMMARY.md`
</output>
