---
phase: quick-260702-de6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/staff-web/app/routes/api.m.checkin.tsx
  - apps/staff-web/app/routes/api.m.admin.check-in.tsx
  - apps/staff-web/server/routes/api/m/checkin.post.ts
  - apps/staff-web/server/routes/api/m/admin/check-in.post.ts
  - apps/staff-web/app/routes/gymos.kiosk.tsx
  - apps/staff-web/app/components/gymos/GymosTopNav.tsx
  - apps/staff-web/app/components/gymos/GymosNavBridge.tsx
  - apps/staff-web/AGENTS.md
  - apps/staff-web/package.json
  - packages/mobile-app/app/checkin-scan.tsx
  - packages/mobile-app/app/_layout.tsx
  - packages/mobile-app/app/(tabs)/schedule.tsx
autonomous: true
requirements: [POST-DEMO-04-CHECKIN]
must_haves:
  truths:
    - "An operator on the /gymos/kiosk tablet page sees a per-class QR and a tap-to-check-in roster for the currently-running class"
    - "Tapping a member's name on the kiosk marks them attended instantly (optimistic), persisted via the mark-booking-attended chokepoint"
    - "A member in the Expo app taps 'Scan to check in', scans the kiosk QR with the camera, and is self-checked-in to that class"
    - "A member who scans a class they are NOT booked into sees a clear 'not booked' message and is NOT checked in"
    - "Every check-in path (kiosk tap + member self-scan) funnels through mark-booking-attended.run — no second attendance write path exists"
  artifacts:
    - path: "apps/staff-web/app/routes/api.m.checkin.tsx"
      provides: "Member self-check-in endpoint (requireMember, temporal window, booked-check, chokepoint call)"
    - path: "apps/staff-web/app/routes/api.m.admin.check-in.tsx"
      provides: "Admin tap-to-check-in endpoint (requireAdmin, chokepoint call, no trainer ownership check)"
    - path: "apps/staff-web/app/routes/gymos.kiosk.tsx"
      provides: "Admin-gated kiosk page — class picker, server-generated per-occurrence QR, tappable roster"
    - path: "packages/mobile-app/app/checkin-scan.tsx"
      provides: "Expo QR scanner screen that parses runstudio-checkin:<id> and POSTs /api/m/checkin"
  key_links:
    - from: "apps/staff-web/app/routes/api.m.checkin.tsx"
      to: "apps/staff-web/actions/mark-booking-attended.ts"
      via: "mod.default.run({bookingId})"
      pattern: "mark-booking-attended"
    - from: "apps/staff-web/app/routes/api.m.admin.check-in.tsx"
      to: "apps/staff-web/actions/mark-booking-attended.ts"
      via: "mod.default.run({bookingId})"
      pattern: "mark-booking-attended"
    - from: "packages/mobile-app/app/checkin-scan.tsx"
      to: "/api/m/checkin"
      via: "apiFetch POST { occurrenceId }"
      pattern: "api/m/checkin"
    - from: "apps/staff-web/app/routes/gymos.kiosk.tsx"
      to: "/api/m/admin/check-in"
      via: "fetch POST { bookingId } credentials:include"
      pattern: "api/m/admin/check-in"
---

<objective>
Build gym check-in (req #4 of the post-demo scope): an operator-login tablet kiosk in staff-web that shows a per-class QR + a tap-to-check-in roster, PLUS member self-check-in in the Expo mobile app by scanning that QR with the camera.

Purpose: replace bSport's check-in with a RunStudio-native flow. The kiosk covers members without the app (tap-your-name); the QR scan covers app members. Both prove RunStudio can run the front desk.

Output: two new self-gating endpoints (member `POST /api/m/checkin`, admin `POST /api/m/admin/check-in`), an admin `/gymos/kiosk` page with a server-generated QR, and a mobile scanner screen + entry point. NO DB migration — check-in only flips `bookings.status` via the existing `mark-booking-attended` chokepoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@apps/staff-web/AGENTS.md

Attendance chokepoint (the SOLE writer of bookings.status='attended' + Meta Schedule CAPI):
@apps/staff-web/actions/mark-booking-attended.ts

Mirror these patterns exactly (gating + ownership + `mod.default.run` chokepoint call, and the Nitro wrapper):
@apps/staff-web/app/routes/api.m.teacher.check-in.tsx
@apps/staff-web/app/routes/api.m.teacher.roster.tsx
@apps/staff-web/server/routes/api/m/teacher/check-in.post.ts
@apps/staff-web/server/routes/api/m/bookings.post.ts
@apps/staff-web/server/routes/api/m/admin/agent/stream.post.ts

Session helpers:
@apps/staff-web/server/lib/admin-session.ts
@apps/staff-web/server/lib/member-session.ts

Query patterns (occurrence lookup, temporal window, roster, booked-set):
@apps/staff-web/app/routes/api.m.bookings.tsx
@apps/staff-web/app/routes/api.m.schedule.tsx

Nav + kiosk page siblings:
@apps/staff-web/app/routes/gymos.tsx
@apps/staff-web/app/routes/gymos.catalog.tsx
@apps/staff-web/app/components/gymos/GymosTopNav.tsx
@apps/staff-web/app/components/gymos/GymosNavBridge.tsx
@apps/staff-web/server/plugins/auth.ts

Mobile:
@packages/mobile-app/lib/api.ts
@packages/mobile-app/lib/whoami.ts
@packages/mobile-app/components/BarcodeScanner.tsx
@packages/mobile-app/app/(tabs)/schedule.tsx
@packages/mobile-app/app/teacher-roster.tsx
@packages/mobile-app/app/_layout.tsx

<interfaces>
<!-- Contracts the executor needs — use directly, no exploration required. -->

Attendance chokepoint (apps/staff-web/actions/mark-booking-attended.ts):
```ts
export default defineAction({
  schema: z.object({ bookingId: z.string().min(1) }),
  run: async ({ bookingId }): Promise<
    { attended: true } | { error: "BOOKING_NOT_FOUND" | "BOOKING_CANCELLED" }
  >
});
// Idempotent: re-marking an already-'attended' booking is a no-op (no 2nd CAPI enqueue).
// Call it: const mod = await import("../../actions/mark-booking-attended.js");
//          const result = await mod.default.run({ bookingId });
```

Session gates:
```ts
// server/lib/admin-session.ts — throws Response 401 (no session) / 403 (not admin)
export async function requireAdmin(request: Request): Promise<{ email: string; userId: string }>;
// server/lib/member-session.ts — throws 401/403/409; returns the gym_members row
export async function requireMember(request: Request): Promise<Member>; // Member has .id, .firstName, .lastName, .email
```

Relevant schema (apps/staff-web/server/db/schema.ts) — all single-tenant, use `// guard:allow-unscoped — single-tenant gym tables`:
```ts
classOccurrences: { id, definitionId, startsAt /*ISO*/, endsAt /*ISO*/, capacity, status: 'scheduled'|'cancelled'|'completed', room, location, trainerId }
classDefinitions: { id, name, category, durationMin }
bookings:         { id, occurrenceId, memberId, status: 'booked'|'attended'|'no_show'|'cancelled', attendedAt, bookedAt }
gymMembers:       { id, firstName, lastName, email, phoneE164, userId }
```

Nitro wrapper depth (count `../` from the wrapper file back to apps/staff-web/, then into app/routes/):
- `server/routes/api/m/bookings.post.ts`        → `../../../../app/routes/api.m.bookings.js`         (4)
- `server/routes/api/m/teacher/check-in.post.ts`→ `../../../../../app/routes/api.m.teacher.check-in.js` (5)
- `server/routes/api/m/admin/check-in.post.ts`  → `../../../../../app/routes/api.m.admin.check-in.js`   (5)  ← this plan
- `server/routes/api/m/checkin.post.ts`         → `../../../../app/routes/api.m.checkin.js`            (4)  ← this plan

Mobile apiFetch (packages/mobile-app/lib/api.ts): `apiFetch(path, init?)` sends `Authorization: Bearer <token>`, throws `Error("<status>: <body>")` on non-2xx (message includes the JSON body, so match error codes with `String(err.message).includes("NOT_BOOKED")`).
</interfaces>
</context>

<constraints_recap>
- STAY ON master. No branch/worktree/stash.
- NO DB migration, NO schema change. Check-in only flips bookings.status via mark-booking-attended.run.
- Every attendance write goes through mark-booking-attended.run. Never write bookings.status='attended' anywhere else.
- Hand-written routes: gate via requireMember / requireAdmin at the top; carry `// guard:allow-unscoped — single-tenant gym tables` on every gym-table query. runWithRequestContext is NOT required here (no accessFilter/resolveAccess usage — all gym tables are single-tenant guard:allow-unscoped, exactly like the teacher check-in route which also omits it).
- /api/m/* is already public in auth.ts publicPaths (each route self-gates). The admin endpoint lives UNDER /api/m but gates with requireAdmin inside (same pattern as /api/m/admin/agent/stream).
- staff-web UI: shadcn/ui (Card, Button, Badge, Select all exist in app/components/ui/) + Tabler icons only, never emojis. Optimistic UI on the kiosk tap.
- Mobile: reuse existing RN conventions (Feather icons, useTheme, expo-camera CameraView). expo-camera is already installed — NO new native dep. NO QR-generation lib on mobile (it only scans; the kiosk generates the QR).
- QR payload is the stateless string `runstudio-checkin:<occurrenceId>`. No crypto/HMAC in v1 (documented as future hardening). Security rests on: member identity from Bearer, server temporal-window check, and the member owning a 'booked' booking.
- TS clean: `cd apps/staff-web && npx tsc --noEmit` must be clean for the NEW files. Pre-existing errors in mark-booking-attended.ts and member-session.test.ts are unrelated — do NOT touch them, ignore them in the diff of results.
- Commit each task: `feat(quick-260702-de6): ...`
</constraints_recap>

<tasks>

<task type="auto">
  <name>Task 1: Member self-check-in + admin check-in endpoints (both call the chokepoint)</name>
  <files>apps/staff-web/app/routes/api.m.checkin.tsx, apps/staff-web/app/routes/api.m.admin.check-in.tsx, apps/staff-web/server/routes/api/m/checkin.post.ts, apps/staff-web/server/routes/api/m/admin/check-in.post.ts</files>
  <action>
Create TWO React Router resource-route actions plus their Nitro wrappers. Both are pure CALLERS of `mark-booking-attended.run` — no new attendance write path.

(A) `app/routes/api.m.checkin.tsx` — member self-check-in. `export async function action({ request }: ActionFunctionArgs)`:
  1. `if (request.method !== "POST") return new Response("Method not allowed", { status: 405 });`
  2. `const member = await requireMember(request);` (throws 401/403 inside).
  3. Parse body `{ occurrenceId }` (string). Missing/invalid → 400 JSON `{error:"Missing occurrenceId"}`. (Client extracts occurrenceId from the QR string, so the body carries occurrenceId, not the raw token — the recommended cleaner contract.)
  4. Load the occurrence joined to class name + validate:
     - Query classOccurrences LEFT JOIN classDefinitions on definitionId, select `{ id, startsAt, endsAt, status, className: classDefinitions.name }` where `eq(classOccurrences.id, occurrenceId)` limit 1. `// guard:allow-unscoped — single-tenant gym tables`
     - Not found → 404 JSON `{error:"OCCURRENCE_NOT_FOUND"}`.
     - `status !== "scheduled"` → 409 JSON `{error:"OCCURRENCE_UNAVAILABLE"}`.
     - Temporal window: `const now = Date.now();` `const startMs = Date.parse(occ.startsAt);` `const endMs = Date.parse(occ.endsAt);` window is `[startMs - 45*60_000, endMs + 15*60_000]`. If `now < startMs - 45*60_000 || now > endMs + 15*60_000` → 409 JSON `{error:"CHECKIN_WINDOW_CLOSED"}`.
  5. Find THIS member's booking for the occurrence: select `{ id, status }` from bookings where `and(eq(occurrenceId), eq(memberId, member.id), inArray(status, ["booked","attended"]))` limit 1. `// guard:allow-unscoped`. None → 409 JSON `{error:"NOT_BOOKED"}`.
  6. If `booking.status === "attended"` → idempotent success: return 200 JSON `{attended:true, className: occ.className, startsAt: occ.startsAt}` WITHOUT re-calling the chokepoint.
  7. Otherwise call the chokepoint: `const mod = await import("../../actions/mark-booking-attended.js"); const parsed = mod.default.schema.safeParse({ bookingId: booking.id }); if (!parsed.success) return 400; const result = await mod.default.run(parsed.data);` If `("error" in result)` map to a JSON response (BOOKING_CANCELLED → 409, BOOKING_NOT_FOUND → 404). On success return 200 JSON `{attended:true, className: occ.className, startsAt: occ.startsAt}`.
  Use a small `jsonResponse(body, status)` helper (copy the one in api.m.bookings.tsx). Import `requireMember` from `../../server/lib/member-session`, `getDb, schema` from `../../server/db`, and `and, eq, inArray` from `drizzle-orm`.

(B) `app/routes/api.m.admin.check-in.tsx` — admin tap-to-check-in. Mirror api.m.teacher.check-in.tsx EXACTLY but:
  - Replace `requireTeacher` with `requireAdmin` from `../../server/lib/admin-session` (throws 401/403 at the top).
  - REMOVE the trainer-ownership lookup/403 entirely (an admin can check in anyone).
  - Body `{ bookingId }` (400 if missing). Call the chokepoint via `mod.default.schema.safeParse` + `mod.default.run` and return its result (`{attended:true}` | `{error:...}`). Keep the file header comment explaining it is a pure caller of the chokepoint, admin-gated, no ownership check.

(C) Nitro wrappers — copy `server/routes/api/m/teacher/check-in.post.ts` verbatim, changing ONLY the import path (see the depth table in <interfaces>):
  - `server/routes/api/m/checkin.post.ts` → imports `action` from `../../../../app/routes/api.m.checkin.js`
  - `server/routes/api/m/admin/check-in.post.ts` → imports `action` from `../../../../../app/routes/api.m.admin.check-in.js`
  Both use the `defineEventHandler` + `setResponseStatus` + parse-text-as-JSON pattern (mirror bookings.post.ts / teacher check-in.post.ts) so thrown Responses (401/403/404/409) forward as clean HTTP statuses with JSON bodies.
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit 2>&1 | grep -vE "mark-booking-attended\.ts|member-session\.test\.ts" | grep -E "api\.m\.checkin|api\.m\.admin\.check-in|routes/api/m/checkin|routes/api/m/admin/check-in" || echo "NO_NEW_TS_ERRORS"</automated>
  </verify>
  <done>Both endpoints exist with Nitro wrappers. api.m.checkin gates with requireMember, validates occurrence + temporal window + NOT_BOOKED, and calls mark-booking-attended.run (idempotent for already-attended). api.m.admin.check-in gates with requireAdmin and calls the chokepoint with no ownership check. tsc reports no NEW errors for the four files.</done>
</task>

<task type="auto">
  <name>Task 2: Admin kiosk page — loader (occurrences + roster + QR) and tappable UI</name>
  <files>apps/staff-web/package.json, apps/staff-web/app/routes/gymos.kiosk.tsx</files>
  <action>
Add the QR dep, then build the kiosk route (sibling of gymos.schedule.tsx; RR v7 flat route → `/gymos/kiosk`).

1. Add deps to apps/staff-web: run `pnpm --filter ./apps/staff-web add qrcode && pnpm --filter ./apps/staff-web add -D @types/qrcode` from the repo root. (No QR lib exists in staff-web yet — confirmed.)

2. `app/routes/gymos.kiosk.tsx`:
  - `export function meta()` → `[{ title: "RunStudio — Kiosk" }]`.
  - `export async function loader({ request }: LoaderFunctionArgs)`:
    a. `await requireAdmin(request);` (import from `../../server/lib/admin-session`) — THIS is the real gate for the page (throws 401/403). 
    b. Read `?occurrenceId=` from the URL search params (may be null).
    c. Query today's scheduled occurrences for the picker: classOccurrences LEFT JOIN classDefinitions, select `{ id, startsAt, endsAt, className: classDefinitions.name }` where `status='scheduled'` AND startsAt within `[now - 12h, now + 12h]` (a sane "today around now" window — reuse the gte/lte + ISO string approach from api.m.schedule.tsx), order by startsAt asc. `// guard:allow-unscoped — single-tenant gym tables`.
    d. Choose the selected occurrence: the `?occurrenceId=` one if present in the list, else auto-select the occurrence "happening now or next" — the first whose `endMs + 15min >= now`, else the last in the list, else null.
    e. If a selected occurrence exists, load its roster (mirror api.m.teacher.roster.tsx query, NO ownership gate): bookings LEFT JOIN gymMembers, select `{ bookingId: bookings.id, memberId, firstName, lastName, status }` where `and(eq(occurrenceId, selectedId), inArray(bookings.status, ["booked","attended"]))`. `// guard:allow-unscoped`.
    f. Generate the QR data URL server-side: `import QRCode from "qrcode";` `const qrDataUrl = selectedId ? await QRCode.toDataURL(\`runstudio-checkin:${selectedId}\`, { width: 512, margin: 2 }) : null;`.
    g. Return `{ occurrences, selectedId, roster, qrDataUrl }`.
  - Default component (`useLoaderData` + `useRevalidator`): kiosk-friendly layout — large text, minimal chrome. Left/top: a shadcn `Select` (class picker) listing occurrences as `"<HH:MM> · <className>"`; changing it navigates to `?occurrenceId=<id>` (use `useSearchParams` setter or `useNavigate`). Center: the big QR — `<img src={qrDataUrl} alt="Check-in QR" className="w-72 h-72 ..." />` with a caption "Scan with the RunStudio app to check in" and an empty-state ("No class selected") when null. Right/below: the roster as big tap targets (shadcn `Card` rows, min-height ~64px, text-lg). Each not-yet-attended row is a `Button`/pressable → on tap, optimistically flip that row to attended (local `Set<string>` of checked bookingIds merged over roster status) and `fetch("/api/m/admin/check-in", { method:"POST", credentials:"include", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ bookingId }) })`; on non-ok response roll back the optimistic Set entry and `toast.error(...)` (sonner). On success call `revalidator.revalidate()` to reconcile. Attended rows show an `IconCircleCheck` (Tabler) "Checked in" badge. Use Tabler icons (`IconQrcode`, `IconUser`, `IconCircleCheck`) — never emojis.
  - Keep it a single self-contained file; no new agent LLM tool (check-in is operational, mirrors mark-booking-attended not being an agent tool).
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit 2>&1 | grep -vE "mark-booking-attended\.ts|member-session\.test\.ts" | grep -E "gymos\.kiosk" || echo "NO_NEW_TS_ERRORS"</automated>
  </verify>
  <done>`qrcode` + `@types/qrcode` are in apps/staff-web/package.json. /gymos/kiosk loader is admin-gated, returns today's occurrences + selected roster + a server-generated QR data URL for `runstudio-checkin:<id>`. The page renders a class picker, a large QR, and a tappable roster that optimistically checks members in via POST /api/m/admin/check-in and revalidates. tsc reports no NEW errors for gymos.kiosk.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Nav exposure (four-area) — Kiosk tab + NavBridge + AGENTS.md docs</name>
  <files>apps/staff-web/app/components/gymos/GymosTopNav.tsx, apps/staff-web/app/components/gymos/GymosNavBridge.tsx, apps/staff-web/AGENTS.md</files>
  <action>
Complete the staff-web four-area contract for the kiosk (UI done in T2; here = nav exposure + docs).

1. `GymosTopNav.tsx`: add `const isKiosk = path.startsWith("/gymos/kiosk");` alongside the other `is*` flags, and render an admin-only tab (inside an `{isAdmin && (...)}` block, next to Catalog/Video): `<Link to="/gymos/kiosk" className={tabClass(isKiosk)}>Kiosk</Link>`.

2. `GymosNavBridge.tsx`: add `kiosk: "/gymos/kiosk",` to the `VIEW_TO_PATH` map so the agent can `navigate({view:'kiosk'})`.

3. `apps/staff-web/AGENTS.md`: 
   - Add three rows to the "Member API" endpoints table (the /api/m/* table near the bottom): 
     - `/api/m/checkin` | POST | Member self-check-in — `{ occurrenceId }` (extracted client-side from the kiosk QR `runstudio-checkin:<id>`). Gated by requireMember. Validates the occurrence is scheduled + inside a temporal window ([-45m, end+15m]) + the member has a 'booked'/'attended' booking (else 409 NOT_BOOKED), then calls the mark-booking-attended chokepoint (idempotent). Returns `{attended:true, className, startsAt}`.
     - `/api/m/admin/check-in` | POST | Admin tap-to-check-in — `{ bookingId }`. Gated by requireAdmin (lives under the public /api/m path, self-gates inside). Mirrors teacher check-in but with NO trainer-ownership check. Calls mark-booking-attended. Powers the /gymos/kiosk roster.
   - Add a short note (near the mark-booking-attended row / attendance-chokepoint callout) that BOTH new check-in paths funnel through `mark-booking-attended.run` — there is still exactly one attendance write path. 
   - Add a one-line mention that `/gymos/kiosk` is the admin-gated tablet check-in surface (per-class QR + tap roster), agent-navigable via `navigate({view:'kiosk'})`, and that the mobile app is an API consumer of `/api/m/checkin` (not bound by the four-area contract).
  </action>
  <verify>
    <automated>cd apps/staff-web && grep -q "gymos/kiosk" app/components/gymos/GymosTopNav.tsx && grep -q "kiosk:" app/components/gymos/GymosNavBridge.tsx && grep -q "api/m/checkin\|/api/m/checkin" AGENTS.md && echo "NAV_AND_DOCS_OK"</automated>
  </verify>
  <done>The Kiosk tab renders admin-only in GymosTopNav, `navigate({view:'kiosk'})` routes to /gymos/kiosk via GymosNavBridge, and AGENTS.md documents both new endpoints, the single-attendance-writer invariant, and the kiosk page.</done>
</task>

<task type="auto">
  <name>Task 4: Mobile QR scanner screen (parse → POST /api/m/checkin → confirmation states)</name>
  <files>packages/mobile-app/app/checkin-scan.tsx, packages/mobile-app/app/_layout.tsx</files>
  <action>
Create the member scanner screen mirroring components/BarcodeScanner.tsx + teacher-roster.tsx conventions (useTheme, Feather icons, apiFetch, react-query).

1. `packages/mobile-app/app/checkin-scan.tsx` (default export `CheckinScanScreen`):
  - Camera: `CameraView` (from expo-camera) with `barcodeScannerSettings={{ barcodeTypes: ["qr"] }}` and `useCameraPermissions()` — replicate BarcodeScanner.tsx's 3-state render (perm null → null/loader; !granted → explanation + Grant button; granted → CameraView with a centered frame overlay + hint "Point at the class QR").
  - `onBarcodeScanned`: self-guard with a `done` ref/state so it fires once. Parse `result.data`: if it does NOT start with `"runstudio-checkin:"`, IGNORE it (reset the guard so the user can keep scanning a valid code — do not error on random QR codes). Extract `occurrenceId = data.slice("runstudio-checkin:".length)`.
  - Submit via a `useMutation`: `apiFetch("/api/m/checkin", { method:"POST", body: JSON.stringify({ occurrenceId }) })`. 
  - Confirmation states (full-screen, mirror teacher-roster styling with useTheme):
     * success → green check (Feather "check-circle") + "Checked in to {className}" + the class time (`new Date(startsAt).toLocaleTimeString(...)`), and a "Done" button that `router.back()`.
     * NOT_BOOKED (match `String(err.message).includes("NOT_BOOKED")`) → "You're not booked in this class" + a "Close" button.
     * CHECKIN_WINDOW_CLOSED → "Check-in isn't open for this class right now."
     * OCCURRENCE_NOT_FOUND / OCCURRENCE_UNAVAILABLE / anything else → generic "Couldn't check you in — please try again" with a "Scan again" button that resets the guard + clears the result.
  - On success, `qc.invalidateQueries({ queryKey: ["schedule"] })` (and `["profile"]` for parity) so the schedule reflects the attended state.
  - Use `useRouter` from expo-router for back navigation; `useQueryClient` for invalidation.

2. `packages/mobile-app/app/_layout.tsx`: register the screen in the `<Stack>` (next to the `teacher-roster` Stack.Screen): `<Stack.Screen name="checkin-scan" options={{ title: "Check in", headerShown: true, presentation: "modal" }} />`.
  </action>
  <verify>
    <automated>cd packages/mobile-app && grep -q "runstudio-checkin:" app/checkin-scan.tsx && grep -q "api/m/checkin" app/checkin-scan.tsx && grep -q "barcodeTypes: \[\"qr\"\]\|barcodeTypes:\[\"qr\"\]" app/checkin-scan.tsx && grep -q "checkin-scan" app/_layout.tsx && echo "SCANNER_WIRED"</automated>
  </verify>
  <done>checkin-scan.tsx scans QR only, ignores non-`runstudio-checkin:` codes, POSTs the parsed occurrenceId to /api/m/checkin, and shows success / NOT_BOOKED / window-closed / generic states, invalidating the schedule cache on success. The screen is registered in _layout.tsx.</done>
</task>

<task type="auto">
  <name>Task 5: Mobile entry point — "Scan to check in" button (members only) on the schedule tab</name>
  <files>packages/mobile-app/app/(tabs)/schedule.tsx</files>
  <action>
Add a members-only entry point to the scanner from the schedule screen.

1. Resolve role: import `fetchRole, type AppRole` from `../../lib/whoami`. Add `const [role, setRole] = useState<AppRole | null>(null);` and a `useEffect(() => { fetchRole().then(setRole); }, []);` (mirror the pattern in _layout.tsx's AgentFabAndSheet). `const isMember = role === "member";`.
2. Render a "Scan to check in" button ABOVE the FlatList (near the pass-balance pill, inside the returned container) — ONLY when `isMember` (not teacher/admin; teacher/admin use the kiosk / teacher-roster flows). Use a styled `Pressable` with a Feather `"maximize"` or `"camera"` icon + label "Scan to check in", `onPress={() => router.push("/checkin-scan")}` (router already exists in this component). Keep it visually secondary to Book (reuse `styles.btnSecondary` / add a compact row style) so it doesn't compete with the primary booking flow.
3. Do NOT change any booking/PARQ/purchase logic — this is purely additive UI.
  </action>
  <verify>
    <automated>cd packages/mobile-app && grep -q "checkin-scan" "app/(tabs)/schedule.tsx" && grep -q "fetchRole\|isMember" "app/(tabs)/schedule.tsx" && echo "ENTRY_POINT_WIRED"</automated>
  </verify>
  <done>The schedule tab shows a "Scan to check in" button for signed-in members only, routing to /checkin-scan. Booking/PARQ/purchase flows are untouched.</done>
</task>

</tasks>

<verification>
- `cd apps/staff-web && npx tsc --noEmit` — no NEW errors beyond the two pre-existing files (mark-booking-attended.ts, member-session.test.ts).
- Grep proves the single-attendance-writer invariant holds: `grep -rn "status.*=.*['\"]attended['\"]\|status: \"attended\"" apps/staff-web/app/routes/api.m.checkin.tsx apps/staff-web/app/routes/api.m.admin.check-in.tsx` returns NOTHING (both only call mark-booking-attended.run; the only writer stays actions/mark-booking-attended.ts).
- Kiosk QR encodes `runstudio-checkin:<occurrenceId>`; mobile scanner parses the same prefix.
- No new migration files under apps/staff-web/server/db/migrations; no edits to schema.ts.
- Nav: Kiosk tab admin-only; `navigate({view:'kiosk'})` resolves.
</verification>

<success_criteria>
- Operator opens /gymos/kiosk on a tablet (signed in as the studio / operator email), picks the current class, sees a big QR + roster, and taps a name to check someone in (persists via chokepoint, optimistic + revalidated).
- A booked member opens the app → schedule → "Scan to check in" → scans the kiosk QR → sees "Checked in to {class}". Their booking flips to attended (via mark-booking-attended, firing the Meta Schedule CAPI event once).
- A member scanning a class they aren't booked into sees "You're not booked in this class" and is not checked in.
- Scanning outside the temporal window is rejected with a clear message.
- Exactly one attendance write path (mark-booking-attended.run) is used by both the kiosk and the mobile scanner.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-de6-gym-check-in-kiosk-member-qr-self-scan-c/260702-de6-SUMMARY.md`.
</output>
