---
phase: quick-260615-k8m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/staff-web/app/routes/api.m.foods.analyze.tsx
  - apps/staff-web/server/routes/api/m/foods/analyze.post.ts
  - apps/staff-web/app/routes/api.m.food-entries.tsx
  - packages/mobile-app/app/food-ai.tsx
  - packages/mobile-app/app/(tabs)/food.tsx
  - apps/staff-web/AGENTS.md
autonomous: true
requirements: [CAL-AI]
must_haves:
  truths:
    - "Member can describe a food in plain text and get an AI calorie + macro estimate"
    - "Member can take/pick a food photo and get an AI calorie + macro estimate"
    - "Member can adjust quantity + meal type and log the estimate to their diary"
    - "A logged AI estimate populates food_entries so the diary + KcalRing totals refresh"
    - "The new 'AI estimate' option appears in the food + (Add) menu"
  artifacts:
    - path: "apps/staff-web/app/routes/api.m.foods.analyze.tsx"
      provides: "POST /api/m/foods/analyze — Claude vision+text estimate, strict-JSON response"
      contains: "export async function action"
    - path: "apps/staff-web/server/routes/api/m/foods/analyze.post.ts"
      provides: "Nitro wrapper delegating to the resource-route action"
    - path: "packages/mobile-app/app/food-ai.tsx"
      provides: "AI estimate screen — photo + text input, estimate, adjust, log"
      min_lines: 120
  key_links:
    - from: "packages/mobile-app/app/food-ai.tsx"
      to: "/api/m/foods/analyze"
      via: "apiFetch POST"
      pattern: "foods/analyze"
    - from: "packages/mobile-app/app/food-ai.tsx"
      to: "/api/m/food-entries"
      via: "apiFetch POST then invalidate ['profile'] + ['food-entries']"
      pattern: "food-entries"
    - from: "packages/mobile-app/app/(tabs)/food.tsx"
      to: "/food-ai"
      via: "router.push in FAB menu"
      pattern: "food-ai"
---

<objective>
Add AI food analysis (photo + text → calorie & macro estimate) to the GymClassOS member mobile app's calorie counter, as a third option in the food "+" menu (alongside Search and Scan barcode). NOT barcode.

Purpose: Let members log foods that have no barcode and aren't in Open Food Facts (home-cooked meals, restaurant plates) by describing them or photographing them — the highest-friction gap in the current calorie counter.

Output:
- New backend endpoint `POST /api/m/foods/analyze` (Claude vision + text → strict-JSON estimate).
- A small additive widening of the existing `POST /api/m/food-entries` action so an LLM estimate logs with `food_items.source = "llm_estimate"` and `food_entries.source = "agent"` (NO DB migration).
- New mobile screen `app/food-ai.tsx` wired into the food FAB menu.
- AGENTS.md doc for the new member endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@apps/staff-web/AGENTS.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — do NOT re-explore. -->

GROUND TRUTH — food tables (apps/staff-web/server/db/schema.ts):
- food_items.source Drizzle enum = ["openfoodfacts","usda","custom","llm_estimate"]  ← "llm_estimate" ALREADY EXISTS, no migration needed.
- food_entries.source Drizzle enum = ["manual","barcode","search","favourite","agent"]  ← NO "llm_estimate". Reuse "agent" for AI-logged entries (no migration).
- Macros are SNAPSHOTTED into food_entries at log time: kcal = kcalPer100g * quantityG / 100 (same for protein/carbs/fat).

EXISTING POST /api/m/food-entries action (apps/staff-web/app/routes/api.m.food-entries.tsx):
- Body: { foodItem: { id?, name, brand?, barcode?, kcalPer100g, proteinPer100g?, carbsPer100g?, fatPer100g?, servingSizeG?, source? }, quantityG, mealType }
- TODAY it (a) types foodItem.source as only "openfoodfacts"|"custom", and (b) hardcodes food_entries.source = body.foodItem.barcode ? "barcode" : "search".
- Task 1 widens this minimally (additive) so an LLM estimate logs correctly.

Member API conventions:
- All /api/m/* routes are React Router resource routes (loader/action) with a thin Nitro wrapper in server/routes/api/m/**. Both gated by requireDemoMember(request) (server/lib/demo-member.ts: NODE_ENV != production + DEMO_MODE=true + X-Demo-Member-Id header).
- CORS / preflight for /api/m/* (incl. X-Demo-Member-Id) already handled by server/middleware/00-public-cors.ts — do NOT touch it.
- React Router v7 framework mode has NO json() export — return plain objects or `new Response(JSON.stringify(...), {...})`. Use `@/` path alias if any (not `~/`).

Anthropic call pattern (reference: apps/staff-web/app/routes/api.m.agent.stream.tsx):
- import Anthropic from "@anthropic-ai/sdk"; const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
- Guard missing key: if (!process.env.ANTHROPIC_API_KEY) return 500.
- Model constant: const MODEL = "claude-sonnet-4-6";  (Haiku-4.5 is a cheaper alternative — see Task 1 note — but default to sonnet-4-6 for vision quality.)
- Use client.messages.create({ model, max_tokens, system, messages }) (non-streaming — this is a single one-shot estimate, NOT SSE).
- Defensive JSON parse: the model may wrap JSON in prose or ```json fences. Strip fences and slice from first "{" to last "}" before JSON.parse, in a try/catch.

Nitro wrapper pattern (reference: apps/staff-web/server/routes/api/m/food-entries.post.ts) — copy verbatim, only change the import path:
```ts
import { defineEventHandler, setResponseStatus } from "h3";
import { action } from "../../../../../app/routes/api.m.foods.analyze.js";
export default defineEventHandler(async (event) => {
  const request = event.req as unknown as Request;
  try {
    const result = await action({ request, params: {}, context: {} } as any);
    if (result instanceof Response) {
      setResponseStatus(event, result.status);
      const text = await result.text();
      try { return JSON.parse(text); } catch { return text; }
    }
    return result;
  } catch (err) {
    if (err instanceof Response) {
      setResponseStatus(event, err.status);
      const text = await err.text();
      try { return JSON.parse(text); } catch { return { error: text }; }
    }
    throw err;
  }
});
```
(Note nesting depth: server/routes/api/m/foods/analyze.post.ts → the app/ dir is six "../" up. Confirm against sibling foods/barcode/[ean].get.ts which uses six "../".)

Mobile conventions (reference: app/food-barcode.tsx, app/food-add.tsx):
- Icons: this is the MOBILE app — it uses `@expo/vector-icons` (Feather), NOT Tabler (Tabler is web-only). Use Feather icons. NO emoji icons. Avoid sparkle/wand (use Feather "camera"/"edit-3"/"zap" — "zap" is fine, not a sparkle).
- Theme: `import { useTheme } from "../lib/theme"` — NEVER hardcode hex. Use theme.colors.* / theme.font.* / theme.radius.* exactly as food-barcode.tsx does.
- Fetch: `import { apiFetch } from "../lib/api"` — it injects X-Demo-Member-Id and Content-Type, throws on !ok, returns res.json().
- Camera: `expo-camera` IS a dep (CameraView + useCameraPermissions, see components/BarcodeScanner.tsx). `CameraView` ref + `takePictureAsync({ base64: true, quality: 0.4 })` gives base64 for the vision call. `expo-image-picker` is NOT a dep — see Task 2 for the library-pick decision.
- After logging: `qc.invalidateQueries({ queryKey: ["food-entries"] })` AND `qc.invalidateQueries({ queryKey: ["profile"] })` then `router.back()`. (Confirmed keys: food.tsx uses ["food-entries", dateKey] + ["profile"]; index.tsx uses ["profile"]. Invalidating the ["food-entries"] prefix matches the dated key.)
- Meal pills + quantity input: mirror the exact pattern in food-add.tsx (mealRow / mealPill / qtyInput styles).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend — POST /api/m/foods/analyze (Claude vision+text estimate) + widen food-entries logging</name>
  <files>apps/staff-web/app/routes/api.m.foods.analyze.tsx, apps/staff-web/server/routes/api/m/foods/analyze.post.ts, apps/staff-web/app/routes/api.m.food-entries.tsx</files>
  <action>
A. Create the resource route `apps/staff-web/app/routes/api.m.foods.analyze.tsx` exporting an `action`:
   - `if (request.method !== "POST")` → 405.
   - `await requireDemoMember(request)` (import from `../../server/lib/demo-member`).
   - `if (!process.env.ANTHROPIC_API_KEY)` → return 500 JSON `{ ok: false, error: "ANTHROPIC_API_KEY not configured" }`.
   - Parse body `{ image?: string, description?: string, mealHint?: string }` in try/catch (400 `{ ok:false, error:"Bad JSON" }` on failure). REQUIRE at least one of `image` or `description` (else 400 `{ ok:false, error:"Provide a photo or a description" }`).
   - Normalise the image input: accept either a bare base64 string or a data URL (`data:image/jpeg;base64,XXXX`). If it's a data URL, split off the prefix to get the raw base64 and capture the media type; default media_type to `"image/jpeg"`. (Keep this tolerant — the mobile screen sends `image/jpeg`.)
   - `const MODEL = "claude-sonnet-4-6";` (Add a one-line comment: `// Haiku-4.5 ("claude-haiku-4-5") is ~cheaper but weaker at portion vision; keep sonnet for v1.`)
   - Build the user message content array: if `image` present, push a vision block `{ type: "image", source: { type: "base64", media_type, data: rawBase64 } }`; always push a `{ type: "text", text: <prompt> }` block. The text block instructs: "You are a nutrition estimator. Estimate the food shown/described and respond with STRICT JSON only — no prose, no markdown fences. Schema: {\"foodName\":string,\"kcalPer100g\":number,\"proteinPer100gG\":number,\"carbsPer100gG\":number,\"fatPer100gG\":number,\"suggestedQuantityG\":number,\"confidence\":\"low\"|\"medium\"|\"high\",\"note\":string}. All per-100g values are grams except kcalPer100g which is kilocalories per 100g. suggestedQuantityG is your best estimate of the portion size in grams. Keep note under 120 chars. If you genuinely cannot identify the food, still return your best guess and set confidence to \"low\"." Append the member's `description` and (if given) `mealHint` into the prompt text.
   - Call `const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });` then `const msg = await client.messages.create({ model: MODEL, max_tokens: 600, messages: [{ role: "user", content }] });` (non-streaming).
   - Extract text: concatenate all `c.text` from `msg.content` where `c.type === "text"`.
   - DEFENSIVE PARSE (reference the agent stream's tolerance): strip leading/trailing ```json fences, then slice from the first `{` to the last `}`, then `JSON.parse` inside try/catch. On parse failure return 502 JSON `{ ok:false, error:"Could not parse estimate" }`.
   - Coerce/validate: foodName → String (fallback to description or "Estimated food"); the four nutrient fields + suggestedQuantityG → `Number(...)` with `Number.isFinite` guards (fallback 0 for nutrients, fallback 100 for suggestedQuantityG); confidence ∈ {low,medium,high} (fallback "low"); note → String.slice(0,200).
   - Return `new Response(JSON.stringify({ ok: true, estimate }), { status: 200, headers: { "Content-Type": "application/json" } })` where `estimate = { foodName, kcalPer100g, proteinPer100gG, carbsPer100gG, fatPer100gG, suggestedQuantityG, confidence, note }`.
   - Wrap the Anthropic call in try/catch → on error return 502 JSON `{ ok:false, error: String(err?.message ?? err) }`.

B. Create the Nitro wrapper `apps/staff-web/server/routes/api/m/foods/analyze.post.ts` — copy the food-entries.post.ts / search.get.ts wrapper VERBATIM, changing only the import to `import { action } from "../../../../../app/routes/api.m.foods.analyze.js";` and calling `action(...)` (not `loader`). Verify the relative depth matches sibling `foods/barcode/[ean].get.ts` (six `../`); adjust if the linter/build complains.

C. Widen the EXISTING `apps/staff-web/app/routes/api.m.food-entries.tsx` action — STRICTLY ADDITIVE, no behaviour change for existing callers:
   - In the body type, change `source?: "openfoodfacts" | "custom"` to `source?: "openfoodfacts" | "custom" | "llm_estimate"`.
   - Add an OPTIONAL top-level body field `entrySource?: "manual" | "barcode" | "search" | "favourite" | "agent"` (matches the food_entries enum).
   - Change the food_entries insert `source:` from the hardcoded `body.foodItem.barcode ? "barcode" : "search"` to: `body.entrySource ?? (body.foodItem.barcode ? "barcode" : "search")`. (Existing barcode/search callers don't send entrySource, so their behaviour is unchanged; the AI screen sends `entrySource: "agent"`.)
   - The food_items insert already passes `source: body.foodItem.source ?? "openfoodfacts"` — passing `"llm_estimate"` from the AI screen now flows through correctly (the column enum already includes it).
   - Do NOT change the food_entries.source DB enum. Do NOT add a migration.

Run `npx prettier --write` on the three files after editing.
  </action>
  <verify>
    <automated>cd apps/staff-web; npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "api.m.foods.analyze|food-entries|analyze.post" ; echo "TSC-FILTER-DONE"</automated>
  </verify>
  <done>
- `apps/staff-web/app/routes/api.m.foods.analyze.tsx` exports an `action` that gates with requireDemoMember, calls Claude with a vision block when an image is supplied, parses strict JSON defensively, and returns `{ ok, estimate }` / `{ ok:false, error }`.
- Nitro wrapper exists and imports the resource-route action.
- food-entries action accepts `entrySource` and `foodItem.source: "llm_estimate"` additively; existing barcode/search callers unchanged.
- No new TS errors introduced in the three touched files; no DB migration added.
- (Runtime, executor performs — see VERIFICATION below): production-build curl to `/api/m/foods/analyze` with `{"description":"chicken caesar salad"}` returns `{ ok:true, estimate:{...} }` (requires ANTHROPIC_API_KEY in .env.local; if absent, a 500 with the key-missing message is an acceptable PASS of the gate path).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mobile — food-ai.tsx screen (photo + text → estimate → log) wired into the FAB menu</name>
  <files>packages/mobile-app/app/food-ai.tsx, packages/mobile-app/app/(tabs)/food.tsx</files>
  <action>
Create `packages/mobile-app/app/food-ai.tsx` — a structured tool screen (NOT an agent composer surface; do not import the shared agent composer). Mirror the structure/styling of `app/food-barcode.tsx` and `app/food-add.tsx` exactly (useTheme, StyleSheet.create in useMemo, Feather icons, theme tokens only — NO hardcoded hex).

State machine: `mode: "input" | "estimating" | "result"`.

INPUT mode:
  - A multiline `TextInput` for the food description (placeholder e.g. "Describe your meal (e.g. chicken caesar salad)"), styled like food-add.tsx's input.
  - A "Take photo" button using the existing `expo-camera` `CameraView`: render an inline `CameraView` with a `ref`, a capture button calling `ref.takePictureAsync({ base64: true, quality: 0.4, imageType: "jpg" })`, and store the returned `base64` (build a `data:image/jpeg;base64,${base64}` string OR send the bare base64 — the endpoint accepts both). Reuse the camera-permission gate pattern from BarcodeScanner.tsx (`useCameraPermissions`, show a Grant button when not granted). Photo capture is OPTIONAL — a thumbnail/checkmark indicates a photo is attached, with a "Retake / Remove" affordance.
  - "Pick from library": `expo-image-picker` is NOT currently a dependency. DECISION (default for this quick task): SKIP a library picker for v1 to keep zero new deps and ship fast — take-photo via CameraView + text description covers both required paths (photo + text). If you choose to add it, install `expo-image-picker` in `packages/mobile-app/package.json` (matching the `~55.x` Expo SDK line), add the `launchImageLibraryAsync({ base64: true, quality: 0.4 })` flow, and note the new dep in the SUMMARY. Do NOT add it silently without noting it.
  - An "Estimate" button (Feather "zap" icon — NOT a sparkle/wand). Disabled unless there's a description OR a photo. On press → set mode "estimating", POST to `/api/m/foods/analyze` via apiFetch with body `{ image?, description?, mealHint: mealType }`. On success → store estimate, set quantity input to `String(Math.round(estimate.suggestedQuantityG || 100))`, set mode "result". On error → show the error inline (theme.colors.danger) and return to "input" mode. Use a loading state (ActivityIndicator with theme.colors.accent) — do NOT block with a spinner-after-click on anything except this estimate call (the estimate genuinely needs the round-trip).

RESULT mode (mirror food-add.tsx's confirmCard):
  - Show `estimate.foodName`, a `{kcalPer100g} kcal / 100g` line, a small confidence badge ("AI estimate · {confidence}", muted), and `estimate.note` if present (muted, small).
  - Editable quantity (g) `TextInput` (keyboardType numeric), prefilled from suggestedQuantityG.
  - Meal pills (breakfast/lunch/dinner/snack) — exact food-add.tsx pattern.
  - "Log" button: validate quantity is a positive finite number; then OPTIMISTIC-where-sensible POST to `/api/m/food-entries` via apiFetch with:
    ```
    {
      foodItem: {
        name: estimate.foodName,
        kcalPer100g: estimate.kcalPer100g,
        proteinPer100g: estimate.proteinPer100gG,
        carbsPer100g: estimate.carbsPer100gG,
        fatPer100g: estimate.fatPer100gG,
        source: "llm_estimate",
      },
      entrySource: "agent",
      quantityG: <number>,
      mealType,
    }
    ```
    Then `qc.invalidateQueries({ queryKey: ["food-entries"] })` AND `qc.invalidateQueries({ queryKey: ["profile"] })`, then `router.back()`. (Match the exact invalidation + router.back pattern in food-add.tsx; a brief logging spinner like food-add/food-barcode is acceptable here.)
  - A "Start over" / "Re-estimate" affordance returning to input mode.

Use shadcn-equivalent native patterns already in the app (Pressable pills, no window.confirm/alert — there are none in RN anyway). No emoji icons anywhere.

WIRE THE FAB MENU — edit `packages/mobile-app/app/(tabs)/food.tsx`:
  - In the Add-food Modal sheet (after the "Scan barcode" option), add a third `Pressable` `addOption` row: Feather icon `"zap"` (avoid sparkle/wand), label "AI estimate", onPress `() => { setAddOpen(false); router.push("/food-ai"); }`. Match the existing addOption markup exactly.

Run `npx prettier --write` on both files.
  </action>
  <verify>
    <automated>cd packages/mobile-app; npx expo export --platform web 2>&1 | Select-String -Pattern "food-ai|error|Error|Unable|failed" ; echo "EXPO-EXPORT-DONE"</automated>
  </verify>
  <done>
- `app/food-ai.tsx` exists: text description + take-photo (expo-camera) inputs, an "Estimate" call to `/api/m/foods/analyze`, a result view with editable quantity + meal pills, and a "Log" that POSTs to `/api/m/food-entries` with `foodItem.source:"llm_estimate"` + `entrySource:"agent"` and invalidates ["food-entries"] + ["profile"] then router.back().
- food.tsx FAB sheet shows three options: Search, Scan barcode, AI estimate (Feather "zap"), the third pushing "/food-ai".
- No hardcoded hex; useTheme tokens throughout; Feather icons only; no emoji icons.
- `npx expo export --platform web` completes without bundling errors (food-ai.tsx resolves).
- If `expo-image-picker` was added, it's recorded in the SUMMARY; otherwise zero new deps.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Doc — record POST /api/m/foods/analyze in staff-web AGENTS.md</name>
  <files>apps/staff-web/AGENTS.md</files>
  <action>
In `apps/staff-web/AGENTS.md`, add documentation for the new member endpoint. The file's existing "Data Sources" / "Agent Actions" tables are about the staff agent; add a short subsection (or extend the member-API notes if a member-API section exists) documenting:

- `POST /api/m/foods/analyze` — Member calorie counter AI estimate. Gated by `requireDemoMember`. Request `{ image?: string (base64 or data URL), description?: string, mealHint?: string }` (at least one of image/description required). Calls Claude (`claude-sonnet-4-6`) with a vision block for photos; returns strict JSON `{ ok: true, estimate: { foodName, kcalPer100g, proteinPer100gG, carbsPer100gG, fatPer100gG, suggestedQuantityG, confidence, note } }` or `{ ok: false, error }`. Logging reuses `POST /api/m/food-entries` with `foodItem.source:"llm_estimate"` + `entrySource:"agent"` (no DB migration; both enum values already exist / are reused). Consumed by the mobile `app/food-ai.tsx` screen reached from the food "+" menu.

Keep it concise (a short paragraph or a one-row table entry consistent with the file's existing style). Run `npx prettier --write apps/staff-web/AGENTS.md`.
  </action>
  <verify>
    <automated>cd apps/staff-web; Select-String -Path AGENTS.md -Pattern "foods/analyze" ; echo "DOC-CHECK-DONE"</automated>
  </verify>
  <done>
- `apps/staff-web/AGENTS.md` documents `POST /api/m/foods/analyze` (request shape, model, response shape, and the food-entries logging reuse with llm_estimate/agent).
  </done>
</task>

</tasks>

<verification>
TYPES / BUILD:
- `cd apps/staff-web; npx tsc --noEmit` introduces no NEW errors in the three touched backend files.
- `cd packages/mobile-app; npx expo export --platform web` succeeds (mobile bundling — one-shot, no watcher).

RUNTIME (executor — local PRODUCTION build, because `agent-native dev` cannot serve routes: NitroViteError):
1. `cd apps/staff-web` then (PowerShell) `$env:NITRO_PRESET=$null; node ../../packages/core/dist/cli/index.js build`
2. `$env:NODE_ENV="development"; $env:PORT="8095"; node --env-file=.env.local .output/server/index.mjs`  (ensure DEMO_MODE=true + ANTHROPIC_API_KEY are in .env.local)
3. `curl -X POST http://localhost:8095/api/m/foods/analyze -H "X-Demo-Member-Id: seedm_05" -H "Content-Type: application/json" -d '{\"description\":\"chicken caesar salad\"}'` → expect `{ "ok": true, "estimate": { ... } }`.
4. (Optional) Log it: `curl -X POST http://localhost:8095/api/m/food-entries -H "X-Demo-Member-Id: seedm_05" -H "Content-Type: application/json" -d '{\"foodItem\":{\"name\":\"Chicken caesar salad\",\"kcalPer100g\":180,\"source\":\"llm_estimate\"},\"entrySource\":\"agent\",\"quantityG\":300,\"mealType\":\"lunch\"}'` → expect `{ "entryId": "fe_..." }`. Clean up the test row afterward if desired.
5. STOP the server (it locks .output/@libsql) BEFORE any rebuild.

CONVENTIONS:
- No hardcoded hex in food-ai.tsx; Feather icons only; no emoji icons; no sparkle/wand.
- Strictly additive backend change; no DB migration; `@agent-native/core` untouched.
</verification>

<success_criteria>
- A member opens Food → "+" → "AI estimate", describes or photographs a meal, taps Estimate, gets an AI calorie+macro estimate, adjusts quantity/meal, taps Log, returns to the diary with the entry visible and the Today totals (food.tsx) + KcalRing (index.tsx) reflecting it.
- `POST /api/m/foods/analyze` returns a strict-JSON estimate for both text-only and photo inputs.
- No new TS errors; mobile bundles; no DB migration; doc updated.
</success_criteria>

<output>
After completion, create `.planning/quick/260615-k8m-add-ai-food-analysis-photo-text-calorie-/260615-k8m-SUMMARY.md` capturing: files created/modified, whether `expo-image-picker` was added (and why), the runtime curl result, and any deviations (e.g. Nitro wrapper relative-path depth, model choice).
</output>
