---
phase: quick-260609-qe9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/worker/src/domain/sendViaMyutik.ts
  - services/worker/src/domain/sendViaMyutik.test.ts
  - services/worker/src/domain/sendMessage.ts
  - services/worker/src/domain/sendMessage.test.ts
autonomous: true
requirements: [WA-05]

must_haves:
  truths:
    - "Every outbound WhatsApp send from the worker goes through MYÜTIK (POST https://myutik.com/api/channels/whatsapp/send), never Meta Graph directly"
    - "Compliance gates (opt-in, 24h window, template-approved) and the message-status state machine are unchanged — only the final 'call Meta' step becomes 'call MYÜTIK'"
    - "A successful send extracts wamid from result.messages[0].id and marks the message row 'sent'"
    - "4xx-class MYÜTIK errors (400/404/409) mark the message 'failed' with no pg-boss retry; 5xx (502)/network errors re-throw so pg-boss retries"
    - "The worker TypeScript build and the worker vitest suite both pass"
  artifacts:
    - path: "services/worker/src/domain/sendViaMyutik.ts"
      provides: "Thin MYÜTIK send HTTP client returning { wamid }"
      contains: "myutik.com/api/channels/whatsapp/send"
    - path: "services/worker/src/domain/sendViaMyutik.test.ts"
      provides: "Unit tests for the MYÜTIK client (fetch mock)"
    - path: "services/worker/src/domain/sendMessage.ts"
      provides: "Chokepoint rewired to sendViaMyutik with gates unchanged"
      contains: "sendViaMyutik"
    - path: "services/worker/src/domain/sendMessage.test.ts"
      provides: "Updated chokepoint tests mocking ./sendViaMyutik.js"
  key_links:
    - from: "services/worker/src/domain/sendMessage.ts"
      to: "services/worker/src/domain/sendViaMyutik.ts"
      via: "import sendViaMyutik from ./sendViaMyutik.js, called in step 7"
      pattern: "sendViaMyutik\\("
    - from: "services/worker/src/domain/sendViaMyutik.ts"
      to: "https://myutik.com/api/channels/whatsapp/send"
      via: "fetch POST with x-api-key header"
      pattern: "fetch\\("
---

<objective>
Rewire the worker's outbound WhatsApp send chokepoint to route through MYÜTIK's relay instead of calling Meta Graph directly.

Purpose: The GymClassOS Meta app is NOT approved to send on Hustle's WABA number (confirmed today — Meta returns code 100/subcode 33 "Object 302631896256150 ... missing permissions" on a real send). Direct Meta sends will NEVER work. MYÜTIK holds the token with the right WhatsApp permissions on that WABA; its relay endpoint shipped 2026-06-05. This is the same pattern already applied to template sync (quick 260608-fb8 repointed `syncTemplates.ts` at MYÜTIK) — now the SEND path follows.

Output: A new `sendViaMyutik.ts` HTTP client + tests, `sendMessage.ts` rewired to call it (gates and status machine unchanged), and updated chokepoint tests. No direct-Meta send path left anywhere in `sendMessage.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Files to read fully before editing (already surveyed at plan time):
@services/worker/src/domain/sendMessage.ts
@services/worker/src/domain/syncTemplates.ts
@services/worker/src/domain/sendMessage.test.ts

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration. -->

MYÜTIK send contract (LIVE, shipped 2026-06-05):
- POST https://myutik.com/api/channels/whatsapp/send
- Headers: x-api-key: <MYUTIK_API_KEY>, Content-Type: application/json
  (the key needs the whatsapp:send permission — see "Activation" in SUMMARY)
- Request body (omit undefined fields):
  - to            E.164, with or without leading + (REQUIRED) — KEEP the leading +
  - phoneNumberId pass "302631896256150" (account owns multiple numbers; omitting risks 400)
  - text          free-form (open 24h window) — mutually exclusive with templateName
  - templateName  (instead of text)
  - templateLanguage (optional; MYÜTIK defaults "en"; we pass our template language, "en_US")
  - templateComponents (optional Meta component array)
- Responses:
  - 200 { sent, type, conversationId, result }  where wamid = result.messages[0].id
  - 400 { error }
  - 404 { error: "phoneNumberId not found for this account" }
  - 409 { error, requiresTemplate: true, conversationId }   (window closed, text only)
  - 502 { error }   (Meta send failed downstream)
- Account is resolved from the API key — do NOT pass any Meta token.

Existing secrets resolvers (services/worker/src/lib/secrets.ts — ALREADY EXIST, do not modify):
  export async function getMyutikApiKey(db): Promise<string>          // app_secrets → pgcrypto → env → throws
  export async function getMyutikPhoneNumberId(db): Promise<string>   // app_secrets → pgcrypto → env → never throws

Existing typed errors (services/worker/src/lib/errors.js — unchanged):
  NoOptInError, WindowExpiredError, TemplateNotApprovedError

MYÜTIK client style to MIRROR (services/worker/src/domain/syncTemplates.ts):
  const res = await fetch(url, { headers: { "x-api-key": apiKey } });
  if (!res.ok) {
    const body = await res.text().catch(() => "");
    throw new Error(`MYÜTIK ... ${res.status}: ${body.slice(0, 200)}`);
  }

sendMessage error classifier (services/worker/src/domain/sendMessage.ts step-7 catch — KEEP semantics):
  status >= 400 && status < 500  → mark messages.status='failed' + errorCode, return { externalId: "" }  (no retry)
  >= 500 / network               → re-throw  (pg-boss retries)
  So sendViaMyutik MUST throw errors carrying .status so this classifier branches correctly.
</interfaces>
</context>

<constraints>
- HARD CONSTRAINT (user): ALL sends route through MYÜTIK. Leave NO direct-Meta send path in sendMessage.ts.
- SCOPE: services/worker only. Gates 1-5 and the status state machine (steps 8-9) stay byte-for-byte intact in behaviour — only the final adapter call (step 7) changes.
- Do NOT touch the @gymos/whatsapp package — other paths still import it. Only remove its import from sendMessage.ts THIS file.
- KEEP the leading + on the phone number (the OLD code stripped it for Meta; MYÜTIK accepts with or without + and we keep it).
- TypeScript only. Conventional commits. Do NOT create/switch git branches.
- Per user memory: no AGENTS.md "---" 🟢/🟡/🔴 status footer; convey status inline.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sendViaMyutik.ts client + sendViaMyutik.test.ts</name>
  <files>services/worker/src/domain/sendViaMyutik.ts, services/worker/src/domain/sendViaMyutik.test.ts</files>
  <behavior>
    sendViaMyutik.test.ts (mock global fetch):
    - text send: body posted is { to, phoneNumberId, text } (NO templateName/templateLanguage/templateComponents keys present); headers include x-api-key + Content-Type: application/json; method POST; URL is https://myutik.com/api/channels/whatsapp/send.
    - template send: body posted is { to, phoneNumberId, templateName, templateLanguage, templateComponents } where templateComponents is a SINGLE body component with ordered params; undefined fields omitted (e.g. text absent).
    - 200 with result.messages[0].id="wamid_x" → returns { wamid: "wamid_x" }.
    - 200 with NO messages id → throws an Error whose .status is >= 500 (so pg-boss retries).
    - 400 → throws Error with .status === 400; message includes "400".
    - 409 (body { error, requiresTemplate: true }) → throws Error with .status === 409; message carries the requiresTemplate detail.
    - 502 → throws Error with .status === 502.
  </behavior>
  <action>
    Create services/worker/src/domain/sendViaMyutik.ts exporting:

      export async function sendViaMyutik(args: {
        apiKey: string;
        phoneNumberId: string;
        to: string;
        text?: string;
        templateName?: string;
        templateLanguage?: string;
        templateComponents?: unknown[];
      }): Promise<{ wamid: string }>

    Behaviour:
    - Build the JSON body from provided fields, OMITTING any undefined field (do not send keys with undefined values). Always include `to` and `phoneNumberId`.
    - POST https://myutik.com/api/channels/whatsapp/send with headers
      { "x-api-key": args.apiKey, "Content-Type": "application/json" } and JSON.stringify(body).
    - Mirror syncTemplates.ts fetch + error style.
    - On non-200: read the body (try res.json() then fall back to res.text()), derive a `detail` string. For 409 include the requiresTemplate flag in the detail. Throw:
        throw Object.assign(new Error(`MYÜTIK send ${status}: ${detail}`), { status });
      so sendMessage's existing classifier branches on err.status.
    - On 200: parse JSON; wamid = json.result?.messages?.[0]?.id ?? "". If wamid is empty, throw a >= 500 error (e.g. Object.assign(new Error("MYÜTIK send 200 but no wamid in result.messages"), { status: 502 })) so pg-boss retries. Otherwise return { wamid }.

    Create services/worker/src/domain/sendViaMyutik.test.ts per the <behavior> block. Mock global.fetch (vi.stubGlobal("fetch", vi.fn()) or beforeEach assigning global.fetch = vi.fn()). Build a fetch Response stub with { ok, status, json: async () => (...), text: async () => "..." }. Assert the request URL, method, headers, and parsed body (JSON.parse the second arg's body), wamid extraction, and that 400/409/502 throw an Error carrying .status.
  </action>
  <verify>
    <automated>cd services/worker && npx vitest run src/domain/sendViaMyutik.test.ts</automated>
  </verify>
  <done>sendViaMyutik.ts exists and posts to MYÜTIK with x-api-key, omits undefined fields, extracts wamid from result.messages[0].id, and throws .status-carrying errors on non-200 / empty-wamid. Its test file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire sendMessage.ts to sendViaMyutik + update sendMessage.test.ts</name>
  <files>services/worker/src/domain/sendMessage.ts, services/worker/src/domain/sendMessage.test.ts</files>
  <behavior>
    sendMessage.test.ts after rewrite:
    - Gate-failure tests (opt-in/window/template-not-approved) assert sendViaMyutik was NOT called (replacing the old sendText/sendTemplate "not called" assertions).
    - Text-in-window success → sendViaMyutik called with { apiKey: "myutik_test_key", phoneNumberId: "302631896256150", to: "+447700900000", text: "hello" }; message row marked 'sent' with externalId = returned wamid.
    - Template (outside window) success → sendViaMyutik called with { apiKey, phoneNumberId, to, templateName, templateLanguage: "en_US", templateComponents } where templateComponents is a SINGLE body component with params ordered by placeholder number; message row 'sent'.
    - to KEEPS the leading + (the old strip-+ test is updated to assert to === "+447700900123").
    - 4xx (sendViaMyutik throws { status: 409 }) → row 'failed', function returns { externalId: "" }, no re-throw.
    - 5xx (sendViaMyutik throws { status: 502 }) → re-throws.
  </behavior>
  <action>
    EDIT services/worker/src/domain/sendMessage.ts:
    1. Remove `import { sendText, sendTemplate } from "@gymos/whatsapp";` (do NOT touch the package itself).
    2. Add `import { sendViaMyutik } from "./sendViaMyutik.js";`.
    3. In the secrets import, replace `getWhatsAppAccessToken, getWhatsAppPhoneNumberId` with `getMyutikApiKey, getMyutikPhoneNumberId` (THIS file only).
    4. Gates 1-5 (opt-in, member load, conversation load, window, template-approved) UNCHANGED.
    5. Step 6 — resolve creds:
         const apiKey = await getMyutikApiKey(db);
         const phoneNumberId = await getMyutikPhoneNumberId(db);
    6. Step 7 — replace the adapter block:
       - to = member.phoneE164 (KEEP leading + — delete the .replace(/^\+/, "") strip).
       - text branch:
           const result = await sendViaMyutik({ apiKey, phoneNumberId, to, text: payload.body });
           externalId = result.wamid;
       - template branch — build templateComponents as a SINGLE body component, params ordered by placeholder number:
           const orderedValues = Object.entries(payload.vars)
             .sort(([a], [b]) => Number(a) - Number(b))
             .map(([, v]) => v);
           const templateComponents = orderedValues.length
             ? [{ type: "body", parameters: orderedValues.map((v) => ({ type: "text", text: v })) }]
             : undefined;
           const result = await sendViaMyutik({
             apiKey, phoneNumberId, to,
             templateName: payload.name,
             templateLanguage: payload.language ?? "en_US",
             templateComponents,
           });
           externalId = result.wamid;
       - KEEP the existing catch classifier exactly: err.status in [400,500) (400/404/409) → mark messages.status='failed' + errorCode, return { externalId: "" }; >= 500 (502) / network → re-throw.
    7. Steps 8-9 (mark 'sent' with externalId + sentAt; update conversation.lastOutboundAt) UNCHANGED.
    Update the step-6/step-7 doc comments to say MYÜTIK relay instead of @gymos/whatsapp adapter.

    EDIT services/worker/src/domain/sendMessage.test.ts:
    1. Replace `vi.mock("@gymos/whatsapp", () => ({ sendText, sendTemplate }))` and the `const sendText = vi.fn(); const sendTemplate = vi.fn();` with:
         const sendViaMyutik = vi.fn();
         vi.mock("./sendViaMyutik.js", () => ({ sendViaMyutik }));
    2. In the `vi.mock("../lib/secrets.js", ...)` return, add:
         getMyutikApiKey: vi.fn().mockResolvedValue("myutik_test_key"),
         getMyutikPhoneNumberId: vi.fn().mockResolvedValue("302631896256150"),
       (keep the existing entries).
    3. beforeEach: replace sendText.mockReset()/sendTemplate.mockReset() with sendViaMyutik.mockReset().
    4. Update each test per the <behavior> block:
       - opt-in / window / template-not-approved → assert sendViaMyutik not called.
       - text success → sendViaMyutik.mockResolvedValueOnce({ wamid: "wamid_OK" }); assert sendViaMyutik called with { apiKey: "myutik_test_key", phoneNumberId: "302631896256150", to: "+447700900000", text: "hello" }; row 'sent' with externalId "wamid_OK".
       - template success → sendViaMyutik.mockResolvedValueOnce({ wamid: "wamid_sent_abc" }); assert sendViaMyutik called with { apiKey, phoneNumberId, to: "+447700900000", templateName: "class_reminder", templateLanguage: "en_US", templateComponents: [{ type: "body", parameters: [{ type: "text", text: "Yoga" }] }] }; isInWindow NOT consulted.
       - strip-+ test → rename intent to "keeps leading +"; assert sendViaMyutik call arg `to === "+447700900123"`.
       - 4xx test → sendViaMyutik.mockRejectedValueOnce(Object.assign(new Error("..."), { status: 409 })); expect result.externalId === "" and row 'failed'.
       - 5xx test → sendViaMyutik.mockRejectedValueOnce(Object.assign(new Error("Bad gateway"), { status: 502 })); expect rejects.
       - Remove the TEST_CREDS fixture (no longer passed as a 2nd arg).
  </action>
  <verify>
    <automated>cd services/worker && npx vitest run</automated>
  </verify>
  <done>No `@gymos/whatsapp` import in sendMessage.ts; no `.replace(/^\+/` phone strip; sendViaMyutik is the only send call; gates 1-5 and status steps 8-9 unchanged. Full worker vitest suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Typecheck, prettier, full verify</name>
  <files>services/worker/src/domain/sendViaMyutik.ts, services/worker/src/domain/sendViaMyutik.test.ts, services/worker/src/domain/sendMessage.ts, services/worker/src/domain/sendMessage.test.ts</files>
  <action>
    From services/worker:
    1. Run prettier on all four changed/new files: `npx prettier --write src/domain/sendViaMyutik.ts src/domain/sendViaMyutik.test.ts src/domain/sendMessage.ts src/domain/sendMessage.test.ts`.
    2. Run the TypeScript build/typecheck for the worker (use the worker's own build/typecheck script — check services/worker/package.json for `build` or `typecheck`; if a build script exists run it, else `npx tsc --noEmit`).
    3. Run the full worker vitest suite once more: `npx vitest run`.
    Both the typecheck and the vitest suite MUST pass before this task is done. If anything fails, fix it (no direct-Meta path may be reintroduced).
  </action>
  <verify>
    <automated>cd services/worker && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Prettier applied to all four files; worker TypeScript typecheck/build passes; full worker vitest suite passes (green).</done>
</task>

</tasks>

<verification>
- `grep` confirms no `@gymos/whatsapp` import and no `sendText`/`sendTemplate` reference remains in services/worker/src/domain/sendMessage.ts.
- `grep` confirms no `.replace(/^\+/` phone-strip remains in sendMessage.ts.
- sendViaMyutik is imported and is the sole send call site in sendMessage.ts.
- `cd services/worker && npx tsc --noEmit` passes.
- `cd services/worker && npx vitest run` passes (all tests, including the two new/updated suites).
</verification>

<success_criteria>
- All outbound worker sends route through MYÜTIK (POST https://myutik.com/api/channels/whatsapp/send); zero direct-Meta send paths left in sendMessage.ts.
- Gates (opt-in, 24h window, template-approved) and the message-status state machine behave identically to before.
- Text sends post { to (with +), phoneNumberId, text }; template sends post { to, phoneNumberId, templateName, templateLanguage, templateComponents } with a single body component of ordered params.
- wamid extracted from result.messages[0].id → message row 'sent'; 4xx (400/404/409) → 'failed' no retry; 5xx (502)/network → re-throw for pg-boss retry.
- Worker typecheck and vitest both green; prettier applied.
</success_criteria>

<output>
After completion, create `.planning/quick/260609-qe9-route-worker-outbound-whatsapp-sends-thr/260609-qe9-SUMMARY.md`.

Capture in the SUMMARY (NOT in code):
(a) DEPLOY: this is a WORKER change. The Fly worker process (gymos-edge-webhooks worker) must be redeployed — pushing to GitHub does NOT auto-deploy the Fly worker. Flag the manual `fly deploy` (worker process) as the ship step.
(b) ACTIVATION: the stored MYUTIK_API_KEY must have the `whatsapp:send` permission. Templates currently work via `whatsapp:read`; a gym-class-os key holding BOTH scopes may still need minting. If the key lacks `send`, MYÜTIK returns 400/401/403 on send — call this out as the activation step to verify after deploy.
</output>
