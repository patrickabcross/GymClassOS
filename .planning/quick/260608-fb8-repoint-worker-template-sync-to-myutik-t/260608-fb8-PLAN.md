---
phase: quick-260608-fb8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/worker/src/domain/syncTemplates.ts
  - services/worker/src/domain/syncTemplates.test.ts
  - services/worker/src/lib/secrets.ts
  - services/worker/src/lib/env.ts
  - services/worker/src/queues/housekeeping.ts
autonomous: true
requirements: [WA-08]

must_haves:
  truths:
    - "The daily templates-sync cron pulls templates from MYÜTIK, not Meta Graph."
    - "Templates upserted into whatsapp_templates carry a lowercase status (e.g. 'approved'), so templateGate's status='approved' filter matches and sends are not blocked."
    - "Multi-page MYÜTIK responses are fully paginated (follows paging.next via ?after=)."
    - "When no MYÜTIK key is configured, the worker still boots — the handler logs a warning and returns cleanly."
  artifacts:
    - path: "services/worker/src/domain/syncTemplates.ts"
      provides: "syncWhatsAppTemplates(apiKey, phoneNumberId, db) hitting MYÜTIK with x-api-key + pagination + status lowercasing"
      contains: "myutik.com/api/channels/whatsapp/templates"
    - path: "services/worker/src/lib/secrets.ts"
      provides: "getMyutikApiKey + getMyutikPhoneNumberId resolvers"
      contains: "getMyutikApiKey"
    - path: "services/worker/src/lib/env.ts"
      provides: "Optional MYUTIK_API_KEY + MYUTIK_PHONE_NUMBER_ID env vars"
      contains: "MYUTIK_API_KEY"
  key_links:
    - from: "services/worker/src/queues/housekeeping.ts"
      to: "syncWhatsAppTemplates"
      via: "templates-sync consumer resolves MYÜTIK key + phoneNumberId then calls sync"
      pattern: "syncWhatsAppTemplates\\("
    - from: "services/worker/src/domain/syncTemplates.ts"
      to: "whatsapp_templates"
      via: "upsert with lowercased status"
      pattern: "toLowerCase"
---

<objective>
Repoint the worker's daily `templates-sync` cron from the Meta Graph API
(`graph.facebook.com/v23.0/{wabaId}/message_templates`, Bearer auth) to the
MYÜTIK Template Extract API. The direct-Meta route is blocked by token scope;
MYÜTIK already holds a Meta token authorized on the Hustle gym's real WABA.

Purpose: unblock nightly template-status sync so the send-side gate
(`templateGate.ts`) sees current approval status.
Output: a MYÜTIK-backed `syncWhatsAppTemplates`, new MYÜTIK secret resolvers,
optional MYÜTIK env vars, a rewired cron consumer, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@services/worker/src/domain/syncTemplates.ts
@services/worker/src/domain/syncTemplates.test.ts
@services/worker/src/lib/secrets.ts
@services/worker/src/lib/env.ts
@services/worker/src/queues/housekeeping.ts
@services/worker/src/domain/gates/templateGate.ts

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly. -->

MYÜTIK endpoint (verified working, 200 end-to-end):
  GET https://myutik.com/api/channels/whatsapp/templates
  Header: x-api-key: <key>   (key has whatsapp:read permission)
  Query params:
    phoneNumberId (REQUIRED) = "302631896256150" (leased number → wabaId 115640014972621, Hustle's real WABA)
    limit (optional, default 200, clamp 1–1000)
    after (optional, pagination cursor)
    status (optional, omit for all)
  200 response shape:
    {
      phoneNumberId: string,
      wabaId: string,
      templates: Array<{ name, language, status, category, components }>,  // status is UPPERCASE: APPROVED/PENDING/REJECTED
      paging: { next: string | null }
    }
  Paginate: re-call with ?after=<paging.next> until paging.next is null.
  Errors: 401 bad key, 403 lacks whatsapp:read, 400 no/ambiguous WA number,
          404 phoneNumberId not owned, 502 Meta Graph error (proxied).

Existing upsert (KEEP verbatim except status lowercasing):
  INSERT INTO whatsapp_templates (name, status, category, language, components_json, last_synced_at)
  VALUES (...) ON CONFLICT (name) DO UPDATE SET ...
  Keep the `// guard:allow-unscoped` comment above it.

Read-side gate (why lowercasing is CRITICAL):
  templateGate.isTemplateApproved filters status = "approved" (lowercase).
  MYÜTIK returns "APPROVED". Without toLowerCase(), every template send is blocked.

Existing secrets resolver shape to mirror (getWhatsAppAccessToken):
  readSecret('<name>', db) → env fallback → throw.
  getWhatsAppBusinessAccountId returns null instead of throwing (optional).

Worker scripts: `npm test` (vitest run), `npm run typecheck` (tsc --noEmit).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MYÜTIK secret resolvers + optional env vars</name>
  <files>services/worker/src/lib/secrets.ts, services/worker/src/lib/env.ts</files>
  <behavior>
    - getMyutikApiKey(db): returns secrets.myutik_api_key if present, else env MYUTIK_API_KEY, else throws (mirror getWhatsAppAccessToken exactly).
    - getMyutikPhoneNumberId(db): returns secrets.myutik_phone_number_id if present, else env MYUTIK_PHONE_NUMBER_ID, else env WHATSAPP_PHONE_NUMBER_ID (last fallback never throws — WHATSAPP_PHONE_NUMBER_ID is required env).
    - env.ts: MYUTIK_API_KEY and MYUTIK_PHONE_NUMBER_ID parse as optional strings; absence does not fail env validation.
  </behavior>
  <action>
    In `env.ts`, add to `EnvSchema` (strictly additive — do not touch existing keys):
      MYUTIK_API_KEY: z.string().min(8).optional(),
      MYUTIK_PHONE_NUMBER_ID: z.string().min(4).optional(),
    Place them near the WhatsApp block with a short comment noting they back the
    MYÜTIK template-sync path.

    In `secrets.ts`, add two new exported async functions (do NOT delete the
    existing Meta resolvers getWhatsAppAccessToken / getWhatsAppBusinessAccountId /
    getWhatsAppPhoneNumberId):
      getMyutikApiKey(db): readSecret("myutik_api_key", db) → if null, env.MYUTIK_API_KEY
        → if absent, throw a descriptive error mirroring getWhatsAppAccessToken's message
        (reference both secrets.myutik_api_key and env MYUTIK_API_KEY).
      getMyutikPhoneNumberId(db): readSecret("myutik_phone_number_id", db) → if null,
        env.MYUTIK_PHONE_NUMBER_ID → if absent, env.WHATSAPP_PHONE_NUMBER_ID (required, so
        always present; never throws).
    Run prettier on both files after editing.
  </action>
  <verify>
    <automated>cd services/worker && npx vitest run src/domain/syncTemplates.test.ts</automated>
  </verify>
  <done>Both resolvers exported; env vars optional and additive; `npm run typecheck` clean for the touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite syncWhatsAppTemplates to call MYÜTIK (new signature, pagination, status lowercasing) + update tests</name>
  <files>services/worker/src/domain/syncTemplates.ts, services/worker/src/domain/syncTemplates.test.ts</files>
  <behavior>
    - Calls https://myutik.com/api/channels/whatsapp/templates with header x-api-key set to apiKey and a phoneNumberId query param; NO Authorization header.
    - UPPERCASE status from the API is lowercased before upsert: feed "APPROVED" → upserted arg carries "approved".
    - Non-2xx response throws an error whose message includes the status code.
    - Pagination: first response paging.next="cursor1", second paging.next=null → fetch called twice, second call URL includes after=cursor1, synced count spans both pages.
    - Returns { synced } = total templates upserted across all pages.
  </behavior>
  <action>
    Rewrite `syncWhatsAppTemplates` with the new signature:
      export async function syncWhatsAppTemplates(apiKey: string, phoneNumberId: string, db): Promise<{ synced: number }>
    Update the file's doc comment to say MYÜTIK Template Extract API (not Meta).

    Build the base URL: https://myutik.com/api/channels/whatsapp/templates with
    query params phoneNumberId=<phoneNumberId> and limit=200 (use URLSearchParams /
    URL). Fetch with headers { "x-api-key": apiKey } — do NOT send Authorization.

    Pagination loop (defensive cap ≤20 iterations):
      - On each call, if !res.ok: read body text, throw
        new Error(`MYÜTIK Template API ${res.status}: ${body.slice(0,200)}`).
      - Parse JSON as { templates?: Array<{ name; status; category?; language?; components? }>; paging?: { next?: string | null } }.
      - For each tpl, run the SAME upsert (keep the `// guard:allow-unscoped` comment),
        but with status = tpl.status.toLowerCase() and language defaulting to "en_US"
        when absent. Increment synced.
      - If paging?.next is truthy, set the after param to it and loop; else break.

    Return { synced }; keep the log.info({ synced }, ...) line.

    Update `syncTemplates.test.ts`:
      (a) Replace the Bearer/Meta-URL test: stub one 200 with { templates: [], paging: { next: null } };
          assert url contains "myutik.com/api/channels/whatsapp/templates", that init.headers["x-api-key"]
          equals the passed key, that the url has a phoneNumberId query param, and that there is NO
          Authorization header. Call with the new signature ("key_abc", "302631896256150", mockDb).
      (b) Status-lowercasing test: stub a template with status "APPROVED"; after sync, scan the upsert
          via JSON.stringify(executeMock.mock.calls[0][0]) (the existing serialize-the-drizzle-sql approach)
          and assert it contains "approved" and NOT "APPROVED".
      (c) Keep the non-2xx throw test (now expect a 401 → rejects.toThrow(/401/), with text() returning a body).
      (d) Pagination test: fetchMock.mockResolvedValueOnce a 200 with templates [t1] and paging.next="cursor1",
          then mockResolvedValueOnce a 200 with templates [t2] and paging.next=null. Assert fetch called twice,
          the second call URL includes "after=cursor1", and result.synced === 2.
    Update the top-of-file comment in the test to say MYÜTIK. Run prettier on both files.
  </action>
  <verify>
    <automated>cd services/worker && npx vitest run src/domain/syncTemplates.test.ts</automated>
  </verify>
  <done>All syncTemplates tests pass; URL is MYÜTIK with x-api-key + phoneNumberId; UPPERCASE status is lowercased; pagination followed; non-2xx throws.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the templates-sync cron consumer to MYÜTIK</name>
  <files>services/worker/src/queues/housekeeping.ts</files>
  <action>
    In the `boss.work(TEMPLATES_SYNC_QUEUE, ...)` handler, replace the Meta
    resolution (getWhatsAppBusinessAccountId + getWhatsAppAccessToken) with MYÜTIK:
      - Update the imports: import getMyutikApiKey and getMyutikPhoneNumberId from
        ../lib/secrets.js; remove the now-unused getWhatsAppBusinessAccountId and
        getWhatsAppAccessToken imports.
      - Resolve the MYÜTIK key with a try/catch: let apiKey; try { apiKey = await getMyutikApiKey(db); }
        catch { /* unconfigured */ }. If !apiKey, log.warn(...) referencing
        MYUTIK_API_KEY / in-app Settings and `return` cleanly (mirror the existing
        wabaId guard so the worker still boots).
      - Resolve phoneNumberId = await getMyutikPhoneNumberId(db).
      - Call syncWhatsAppTemplates(apiKey, phoneNumberId, db) inside the existing
        try/catch (keep the re-throw on error so pg-boss marks the tick failed).
    Update the warn message and the file-level doc comment to reference MYÜTIK
    (not Meta / WHATSAPP_BUSINESS_ACCOUNT_ID). Keep the boss.schedule registration
    and queue name unchanged. Run prettier.
  </action>
  <verify>
    <automated>cd services/worker && npx tsc --noEmit</automated>
  </verify>
  <done>Consumer resolves MYÜTIK key + phoneNumberId, warns+returns when unconfigured, calls the new sync signature; typecheck clean; no dangling unused Meta imports.</done>
</task>

</tasks>

<verification>
- `cd services/worker && npx vitest run src/domain/syncTemplates.test.ts` passes.
- `cd services/worker && npx tsc --noEmit` clean.
- Grep confirms: syncTemplates.ts contains "myutik.com" and "toLowerCase"; no "graph.facebook.com" remains in syncTemplates.ts; housekeeping.ts imports getMyutikApiKey.
- Meta resolvers in secrets.ts remain intact (getWhatsAppAccessToken etc. not deleted).
</verification>

<success_criteria>
- templates-sync cron calls MYÜTIK with x-api-key + phoneNumberId, fully paginated.
- Upserted template status is lowercase so templateGate matches.
- Env/schema changes are strictly additive; no DB schema changes; guard comments preserved.
- Worker still boots when MYÜTIK key is absent (warn + clean return).
</success_criteria>

<output>
After completion, create `.planning/quick/260608-fb8-repoint-worker-template-sync-to-myutik-t/260608-fb8-SUMMARY.md`
</output>
