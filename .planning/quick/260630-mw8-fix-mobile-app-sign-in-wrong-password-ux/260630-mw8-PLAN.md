---
phase: quick-260630-mw8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/mobile-app/lib/sign-in-api.ts]
autonomous: true
requirements: [UAT-MA1-03-BUG-2]
must_haves:
  truths:
    - "A wrong-password sign-in shows 'Incorrect email or password.' instead of a raw JSON/status dump"
    - "Any other non-2xx sign-in failure shows 'Couldn't sign you in. Please try again.'"
    - "The missing set-auth-token header path, the PHONE_REQUIRED 403 flow, and network-failure path are all unchanged"
  artifacts:
    - path: "packages/mobile-app/lib/sign-in-api.ts"
      provides: "Friendly auth-failure mapping in the !res.ok branch of signInWithEmail"
      contains: "Incorrect email or password."
  key_links:
    - from: "packages/mobile-app/lib/sign-in-api.ts"
      to: "packages/mobile-app/app/sign-in.tsx"
      via: "thrown Error.message surfaced via setError(err?.message)"
      pattern: "Incorrect email or password"
---

<objective>
Fix the mobile-app sign-in wrong-password UX (bug #2 from the MA1-03 device UAT). A failed sign-in currently throws `Sign-in failed (${res.status}): ${text.slice(0,200)}`, leaking a raw `Sign-in failed (401): {"message":"Invalid email or password","code":"INVALID_EMAIL_OR_PASSWORD"}` string onto the login screen. Map auth failures to friendly, human messages.

Purpose: Members who fat-finger their password should see a clean "Incorrect email or password." instead of an HTTP/JSON dump.
Output: A single-file edit to `packages/mobile-app/lib/sign-in-api.ts`. No UI change (the screen already does `setError(err?.message)`), no server, no DB, no migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The file to change (full current contents below for reference)
@packages/mobile-app/lib/sign-in-api.ts

# The consumer — verify only, NO change expected. Login screen already surfaces err.message:
#   line ~179: setError(err?.message ?? "Sign-in failed. Please try again.")
#   PHONE_REQUIRED 403 is handled LATER via the apiFetch("/api/m/profile") catch (lines ~161-177), NOT in sign-in-api.ts
@packages/mobile-app/app/sign-in.tsx

<facts>
- CONFIRMED against live prod (gym-class-os.vercel.app): wrong-password response is HTTP 401 with body
  `{"message":"Invalid email or password","code":"INVALID_EMAIL_OR_PASSWORD"}`.
- The ONLY code touched is the `if (!res.ok)` branch (currently lines ~32-35).
- The `set-auth-token` missing-header throw (lines ~36-39) is AFTER res.ok — leave it exactly as-is.
- Origin-header logic and the fetch network-failure path (fetch rejection propagates naturally) must NOT change.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map auth failures to friendly messages in the !res.ok branch</name>
  <files>packages/mobile-app/lib/sign-in-api.ts</files>
  <action>
    Replace ONLY the `if (!res.ok) { ... }` block (currently lines ~32-35) in `signInWithEmail`.

    New behavior:
    1. Read the raw body once: `const text = await res.text();`
    2. Parse defensively — the body may not be JSON:
       ```ts
       let code: string | undefined;
       try {
         code = (JSON.parse(text) as { code?: string })?.code;
       } catch {
         // non-JSON body — leave code undefined
       }
       ```
    3. If `res.status === 401 || code === "INVALID_EMAIL_OR_PASSWORD"` →
       `throw new Error("Incorrect email or password.");`
    4. Otherwise (any other non-2xx) →
       `throw new Error("Couldn't sign you in. Please try again.");`

    Do NOT change anything else:
    - Keep the Origin header and the fetch call as-is.
    - Keep the `set-auth-token` missing-header throw (after res.ok) exactly as-is.
    - Do not catch or alter the network-failure path (a rejected fetch must propagate).

    After editing, run prettier on the file:
    `npx prettier --write packages/mobile-app/lib/sign-in-api.ts`
  </action>
  <verify>
    <automated>cd packages/mobile-app && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "lib/sign-in-api" || echo "no sign-in-api type errors"</automated>
  </verify>
  <done>
    - The `!res.ok` branch parses the body defensively (try/catch around JSON.parse).
    - 401 or code === "INVALID_EMAIL_OR_PASSWORD" throws Error("Incorrect email or password.").
    - All other non-2xx throws Error("Couldn't sign you in. Please try again.").
    - The set-auth-token throw, Origin logic, and network path are untouched.
    - File is prettier-formatted; no new TypeScript errors in sign-in-api.ts.
  </done>
</task>

</tasks>

<verification>
- Read packages/mobile-app/app/sign-in.tsx and confirm NO change is needed: handleSignIn's catch still does `setError(err?.message ?? "Sign-in failed. Please try again.")`, so the new friendly messages flow straight through.
- Confirm the PHONE_REQUIRED 403 path is untouched: it lives in the inner `try { await apiFetch("/api/m/profile") } catch` (sign-in.tsx ~161-177), driven by the profile fetch — NOT by sign-in-api.ts. signInWithEmail never sees a 403 for PHONE_REQUIRED.
- Mentally trace the three branches against the confirmed prod 401 body and confirm the message reads cleanly on screen ("Incorrect email or password.").
</verification>

<success_criteria>
- Wrong password → screen shows "Incorrect email or password." (no status code, no JSON).
- Other non-2xx → screen shows "Couldn't sign you in. Please try again."
- Missing set-auth-token header, network failures, Origin handling, and the PHONE_REQUIRED 403 flow are all behaviorally unchanged.
- Single-file change to packages/mobile-app/lib/sign-in-api.ts; TypeScript clean; prettier-formatted.
</success_criteria>

<output>
After completion, create `.planning/quick/260630-mw8-fix-mobile-app-sign-in-wrong-password-ux/260630-mw8-SUMMARY.md`
</output>
