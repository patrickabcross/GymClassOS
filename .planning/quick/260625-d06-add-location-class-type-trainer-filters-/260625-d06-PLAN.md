---
phase: quick-260625-d06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/staff-web/app/routes/gymos.schedule.tsx
  - apps/staff-web/features/forms/lib/schedule-widget-ssr.ts
autonomous: true
requirements:
  - SCH-FILTER (quick task — add location/class-type/trainer filters to the class schedule)
user_setup: []

must_haves:
  truths:
    - "Staff can narrow the in-app calendar by location, class type, and trainer (independent, AND-composed)"
    - "Default state ('All locations / All class types / All trainers') shows every occurrence — no filtering"
    - "Filtering is instant (client-side over already-loaded occurrences) — no loader refetch per filter change"
    - "When filters exclude everything, the calendar shows a clear 'no classes match' message, not a blank grid"
    - "Public schedule embed members can narrow the same three ways"
  artifacts:
    - path: "apps/staff-web/app/routes/gymos.schedule.tsx"
      provides: "Widened loader Query A (location + trainerId + joined trainer name) + three shadcn Select filter controls in a Popover + client-side AND filtering + empty-after-filter state"
      contains: "trainerName"
    - path: "apps/staff-web/features/forms/lib/schedule-widget-ssr.ts"
      provides: "Widened SSR query (location + trainer name) + inline filter dropdowns + client-side JS show/hide + no-match message"
      contains: "trainerName"
  key_links:
    - from: "gymos.schedule.tsx loader Query A"
      to: "schema.trainers (left join) + class_occurrences.location/trainerId"
      via: "leftJoin + select widening"
      pattern: "leftJoin\\(\\s*schema\\.trainers"
    - from: "gymos.schedule.tsx filter Selects"
      to: "data.occurrences (client-side)"
      via: "useState filter values + filtered array before groupByDay"
      pattern: "filter"
---

<objective>
Add three independent, AND-composed filters — location, class type, and trainer — to the class schedule. The PRIMARY surface is the in-app staff calendar (`apps/staff-web/app/routes/gymos.schedule.tsx`); the SECONDARY surface is the public schedule embed (`apps/staff-web/features/forms/lib/schedule-widget-ssr.ts`).

The staff loader's Query A currently selects `category` but NOT `location` or `trainerId`, and does not join `trainers` for the trainer name — so filter-by-location and filter-by-trainer have no per-occurrence data to work with. This plan widens that query (left-join `trainers` for the name) and adds compact filter controls that narrow the rendered occurrences client-side. The embed query is widened the same way with member-facing filter dropdowns.

Purpose: operators (and members on the public embed) can focus the timetable on one location, one class type, or one trainer at a glance.
Output: two modified files; no schema migration (columns already exist); no new actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@apps/staff-web/AGENTS.md

# PRIMARY surface — read fully before editing
@apps/staff-web/app/routes/gymos.schedule.tsx

# SECONDARY surface — public embed (plain SSR HTML string + hand-written inline JS, NOT React)
@apps/staff-web/features/forms/lib/schedule-widget-ssr.ts

<interfaces>
<!-- Confirmed from apps/staff-web/server/db/schema.ts — use directly, no codebase exploration needed. -->

class_occurrences (Drizzle export `schema.classOccurrences`) — columns RELEVANT to filtering:
  - id, startsAt, endsAt, capacity, status, room, ruleId  (already selected in Query A)
  - location  → text("location")        // nullable. NOT YET selected in Query A. e.g. "Norwich" | "Wymondham" | null
  - trainerId → text("trainer_id")       // nullable soft-ref to trainers.id. NOT YET selected in Query A.

class_definitions (Drizzle export `schema.classDefinitions`):
  - name, durationMin, category          // category already selected in Query A as `category`

trainers (Drizzle export `schema.trainers`):
  - id, name, homeLocation               // join target for the trainer NAME

Already loaded by the staff loader and reusable for filter option lists:
  - data.classTypes : Array<{ id, name, durationMin, defaultCapacity, category }>   (Query D, active definitions)
  - data.trainers   : Array<{ id, name, homeLocation }>                              (Query E, active trainers)

Filter option semantics:
  - Class type filter → match on occurrence.className (the class definition name). Build the option
    list from data.classTypes (names) OR from the distinct className set in occurrences. Prefer
    data.classTypes names so the dropdown is stable even on months with no occurrences.
  - Trainer filter → match on the joined trainerName. Build option list from data.trainers.
    Occurrences with trainerId = null have trainerName = null; the "All trainers" default includes them.
  - Location filter → match on occurrence.location. Locations are NOT a loaded list — derive the
    distinct non-null location set from data.occurrences (e.g. ["Norwich","Wymondham"]). Occurrences
    with location = null are included under "All locations".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen staff loader Query A + add three Select filters with client-side AND filtering</name>
  <files>apps/staff-web/app/routes/gymos.schedule.tsx</files>
  <action>
Two edits in this single file — the loader (data) and the component (UI + filtering).

LOADER — widen Query A only (leave Query B/C/D/E untouched):
- Add a `.leftJoin(schema.trainers, eq(schema.classOccurrences.trainerId, schema.trainers.id))` to Query A (the `occurrences` select), AFTER the existing `.leftJoin(schema.classDefinitions, ...)`.
- Add three fields to the Query A select object:
  - `location: schema.classOccurrences.location,`
  - `trainerId: schema.classOccurrences.trainerId,`
  - `trainerName: schema.trainers.name,`
- Keep the existing `// guard:allow-unscoped — single-tenant gym tables` comment above the query (single-tenant gym tables — do NOT add accessFilter). The new join is to another single-tenant table, so the existing guard comment covers it; if you prefer, restate it. No schema migration — these columns already exist (location v24, trainer_id v25, trainers table v22).
- The `Occurrence` type alias (line ~262, `Awaited<ReturnType<typeof loader>>["occurrences"][number]`) picks up the new fields automatically — no manual type edit needed.

COMPONENT — add filter state, derived option lists, filtering, and the filter UI:
- Add three `useState<string>` filter values defaulting to a sentinel "all": `locationFilter`, `classTypeFilter`, `trainerFilter` (all default `"all"`).
- Derive the LOCATION option list from `data.occurrences`: the distinct, non-null, sorted set of `o.location`. (Locations are not a loaded list.)
- Use `data.classTypes` (names, deduped/sorted) for the CLASS TYPE options and `data.trainers` for the TRAINER options (value = trainer id, label = trainer name; filter compares against `o.trainerName` OR `o.trainerId` — pick `o.trainerId` for exact match since ids are stable).
- Compute `filteredOccurrences` from `data.occurrences` with three independent AND conditions, BEFORE `groupByDay`:
  - location: `locationFilter === "all" || o.location === locationFilter`
  - class type: `classTypeFilter === "all" || o.className === classTypeFilter`
  - trainer: `trainerFilter === "all" || o.trainerId === trainerFilter`
- Replace the existing `const byDay = groupByDay(data.occurrences);` with `groupByDay(filteredOccurrences)`. Also feed `totalThisMonth`'s day lookups off the filtered `byDay` (it already reads from `byDay`, so changing the source is enough). The calendar day-cell counts and the day-detail pane both already read from `byDay` / `selectedOccurrences` (which derives from `byDay`), so they narrow automatically.
- FILTER UI — progressive disclosure, do NOT clutter the header. Add a single shadcn `Popover` with a `Button variant="outline" size="sm"` trigger labelled "Filter" using a Tabler `IconFilter` (import from `@tabler/icons-react`). Show a small count badge on the trigger when any filter ≠ "all" (e.g. number of active filters). Place this trigger in the header's right-hand control cluster (the `<div className="flex items-center gap-2">` next to ManageTrainersDialog / NewClassDialog / the existing Share Popover) — do NOT add three always-visible Selects to the header.
- Inside the Popover, render three labelled shadcn `Select` controls (Location / Class type / Trainer), each with an "All …" item (value `"all"`) first, plus a "Clear filters" `Button variant="ghost"` that resets all three to `"all"`. Use ONLY the shadcn `Select`/`Popover` primitives already imported at the top of the file (`Select, SelectContent, SelectItem, SelectTrigger, SelectValue`, `Popover, PopoverContent, PopoverTrigger`). NEVER a custom `position:absolute` dropdown. Tabler icons only — no emojis.
- EMPTY-AFTER-FILTER state: the day-detail pane already shows "No classes scheduled" when `selectedOccurrences.length === 0`. When filters are active (any ≠ "all") AND the selected day has zero matches, change that message to make it clear it is the FILTER excluding classes (e.g. "No classes match your filters" + a "Clear filters" affordance), not just an empty day. Keep the existing IconCalendarEvent empty-state layout; just branch the copy on `hasActiveFilters`.
- This is all client-side over already-loaded `data.occurrences` — NO new loader param, NO `navigateTo`/searchParams round-trip for filters, NO refetch. Filtering must feel instant per AGENTS.md optimistic-UI rule. (URL-persistence of filters is explicitly out of scope for this quick task.)
- Run `npx prettier --write apps/staff-web/app/routes/gymos.schedule.tsx` after editing.
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "gymos.schedule" || echo "no schedule type errors"</automated>
  </verify>
  <done>Query A selects location + trainerId + trainerName (left-joined trainers); three shadcn Selects inside a "Filter" Popover narrow the rendered occurrences client-side with AND semantics; defaults show everything; empty-after-filter shows a "no classes match" message with a clear-filters affordance; tsc clean for this file; prettier applied.</done>
</task>

<task type="auto">
  <name>Task 2: Add the same three filters to the public schedule embed (SSR + inline JS)</name>
  <files>apps/staff-web/features/forms/lib/schedule-widget-ssr.ts</files>
  <action>
This file is plain SSR HTML-string generation + hand-written inline `<script>` JS (NOT React). Mirror Task 1's filters for members.

QUERY — widen `getUpcomingClasses()`:
- Extend the `ClassRow` type with `location: string | null;` and `trainerName: string | null;`.
- Add a `.leftJoin(schema.trainers, eq(schema.classOccurrences.trainerId, schema.trainers.id))` to the query (after the existing `.innerJoin(classDefinitions, ...)`). Import any missing operators — `eq` is already imported; the `schema.trainers` export is available via the existing `schema` import.
- Add to the select: `location: schema.classOccurrences.location,` and `trainerName: schema.trainers.name,`. Keep the existing `// guard:allow-unscoped` comment (single-tenant gym tables, anonymous public endpoint).

DATA ATTRIBUTES — make each rendered class card filterable client-side:
- In `renderClassCard`, add `data-location`, `data-classname`, and `data-trainer` attributes to the `.class-card` element (HTML-escaped). Use empty string for null. The class name is already `cls.className`; location is `cls.location`; trainer is `cls.trainerName`. These let the inline JS show/hide cards without a server round-trip.
- Optionally surface the trainer/location as small `.class-cat`-style chips in the card details (consistent with the existing category/duration/room chips) so members can see what they are filtering on — keep it minimal.

FILTER UI — add a compact filter bar above the schedule:
- In `renderPage` (or a new `renderFilterBar(classes)` helper), render a single row of three native `<select>` controls (Location / Class type / Trainer) ABOVE the `${scheduleHtml}` container. This is a standalone SSR page (no shadcn/React available in the iframe), so native `<select>` styled with the existing CSS-variable theme is the correct primitive here — do NOT hand-roll a custom dropdown. Each select's first option is "All locations" / "All class types" / "All trainers" with value `""`.
- Derive the option lists server-side from the fetched `classes`: distinct non-null `location`, distinct `className`, distinct non-null `trainerName`. HTML-escape every option label/value.
- Add CSS for the filter bar + selects in the `CSS(brand)` block, reusing existing tokens (`--border`, `--muted`, `--radius`, font inherits). Keep it visually quiet — a single responsive row that wraps on narrow widths.

INLINE JS — client-side AND filtering + no-match message:
- In the existing IIFE `<script>`, add an `applyFilters()` function that reads the three `<select>` values and shows/hides each `.class-card` whose `data-location` / `data-classname` / `data-trainer` match (empty filter value = match-all; AND-composed across the three).
- After hiding cards, also hide any `.day-group` that has zero visible cards (so empty day headers don't linger).
- If ALL cards are hidden, reveal a "no classes match your filters" message element (add a hidden `<div class="filter-empty">` to the page and toggle its display); when at least one card is visible, hide it. This is distinct from the existing `.empty-state` (which covers zero upcoming classes at all).
- Wire each `<select>`'s `change` event to `applyFilters()`. Call `sendResize()` after filtering so the parent iframe (embed.js) re-fits height (the existing MutationObserver on `style`/`childList` may already catch this, but call `sendResize()` explicitly to be safe).
- Do NOT refetch or reload — filtering is pure client-side over the already-rendered cards.
- Run `npx prettier --write apps/staff-web/features/forms/lib/schedule-widget-ssr.ts` after editing.
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "schedule-widget-ssr" || echo "no embed type errors"</automated>
  </verify>
  <done>getUpcomingClasses joins trainers + selects location/trainerName; class cards carry data-location/data-classname/data-trainer; a three-select filter bar above the schedule filters cards client-side with AND semantics; empty-after-filter shows a distinct "no classes match" message and empty day-groups hide; sendResize fires after filtering; tsc clean for this file; prettier applied.</done>
</task>

</tasks>

<verification>
- Staff calendar: with all filters "All", every occurrence renders exactly as before (no regression to day counts, badges, booking dialog, share popover, series-cancel).
- Staff calendar: selecting one location / one class type / one trainer narrows day-cell counts AND the day-detail list; combining two narrows further (AND); "Clear filters" restores everything.
- Staff calendar: a selected day whose only classes are filtered out shows "no classes match your filters", not a bare empty day.
- Embed: opening /embed/schedule shows the three native selects; choosing a trainer/location/class type hides non-matching cards instantly and collapses empty day groups; clearing restores; the iframe re-fits height.
- No loader refetch on any filter change (Network tab shows no request); no schema migration ran; no `drizzle-kit push` invoked.
- `cd apps/staff-web && npx tsc --noEmit` is clean for both edited files.
</verification>

<success_criteria>
- Loader Query A (staff) and getUpcomingClasses (embed) both select location + trainer name via a left join to trainers — confirmed against schema columns (location, trainer_id, trainers.name).
- Three independent, AND-composed filters (location, class type, trainer) on BOTH surfaces, defaulting to "all".
- Filtering is client-side over already-loaded data — instant, no round-trip.
- shadcn Select/Popover on the staff app (progressive-disclosure "Filter" Popover, not three header Selects); native styled selects on the standalone SSR embed; no custom position:absolute dropdowns anywhere; Tabler icons only.
- Empty-after-filter shows a clear "no classes match" message on both surfaces.
- No schema migration; strictly additive; prettier run on both files; tsc clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260625-d06-add-location-class-type-trainer-filters-/260625-d06-SUMMARY.md`
</output>
