---
phase: quick-260613-ey3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/staff-web/actions/list-payments.ts
  - apps/staff-web/app/routes/gymos.payments.tsx
  - apps/staff-web/AGENTS.md
autonomous: true
requirements: [PAY-VIEW]
must_haves:
  truths:
    - "Staff can open /gymos/payments and see recent payments, most recent first"
    - "Each payment row shows the member who paid (name → phone fallback → —), the amount (e.g. £12.00), the date, and a colour-coded status"
    - "When there are no payments, the page shows a clean empty state instead of an empty table"
    - "list-payments is a registered defineAction GET endpoint the agent can call"
  artifacts:
    - path: "apps/staff-web/actions/list-payments.ts"
      provides: "list-payments defineAction — LEFT JOIN payments→gym_members, ORDER BY occurred_at DESC LIMIT 100"
      contains: "defineAction"
    - path: "apps/staff-web/app/routes/gymos.payments.tsx"
      provides: "Real payments page with loader + shadcn Table + empty state"
      contains: "useLoaderData"
    - path: "apps/staff-web/AGENTS.md"
      provides: "list-payments documented in Agent Actions table"
      contains: "list-payments"
  key_links:
    - from: "apps/staff-web/app/routes/gymos.payments.tsx"
      to: "schema.payments / schema.gymMembers"
      via: "loader getDb() LEFT JOIN"
      pattern: "leftJoin"
    - from: "apps/staff-web/actions/list-payments.ts"
      to: "schema.payments"
      via: "drizzle select with leftJoin to gymMembers"
      pattern: "schema\\.payments"
---

<objective>
Turn the `/gymos/payments` "Coming soon" stub into a real page that lists recent payments with the member who paid.

Purpose: PAY-01/STR-02 audit gap — the worker reducers (P1b-07) already populate the `payments` table, but staff have no surface to view them. This makes recorded payments visible.

Output:
- A new `list-payments` read action (defineAction GET) mirroring the existing `list-*` convention.
- A rewired `gymos.payments.tsx` route: loader → shadcn Table (Date / Member / Amount / Status) with a clean empty state.
- The action documented in `apps/staff-web/AGENTS.md`.

No DB schema changes — the `payments` table already exists (STR-03). The table is currently EMPTY (0 rows), so the empty state is the default render and must be clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@apps/staff-web/AGENTS.md

<interfaces>
<!-- Extracted from the codebase — use these directly, no exploration needed. -->

`payments` table (apps/staff-web/server/db/schema.ts ~line 400), Drizzle export `schema.payments`:
```ts
{
  id: text (pk, "pay_<paymentIntentId>"),
  memberId: text("member_id"),                 // nullable
  stripePaymentIntentId: text (unique),
  amountMinorUnits: integer("amount_minor_units"),  // NOT NULL
  currency: text("currency"),                   // NOT NULL, e.g. "gbp"
  status: text enum ["succeeded","failed","refunded","pending"],
  rawJson: text("raw_json"),
  occurredAt: text("occurred_at"),              // ISO string, NOT NULL
}
```

`gym_members` table, Drizzle export `schema.gymMembers`:
```ts
{
  id: text (pk),
  firstName: text("first_name"),   // NOT NULL
  lastName: text("last_name"),     // nullable
  email: text("email"),            // nullable
  phoneE164: text("phone_e164"),   // nullable
}
```
NOTE: gym_members has NO single `name` column — compose `firstName + " " + lastName`.

Action convention (from apps/staff-web/actions/list-renewals.ts):
```ts
import { z } from "zod";
import { defineAction } from "@agent-native/core";
import { getDb, schema } from "../server/db/index.js";   // .js ESM convention
// drizzle helpers: import { desc, eq } from "drizzle-orm";

export default defineAction({
  description: "...",       // explains when the agent should call it + return shape
  schema: z.object({}),     // empty for a no-arg read
  http: { method: "GET" },  // GET = readOnly read action
  run: async () => {
    const db = getDb();
    // guard:allow-unscoped — single-tenant gym tables (no ownableColumns) ...
    const rows = await db.select({...}).from(schema.payments)...;
    return { ... };
  },
});
```

Route loader convention (from apps/staff-web/app/routes/gymos.members.tsx):
- `import { getDb, schema } from "../../server/db";`
- `import { useLoaderData, Link } from "react-router";`
- shadcn components via `@/` alias, e.g. `import { Badge } from "@/components/ui/badge";`
- `import type { LoaderFunctionArgs } from "react-router";`
- `export async function loader(...) { const db = getDb(); ... return { payments }; }` — return a PLAIN object (RR v7 framework mode has NO `json()` export).
- `export default function GymosPayments() { const data = useLoaderData<typeof loader>(); ... }`
- shadcn Table primitives exist at `@/components/ui/table` (Table, TableHeader, TableBody, TableRow, TableHead, TableCell).
- Tabler icons via `@tabler/icons-react`; `format` from `date-fns`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the list-payments read action</name>
  <files>apps/staff-web/actions/list-payments.ts</files>
  <action>
Create `apps/staff-web/actions/list-payments.ts` mirroring `list-renewals.ts` exactly (imports, defineAction shape, GET, guard comment).

- Import: `z` from zod, `defineAction` from `@agent-native/core`, `{ getDb, schema }` from `"../server/db/index.js"`, and `{ desc, eq }` from `drizzle-orm`.
- `description`: explain this lists recent Stripe payments with the member who paid, for the /gymos/payments surface and for answering "show me recent payments / who paid". State the return shape (array of payment rows).
- `schema: z.object({})` (no args).
- `http: { method: "GET" }`.
- `run`: `const db = getDb();` then a single `db.select({...}).from(schema.payments).leftJoin(schema.gymMembers, eq(schema.gymMembers.id, schema.payments.memberId)).orderBy(desc(schema.payments.occurredAt)).limit(100)`.
  - Place the `// guard:allow-unscoped — single-tenant gym tables (no ownableColumns) per P1b.1-RESEARCH.md §6 "no unscoped queries" exemption.` comment immediately above the query (the CI guard scans the same file for this marker).
  - Select these aliased columns: `id`, `memberId`, `amountMinorUnits`, `currency`, `status`, `occurredAt`, and member fields `firstName: schema.gymMembers.firstName`, `lastName: schema.gymMembers.lastName`, `phoneE164: schema.gymMembers.phoneE164`.
- Return `{ payments: rows.map(r => ({ id, memberId, amountMinorUnits: Number(r.amountMinorUnits), currency: r.currency, status: r.status, occurredAt: r.occurredAt, memberName: [r.firstName, r.lastName].filter(Boolean).join(" ").trim() || null, memberPhone: r.phoneE164 ?? null })) }`. Keep amount as minor units + currency — do NOT format money in the action; the UI formats.

Keep it a single LEFT JOIN query (one round-trip). Run `npx prettier --write apps/staff-web/actions/list-payments.ts` after writing.
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "list-payments" -Quiet; if ($?) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>list-payments.ts exists, type-checks (no errors referencing the new file), uses defineAction + GET + guard:allow-unscoped marker, LEFT JOINs payments→gym_members, ORDER BY occurred_at DESC LIMIT 100, returns minor-units amounts (unformatted).</done>
</task>

<task type="auto">
  <name>Task 2: Rewire gymos.payments.tsx to list payments + document the action</name>
  <files>apps/staff-web/app/routes/gymos.payments.tsx, apps/staff-web/AGENTS.md</files>
  <action>
Replace the "Coming soon" stub in `apps/staff-web/app/routes/gymos.payments.tsx` with a real loader + table page. Match the layout/idiom of `gymos.members.tsx` (same outer wrapper, header with title + count Badge + "← Back to inbox" Link, CSR via useLoaderData).

LOADER — `export async function loader({ request }: LoaderFunctionArgs)`:
- `const db = getDb();` (import `{ getDb, schema }` from `"../../server/db"`).
- Reuse the SAME query shape as Task 1's action (LEFT JOIN payments→gym_members, ORDER BY occurredAt DESC, LIMIT 100). Put the `// guard:allow-unscoped — single-tenant gym tables ...` comment directly above the query (the loader file is scanned independently).
- Return a plain object `{ payments }` (NO `json()` — RR v7 framework mode). Each payment: `{ id, memberName, memberPhone, amountMinorUnits, currency, status, occurredAt }`.

COMPONENT — render with shadcn Table from `@/components/ui/table`:
- Columns: **Date** / **Member** / **Amount** / **Status**.
- Date: `format(new Date(occurredAt), "d MMM yyyy, HH:mm")` via date-fns; guard NaN → "—".
- Member: `memberName || memberPhone || "—"`.
- Amount: format minor units to a currency string. Write a small `formatAmount(minor, currency)` helper using `new Intl.NumberFormat("en-GB", { style: "currency", currency: (currency || "GBP").toUpperCase() }).format(minor / 100)` → e.g. "£12.00". (Stripe currency codes are lowercase like "gbp"; uppercase before passing to Intl.)
- Status: shadcn `Badge` colour-coded — succeeded = green, refunded = amber, failed = red, pending = muted. Use the `variant`/`className` approach already used in this codebase (e.g. `variant="outline"` + a `className` with tailwind colour tokens like `text-green-600 border-green-600/30`, `text-amber-600 ...`, `text-red-600 ...`, `text-muted-foreground`). Map via a small `statusStyle(status)` lookup. Capitalise the label.
- Header: `<h1 className="text-sm font-semibold">Payments</h1>` + a count `Badge variant="outline"` showing `data.payments.length`, plus the "← Back to inbox" Link to `/gymos` (mirror members route header markup/classes).
- EMPTY STATE (data.payments.length === 0): render a clean bordered card (like the stub's `rounded-lg border ...` card) with a Tabler icon (e.g. `IconReceipt` or `IconCreditCard` from `@tabler/icons-react`, muted) and the copy: **"No payments yet — they'll appear here as members pay"**. Do NOT render an empty table shell. No emojis.
- `export function meta()` returns `[{ title: "GymClassOS — Payments" }]`.

Keep the page minimal and clean per AGENTS.md UI conventions — no extra toolbars/filters/buttons beyond header + table + back link. Run `npx prettier --write apps/staff-web/app/routes/gymos.payments.tsx`.

DOCS — in `apps/staff-web/AGENTS.md`, add a row to the "Agent Actions (LLM tools)" table (Tier 1 read actions, near `list-renewals`/`list-revenue`):
`| `list-payments` | 1 | "Show me recent payments" / "who paid recently" / payment history with the member who paid | Array of `{id, memberName, memberPhone, amountMinorUnits, currency, status, occurredAt}` |`
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit -p tsconfig.json 2>&1 | Select-String -Pattern "gymos.payments" -Quiet; if ($?) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>gymos.payments.tsx has a loader (LEFT JOIN + guard comment + plain-object return) and a component rendering a shadcn Table (Date/Member/Amount/Status) with colour-coded status Badges and the clean empty-state card "No payments yet — they'll appear here as members pay". Money formatted in the UI (e.g. "£12.00"), member falls back name→phone→"—". list-payments documented in apps/staff-web/AGENTS.md. tsc reports no errors for the route file.</done>
</task>

</tasks>

<verification>
- `cd apps/staff-web && npx tsc --noEmit -p tsconfig.json` shows no NEW errors in `actions/list-payments.ts` or `app/routes/gymos.payments.tsx` (pre-existing repo-wide errors unrelated to these two files are acceptable for a quick task — confirm none reference the changed files).
- Manual code review (no local HTTP run — `agent-native dev` cannot boot per STATE.md "P1c-WIDE VERIFICATION CONSTRAINT"; runtime check happens on the next Vercel deploy):
  - Both the action and the loader carry a `// guard:allow-unscoped` marker above their query (CI guard requirement).
  - Both queries LEFT JOIN payments→gym_members, ORDER BY occurredAt DESC, LIMIT 100.
  - The route returns a plain object (no `json()`), uses `@/` aliased shadcn imports, Tabler icons (no emojis).
  - Empty state copy is exactly "No payments yet — they'll appear here as members pay".
</verification>

<success_criteria>
- `list-payments` is a registered defineAction GET endpoint returning recent payments joined to the paying member.
- `/gymos/payments` renders a clean shadcn Table (Date / Member / Amount / Status with colour-coded badges) of the 100 most-recent payments, or the clean empty state when there are none (current state — table is empty).
- No DB schema changes. CI guards (no-unscoped-queries) satisfied via the guard:allow-unscoped markers. AGENTS.md documents the new action.
</success_criteria>

<output>
After completion, create `.planning/quick/260613-ey3-build-the-real-gymos-payments-page-list-/260613-ey3-SUMMARY.md`.
</output>
