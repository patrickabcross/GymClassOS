---
phase: quick-260626-m1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/staff-web/public/marketing/runstudio-film.mp4
  - apps/staff-web/features/marketing/lib/marketing-ssr.ts
autonomous: true
requirements: [QUICK-260626-m1c]
must_haves:
  truths:
    - "All four locale marketing homepages (/, /uk, /us, /fr, /de) render a real, inline-playing video in the agent section instead of the play-button placeholder"
    - "The video autoplays muted, loops, and plays inline on mobile (no tap required)"
    - "The TAG and CAPTION overlays still render on top of the video; the play-button overlay is gone"
  artifacts:
    - path: "apps/staff-web/public/marketing/runstudio-film.mp4"
      provides: "The marketing roughcut video served at /marketing/runstudio-film.mp4"
    - path: "apps/staff-web/features/marketing/lib/marketing-ssr.ts"
      provides: "videoSlot() emitting a <video> tag; agentSection passing the src"
  key_links:
    - from: "agentSection() videoSlot call (~line 336)"
      to: "/marketing/runstudio-film.mp4"
      via: "videoSlot({ src }) param threaded into a <video src> tag"
      pattern: "marketing/runstudio-film.mp4"
---

<objective>
Swap the marketing homepage agent-section video slot from its play-button placeholder to the real roughcut clip `roughcut_overlaid_v4.mp4`.

Purpose: The homepage agent section currently shows a static placeholder (pulse play button + caption). The roughcut is ready; show the actual film so all five locale homepages present a real, inline-autoplaying clip.
Output: A ~12 MB MP4 committed under `apps/staff-web/public/` and a small SSR markup change so `videoSlot()` renders a `<video>` instead of the placeholder.
</objective>

<context>
@.planning/STATE.md

# The marketing homepage is plain HTML-string SSR — NOT React, NOT client JS.
# All five locale homepages (/ /uk /us /fr /de) share ONE call site: agentSection().
@apps/staff-web/features/marketing/lib/marketing-ssr.ts

<interfaces>
<!-- Current videoSlot() signature (marketing-ssr.ts ~line 201). One call site: agentSection() ~line 336. -->
function videoSlot(opts: {
  ar: "ar-16x9" | "ar-9x16";
  tag: string;
  caption: string;
  className?: string;
}): string

<!-- Existing CSS already supports a real <video> behind the overlays (marketing-ssr.ts ~line 718): -->
<!-- .video-slot video,.video-slot img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:-1} -->
<!-- .video-slot.ar-16x9{aspect-ratio:16/9}  — keep this class so layout is stable. -->
<!-- .video-slot__tag (top-left pill) and .video-slot__cap (bottom caption) sit z-index>=1, so they render OVER the video. -->
<!-- .video-slot__play is the play button to DROP when a real video is present. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the roughcut MP4 into staff-web public assets</name>
  <files>apps/staff-web/public/marketing/runstudio-film.mp4</files>
  <action>
    The `public/marketing/` directory does not exist yet — create it, then copy the source binary in.

    Run (from the worktree root — use the worktree's own path, not the main checkout):
      mkdir -p apps/staff-web/public/marketing
      cp "C:/Users/dimet/video-editing/runstudio/roughcut_overlaid_v4.mp4" apps/staff-web/public/marketing/runstudio-film.mp4

    Notes:
    - Destination filename is the clean, stable `runstudio-film.mp4` (decided in this plan — do NOT carry the `roughcut_overlaid_v4` working name into the repo).
    - The source is ~12 MB and lives OUTSIDE the repo; committing this binary to git is acceptable per the deploy model (`git push origin master`).
    - `public/` is served at the web root, so this file becomes `/marketing/runstudio-film.mp4` at runtime.
    - Do NOT add a poster image. (Optional only; not a blocker. Skip unless ffmpeg is trivially available — it is not required.)
  </action>
  <verify>
    <automated>test -f apps/staff-web/public/marketing/runstudio-film.mp4 && [ "$(wc -c < apps/staff-web/public/marketing/runstudio-film.mp4)" -gt 1000000 ] && echo OK</automated>
  </verify>
  <done>`apps/staff-web/public/marketing/runstudio-film.mp4` exists in the worktree and is &gt;1 MB (a real video, not a stub).</done>
</task>

<task type="auto">
  <name>Task 2: Render the real video in videoSlot() and wire the agent section</name>
  <files>apps/staff-web/features/marketing/lib/marketing-ssr.ts</files>
  <action>
    Make `videoSlot()` emit a real `<video>` when a source is provided, and pass the source from the single call site.

    1. Extend the `videoSlot` opts type (~line 201) with an optional `src?: string` field.

    2. In the `videoSlot` body (~lines 207-212):
       - When `opts.src` is present: render the `<video>` element INSTEAD of the `.video-slot__play` div. Keep the `.video-slot__tag` and `.video-slot__cap` spans exactly as they are (they overlay the video via the existing CSS z-index).
       - Use attributes that satisfy mobile inline-autoplay rules: `autoplay muted loop playsinline` (and add `preload="metadata"`). Escape the src with the existing `escapeHtml` helper.
       - When `opts.src` is absent: keep the current placeholder markup (the `.video-slot__play` button) unchanged, so the function stays backward-compatible.
       - Example markup when src is present:
         `<video src="${escapeHtml(opts.src)}" autoplay muted loop playsinline preload="metadata"></video>`
       - Keep the `${opts.ar}` aspect-ratio class on the wrapper (already there) so layout does not shift.

    3. Update the call site in `agentSection()` (~line 336) to pass the new src:
       `${videoSlot({ ar: "ar-16x9", src: "/marketing/runstudio-film.mp4", tag: L.agent.videoTag, caption: L.agent.videoCaption, className: "reveal" })}`

    4. Update the function's doc-comment (~lines 193-200) so the example reflects the now-wired `src` param + `runstudio-film.mp4` filename (and that the play button is dropped when src is set).

    Constraints reminder: NO React, NO client JS, NO new CSS needed (the `.video-slot video {...}` rule already exists). NO poster attribute (not extracting one this task). NO DB/schema/backend changes. Run `npx prettier --write apps/staff-web/features/marketing/lib/marketing-ssr.ts` after editing.
  </action>
  <verify>
    <automated>cd apps/staff-web && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20; grep -q 'marketing/runstudio-film.mp4' features/marketing/lib/marketing-ssr.ts && grep -q 'playsinline' features/marketing/lib/marketing-ssr.ts && echo WIRED_OK</automated>
  </verify>
  <done>`videoSlot()` emits a `<video ... autoplay muted loop playsinline>` when `src` is set; `agentSection()` passes `src: "/marketing/runstudio-film.mp4"`; the `.video-slot__play` button no longer renders when a real video is present; tsc is clean for staff-web.</done>
</task>

</tasks>

<verification>
- `apps/staff-web/public/marketing/runstudio-film.mp4` exists (>1 MB).
- `marketing-ssr.ts` references `/marketing/runstudio-film.mp4` and emits a `<video>` with `autoplay muted loop playsinline`.
- staff-web `tsc --noEmit` is clean.
- The `.video-slot__tag` and `.video-slot__cap` overlays are preserved; `.video-slot__play` is dropped when src is present.
- No DB migration, schema change, or backend logic introduced (this is a static asset + SSR markup change only).
</verification>

<success_criteria>
On deploy (`git push origin master`), all five locale homepages (/, /uk, /us, /fr, /de) show the real roughcut clip in the agent section, autoplaying muted and looping inline, with the brand tag pill and caption still overlaid and no play-button placeholder.
</success_criteria>

<output>
After completion, create `.planning/quick/260626-m1c-swap-marketing-homepage-video-slot-to-ro/260626-m1c-SUMMARY.md`.
</output>
