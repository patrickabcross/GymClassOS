---
phase: quick-260701-fq6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mobile-app/app/_layout.tsx
  - packages/mobile-app/app/(tabs)/_layout.tsx
  - packages/mobile-app/components/AgentSheet.tsx
  - packages/mobile-app/lib/bottom-sheet-impl.ts
autonomous: true
requirements: [MOBILE-ROLE-UX, AGENT-COMPOSER-FIX]

must_haves:
  truths:
    - "Only admin (owner) sees the AI chat FAB; members and teachers see no FAB"
    - "Every signed-in role sees the 5 member tabs (Home/Classes/Passes/Log/Profile)"
    - "Teacher additionally sees the Schedule tab; admin sees the 5 tabs plus the ops FAB"
    - "After a long streamed answer, the reply composer stays visible and usable inside the bottom sheet"
    - "The composer clears the iPhone home indicator via safe-area bottom padding"
  artifacts:
    - path: "packages/mobile-app/app/_layout.tsx"
      provides: "FAB gated to isAdmin only"
      contains: "if (!isAdmin) return null"
    - path: "packages/mobile-app/app/(tabs)/_layout.tsx"
      provides: "Member tabs always visible; teacher tab additive"
      contains: "href: undefined"
    - path: "packages/mobile-app/components/AgentSheet.tsx"
      provides: "gorhom keyboard-aware composer"
      contains: "BottomSheetTextInput"
    - path: "packages/mobile-app/lib/bottom-sheet-impl.ts"
      provides: "keyboard props + 90% snap point"
      contains: "keyboardBehavior"
  key_links:
    - from: "AgentSheet.tsx"
      to: "@gorhom/bottom-sheet"
      via: "BottomSheetFlatList + BottomSheetTextInput imports"
      pattern: "BottomSheetFlatList|BottomSheetTextInput"
    - from: "bottom-sheet-impl.ts"
      to: "BottomSheet keyboard coordination"
      via: "keyboardBehavior/keyboardBlurBehavior/android_keyboardInputMode props"
      pattern: "keyboardBehavior"
---

<objective>
Three focused client-only fixes to the Expo mobile app (`packages/mobile-app`):
1. Gate the AI chat FAB to owner/admin only.
2. Show the 5 member tabs to every role (teacher keeps its additive Schedule tab).
3. Fix the AgentSheet reply composer so it stays visible and usable inside the gorhom bottom sheet after a long streamed answer.

Purpose: correct the current role UX (members/teachers see a coach/teacher FAB that has no real backing yet; non-members are locked out of the member tabs) and fix a confirmed on-device bug where the composer is clipped below the visible sheet.
Output: 4 modified files, no server/schema/dependency changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/mobile-app/app/_layout.tsx
@packages/mobile-app/app/(tabs)/_layout.tsx
@packages/mobile-app/components/AgentSheet.tsx
@packages/mobile-app/lib/bottom-sheet-impl.ts

<interfaces>
<!-- Executor should use these directly — no codebase exploration needed. -->

FAB gate today (app/_layout.tsx, AgentFabAndSheet):
  const isAdmin = role === "admin";
  if (onSignIn) return null;
  if (role !== "member" && !isAdmin) return null;   // ← change target
  // endpoint/title switch by isAdmin is KEPT as-is:
  endpoint={isAdmin ? "/api/m/admin/agent/stream" : "/api/m/agent/stream"}
  title={isAdmin ? "RunStudio Ops" : "Agent — GymClassOS Coach"}

Tab gate today (app/(tabs)/_layout.tsx):
  const isMember = role === "member";   // becomes unused after change
  const isTeacher = role === "teacher"; // KEEP
  // index/schedule/passes/food use: href: isMember ? undefined : null  → make href: undefined
  // teacher-schedule stays: href: isTeacher ? undefined : null
  // profile stays always visible

gorhom API (already installed, @gorhom/bottom-sheet 5.2.14):
  import BottomSheet, { BottomSheetView, BottomSheetFlatList, BottomSheetTextInput } from "@gorhom/bottom-sheet";
  BottomSheet props to add: keyboardBehavior="interactive", keyboardBlurBehavior="restore", android_keyboardInputMode="adjustResize"

safe-area (already a dependency):
  import { useSafeAreaInsets } from "react-native-safe-area-context";
  const insets = useSafeAreaInsets(); // insets.bottom for composer padding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate FAB to admin-only and show member tabs for all roles</name>
  <files>packages/mobile-app/app/_layout.tsx, packages/mobile-app/app/(tabs)/_layout.tsx</files>
  <action>
CHANGE 1 (app/_layout.tsx, AgentFabAndSheet):
- Replace the line `if (role !== "member" && !isAdmin) return null;` with `if (!isAdmin) return null;` so the FAB renders ONLY for admins (members + teachers get no FAB). Keep the preceding `if (onSignIn) return null;` guard.
- Keep the existing `isAdmin` derivation, and keep the endpoint/title switching in the AgentSheetContainer child (`isAdmin ? "/api/m/admin/agent/stream" : ...` and title switch) untouched — with admin-only gating the member branch is now effectively dead, but leave it in place (no behavior risk, future-proofs the coach re-add).
- Update the explanatory comment above the removed line to reflect the new rule: FAB is admin/owner only; members + teachers get NO FAB for now (role-specific chatbots are a future task).

CHANGE 2 (app/(tabs)/_layout.tsx):
- For the four member tabs (`index`, `schedule`, `passes`, `food`): change `href: isMember ? undefined : null` to `href: undefined` (always visible).
- Keep `teacher-schedule` as `href: isTeacher ? undefined : null` (additive teacher tab).
- Keep `profile` always visible (unchanged).
- Remove the now-unused `const isMember = role === "member";` line. Keep `const isTeacher = role === "teacher";`.
- Update the header/inline comments that describe the member-tabs-hidden-for-teachers behavior to reflect that member tabs are now shown for all roles and teacher adds Schedule on top.

Net result: member = 5 tabs; teacher = 5 tabs + Schedule; admin = 5 tabs + ops FAB.
  </action>
  <verify>
    <automated>cd packages/mobile-app && npx tsc --noEmit 2>&1 | grep -E "_layout" || echo "no _layout type errors"</automated>
  </verify>
  <done>_layout.tsx FAB gate is `if (!isAdmin) return null;`; (tabs)/_layout.tsx four member tabs use `href: undefined`, teacher-schedule stays `isTeacher`-gated, `isMember` removed, `isTeacher` kept; tsc reports no new errors in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AgentSheet composer on gorhom keyboard-aware components</name>
  <files>packages/mobile-app/components/AgentSheet.tsx</files>
  <action>
Fix the confirmed on-device bug: after a long streamed answer the reply composer is clipped/hidden because RN FlatList + TextInput inside nested KeyboardAvoidingViews do not coordinate with @gorhom/bottom-sheet.

- Imports: from `@gorhom/bottom-sheet` add `BottomSheetFlatList` and `BottomSheetTextInput`. From `react-native` remove `FlatList`, `TextInput`, `KeyboardAvoidingView`, and `Platform` (they become unused — verify none are referenced elsewhere before removing `Platform`). Add `import { useSafeAreaInsets } from "react-native-safe-area-context";`.
- Replace the root: instead of the outer `KeyboardAvoidingView` (behavior padding) wrapper, use a plain `View style={styles.container}` ({flex:1}). Structure inside: header (unchanged) + list (flex:1) + input row pinned at bottom.
- Replace the RN `<FlatList>` with `<BottomSheetFlatList>`. Keep the same props: `ref` (retype `listRef` to `useRef<any>(null)` or the BottomSheetFlatList methods type — `any` is acceptable to avoid gorhom generic friction), `data`, `keyExtractor`, `contentContainerStyle`, `onContentSizeChange` scroll-to-end, and the exact `renderItem` (system line + user/agent bubbles + streaming ActivityIndicator). Add `style={{ flex: 1 }}` to the list so it fills between header and composer.
- Replace the inner `KeyboardAvoidingView` + RN `TextInput` with: a plain `<View style={styles.inputRow}>` (add bottom padding = `insets.bottom` so it clears the iPhone home indicator — e.g. spread `{ paddingBottom: 12 + insets.bottom }` onto the inputRow style inline, keeping the existing borderTop divider). Use `<BottomSheetTextInput>` in place of `<TextInput>` with the SAME props (`value`, `onChangeText`, `placeholder`, `placeholderTextColor`, `style={styles.input}`, `multiline`, `editable={!sending}`). Keep the send `<Pressable>` and Feather send icon exactly as-is.
- PRESERVE all existing logic verbatim: `send()` (draft trim, optimistic user + streaming assistant bubbles, wireMessages build), `streamAgent` wiring (onDelta / onToolUse / onToolResult cache invalidation of ['schedule'],['food-entries'],['profile'] / onDone / onError), the try/catch fallback, `cancelRef` cancel-on-unmount `useEffect`, and the `endpoint`/`title` props + initial system-welcome message.
- Remove the `KeyboardAvoidingView`-related styles are not needed; the `styles` object otherwise stays. Do NOT change bubble/header/input visual styles beyond adding safe-area bottom padding.
  </action>
  <verify>
    <automated>cd packages/mobile-app && npx tsc --noEmit 2>&1 | grep -E "AgentSheet" || echo "no AgentSheet type errors"</automated>
  </verify>
  <done>AgentSheet.tsx imports BottomSheetFlatList + BottomSheetTextInput from @gorhom/bottom-sheet, uses no KeyboardAvoidingView, root is a plain flex:1 View (header + flex:1 list + bottom-pinned inputRow with safe-area padding); all streaming/tool/cache/cancel logic and endpoint/title props unchanged; tsc clean for this file.</done>
</task>

<task type="auto">
  <name>Task 3: Add keyboard props and raise snap point on the bottom sheet</name>
  <files>packages/mobile-app/lib/bottom-sheet-impl.ts</files>
  <action>
In `AgentSheetContainer` (built with `React.createElement`, keep that style — NO JSX):
- Add three keyboard props to the `BottomSheet` element props object: `keyboardBehavior: "interactive"`, `keyboardBlurBehavior: "restore"`, `android_keyboardInputMode: "adjustResize"`.
- Change `snapPoints: ["66%"]` to `snapPoints: ["90%"]` (single snap point) so the composer + keyboard fit.
- Keep everything else identical: `ref`, `index: open ? 0 : -1`, `enablePanDownToClose: true`, `onClose`, `backgroundStyle`, `handleIndicatorStyle`, and the `BottomSheetView` `{ flex: 1 }` child wrapper. Keep the `GestureRoot` export and `BOTTOM_SHEET_IMPL` constant unchanged.
- Update the header comment note only if it references the old snapPoint; otherwise leave the spike documentation as-is.
  </action>
  <verify>
    <automated>cd packages/mobile-app && npx tsc --noEmit 2>&1 | grep -E "bottom-sheet-impl" || echo "no bottom-sheet-impl type errors"</automated>
  </verify>
  <done>bottom-sheet-impl.ts BottomSheet props include keyboardBehavior "interactive", keyboardBlurBehavior "restore", android_keyboardInputMode "adjustResize", snapPoints ["90%"]; React.createElement style preserved; tsc clean for this file.</done>
</task>

</tasks>

<verification>
- `cd packages/mobile-app && npx tsc --noEmit` reports no new type errors across the 4 changed files.
- Manual (on device/build, not blocking): admin sees ops FAB + 5 tabs; member sees 5 tabs, no FAB; teacher sees 5 tabs + Schedule, no FAB; opening the AI sheet as admin and sending a long prompt keeps the composer visible above the keyboard, clearing the home indicator.
</verification>

<success_criteria>
- FAB renders only for admins (`if (!isAdmin) return null;`).
- Four member tabs use `href: undefined` (visible for all roles); teacher-schedule stays additive; `isMember` removed.
- AgentSheet uses BottomSheetFlatList + BottomSheetTextInput, no KeyboardAvoidingView, safe-area bottom padding on composer; all streaming/tool/cache/cancel logic preserved.
- bottom-sheet-impl has keyboard props + `["90%"]` snap point, React.createElement style intact.
- No server, schema, or dependency changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260701-fq6-mobile-role-ux-agent-composer-fab-owner-/260701-fq6-SUMMARY.md`
</output>
