---
phase: quick-260622-lp3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/staff-web/server/db/schema.ts
  - apps/staff-web/server/plugins/db.ts
  - apps/staff-web/actions/list-trainers.ts
  - apps/staff-web/actions/create-trainer.ts
  - apps/staff-web/actions/update-trainer.ts
  - apps/staff-web/actions/create-class-occurrence.ts
  - apps/staff-web/server/plugins/agent-chat.ts
  - apps/staff-web/AGENTS.md
  - apps/staff-web/app/routes/gymos.schedule.tsx
  - apps/staff-web/app/components/gymos/NewClassDialog.tsx
  - apps/staff-web/app/components/gymos/ManageTrainersDialog.tsx
autonomous: true
requirements: [LP3-SCHEMA, LP3-TRAINER-ACTIONS, LP3-OCC-FIELDS, LP3-UI, LP3-SEED]

must_haves:
  truths:
    - "A `trainers` table exists with id/name/home_location/active/created_at and a unique index on lower(name)"
    - "class_occurrences has nullable `location` and `trainer_id` columns"
    - "The 23 HUSTLE trainers are seeded idempotently on Vercel boot (re-deploy does not duplicate)"
    - "list-trainers / create-trainer / update-trainer actions exist, are two-exposed, and use guard:allow-unscoped"
    - "create-class-occurrence accepts + persists optional trainerId and location while preserving room/instructorUserId/notes"
    - "The New Class dialog has a Trainer Select and a Location Select; existing fields + optimistic close-then-revalidate UX preserved"
    - "A 'Manage trainers' button in the Schedule header opens a dialog to list/add/edit/deactivate trainers via the actions"
  artifacts:
    - path: "apps/staff-web/server/plugins/db.ts"
      provides: "Migration versions 22-25 (trainers table + unique index + 2 ALTER COLUMNs + seed)"
      contains: "version: 22"
    - path: "apps/staff-web/server/db/schema.ts"
      provides: "trainers Drizzle table + location/trainerId columns on classOccurrences"
      contains: "export const trainers"
    - path: "apps/staff-web/actions/list-trainers.ts"
      provides: "GET action returning active trainers"
      contains: "defineAction"
    - path: "apps/staff-web/actions/create-trainer.ts"
      provides: "POST mutation creating/reactivating a trainer by name"
      contains: "defineAction"
    - path: "apps/staff-web/actions/update-trainer.ts"
      provides: "POST mutation editing name/homeLocation/active (no hard delete)"
      contains: "defineAction"
    - path: "apps/staff-web/app/components/gymos/ManageTrainersDialog.tsx"
      provides: "Trainers management surface (list/add/edit/deactivate)"
      contains: "useActionMutation"
  key_links:
    - from: "apps/staff-web/app/components/gymos/NewClassDialog.tsx"
      to: "list-trainers"
      via: "useActionMutation / useActionQuery to populate Trainer Select"
      pattern: "list-trainers"
    - from: "apps/staff-web/app/components/gymos/NewClassDialog.tsx"
      to: "create-class-occurrence"
      via: "mutateAsync with trainerId + location in payload"
      pattern: "trainerId"
    - from: "apps/staff-web/actions/create-class-occurrence.ts"
      to: "schema.classOccurrences"
      via: "insert .values({ trainerId, location })"
      pattern: "trainerId|location"
    - from: "apps/staff-web/server/plugins/db.ts"
      to: "trainers table"
      via: "INSERT ... ON CONFLICT DO NOTHING seed against unique lower(name) index"
      pattern: "ON CONFLICT"
---

<objective>
Phase 1 of the recurring-classes/staff feature: stand up a lightweight **trainers roster** (NOT auth users) and a **structured location** foundation (Norwich/Wymondham) on class occurrences, bsport-style (activity → location → teacher).

Purpose: Give the Schedule surface a real teacher roster + location dimension so a class occurrence can record who teaches it and where. This is the data + action + UI foundation that Phase 2 (recurrence rules + worker materialization) and Phase 3 (HUSTLE timetable population) build on.

Output: additive migrations (trainers table + unique name index + location/trainer_id columns on class_occurrences + idempotent HUSTLE trainer seed), matching Drizzle defs, three two-exposed trainer actions, an extended create-class-occurrence, and Schedule UI (Trainer + Location selects in New Class + a Manage trainers dialog).

OUT OF SCOPE (do NOT build): recurrence rules table, worker materialization job, recurring UI (Phase 2); timetable population (Phase 3); bulk multi-select edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@apps/staff-web/AGENTS.md

<verified_facts>
<!-- All refs below were verified against the codebase at plan time. -->

- **Latest migration version in db.ts is 21** (`video_compositions`). New blocks are **22, 23, 24, 25**.
  - NOTE: migrations are applied by version NUMBER, not array order — version 15 (the token_usage trigger) is intentionally placed at the END of the array. Append the new blocks after version 15's block (i.e. just before the closing `]` / `{ table: "mail_migrations" }`).
  - Existing blocks show the exact dual-dialect idioms to copy:
    - Booleans → `INTEGER NOT NULL DEFAULT 1` (see studio_owner_config.digest_enabled).
    - now() default → `TEXT NOT NULL DEFAULT (datetime('now'))` (see studio_brain_docs).
    - `CREATE TABLE IF NOT EXISTS`, `ALTER TABLE … ADD COLUMN IF NOT EXISTS`, `CREATE [UNIQUE] INDEX IF NOT EXISTS` are all used already and are the only DDL forms allowed.

- **class_occurrences current columns** (schema.ts ~201): id, definition_id, starts_at, ends_at, capacity, instructor_user_id, room, status, notes, created_at. NO `location`, NO `trainer_id` yet. The no-hard-FK soft-ref style is `instructor_user_id text` (no references()) — match it for `trainer_id`.

- **Drizzle table helper** in schema.ts is `table(...)` (aliased pgTable-equivalent) with `text(...)`, `integer(..., { mode: "boolean" })`, and `now()` helper already imported at top of schema.ts. Reuse them; do NOT add new imports unless `uniqueIndex` is needed.

- **defineAction shape** (verified in create-class-definition.ts, list-classes.ts, update-class-definition.ts):
  - GET read action: `http: { method: "GET" }`.
  - POST mutation: omit the `http` key entirely (auto-mounts POST-only).
  - Import: `import { defineAction } from "@agent-native/core";` and `import { getDb, schema } from "../server/db/index.js";`.
  - id prefix convention: `cdef_`/`cocc_`/`bkg_` + `nanoid()`. Use `trn_${nanoid()}` for trainers.
  - Every query/insert on a gym table carries `// guard:allow-unscoped — single-tenant gym tables`.
  - brain-init.ts is the idempotent-seed reference: `.onConflictDoNothing()` / `.onConflictDoUpdate({ target, set })`.

- **NewClassDialog.tsx** uses `useActionMutation("create-class-definition")` + `useActionMutation("create-class-occurrence")` and `useRevalidator()`. Optimistic pattern = close dialog + toast + run mutations + `revalidator.revalidate()`. `Select/SelectContent/SelectItem/SelectTrigger/SelectValue`, `Input`, `Label`, `Dialog*`, `Button` are already imported there. classTypes is passed as a prop from the route loader.

- **gymos.schedule.tsx loader** already builds `classTypes` (Query D, active definitions). The header (~347) mounts `<NewClassDialog classTypes={...} defaultDate={selectedKey} />` inside a `flex items-center gap-2` div — the "Manage trainers" button goes next to it. To pass trainers into the dialog without a new loader query you may either (a) add a Query E for active trainers in the loader and pass `trainers` as a prop, OR (b) fetch via list-trainers client-side in the dialog. PREFER (a) loader Query E (SSR-stable, mirrors classTypes) — it is the established pattern.

- **agent-chat.ts** Schedule section is lines 52-59 (a bullet list under "Schedule tab (when the coach is on /gymos/schedule …)"). Add the new actions as bullets there in the same `- name — description ({params}). Returns {…}.` style.

- **apps/staff-web/AGENTS.md** "Agent Actions (LLM tools)" table (the big `| Tool | Tier | Use For | Returns |` table starting ~line 31) is where new action rows go. The "Two-exposure rule" callouts live ~lines 170-180 — add a new AE-style callout for the trainer actions + note the create-class-occurrence param extension.

- **ui primitives present**: dialog.tsx, sheet.tsx, select.tsx, input.tsx, label.tsx, button.tsx, badge.tsx, card.tsx, alert-dialog.tsx. Use Dialog (not Sheet) for Manage trainers — matches the New Class / Book dialog idiom on this screen.
</verified_facts>

<seed_names>
The 23 distinct HUSTLE trainers (home_location = NULL; dedupe by lower(name)):
Matty Wiseman, Eleanor Perlman, Primrose Rushen, Charly Willis, Jacob Golden, Ki Rodrigues, Lauren McDonald, Debbie Bagley, Fiona Brooks, Liey Bedingham, Shahan Toheed, Ben O'Connor, Owen Blunden, City Bedingham, Louise Bates, Anthony Trebble, Mike Stolworthy, Leanne Whitman, Bobby Harrison, Jess Bacon, Ricky Faiers, Jordan Eke, Vicky Faiers.

NOTE: "Ben O'Connor" contains an apostrophe — in the seed SQL it must be escaped as `'Ben O''Connor'`.
</seed_names>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migrations v22-v25 (trainers table + index + occurrence columns + idempotent seed) and Drizzle defs</name>
  <files>apps/staff-web/server/plugins/db.ts, apps/staff-web/server/db/schema.ts</files>
  <action>
ADDITIVE-ONLY. No DROP/RENAME/TRUNCATE. Do NOT edit standalone .sql files. Append the four new migration blocks to the `runMigrations([...])` array in db.ts AFTER the version-15 block (last in array — migrations apply by version number, not array order):

  - version 22: `CREATE TABLE IF NOT EXISTS trainers (
      id          TEXT PRIMARY KEY,
      name        TEXT NOT NULL,
      home_location TEXT,
      active      INTEGER NOT NULL DEFAULT 1,
      created_at  TEXT NOT NULL DEFAULT (datetime('now'))
    )` — copy the boolean (`INTEGER … DEFAULT 1`) + now() (`TEXT … DEFAULT (datetime('now'))`) idioms from the studio_owner_config / studio_brain_docs blocks.

  - version 23: `CREATE UNIQUE INDEX IF NOT EXISTS idx_trainers_name_lower ON trainers (lower(name))` — this is the dedupe target for the seed's ON CONFLICT. (Postgres supports expression indexes; SQLite local dev also supports `lower()` in an index expression — both dialects accept this form, matching the existing IF NOT EXISTS index blocks v8/v10/v19.)

  - version 24 (two separate ALTERs — Postgres allows one ADD COLUMN per ALTER in this codebase's style; keep them as two statements in one sql string separated by `;` OR as two migration sub-versions — PREFER two statements in the single v24 sql joined by `;` only if runMigrations executes multi-statement strings; SAFER: split into version 24 = location, version 25 = trainer_id, each a single ALTER, mirroring how v2/v3/v4 each added ONE column):
      - version 24: `ALTER TABLE class_occurrences ADD COLUMN IF NOT EXISTS location TEXT`
      - version 25: `ALTER TABLE class_occurrences ADD COLUMN IF NOT EXISTS trainer_id TEXT`
    (Match the no-hard-FK soft-ref style of instructor_user_id — plain TEXT, no FK constraint.)

  - version 26: idempotent HUSTLE trainer seed. Single INSERT with a multi-row VALUES list of the 23 names, `home_location` NULL, then `ON CONFLICT DO NOTHING`. Because the unique index is on `lower(name)` (an expression index), use the unqualified `ON CONFLICT DO NOTHING` form (Postgres infers the conflict target from any unique constraint/index; the expression-index inference target `ON CONFLICT (lower(name))` is also valid — use whichever the existing onConflict idioms favor; plain `ON CONFLICT DO NOTHING` is safest cross-dialect). Generate ids inline as `'trn_' || <unique-suffix>` is NOT available in pure SQL portably — instead hardcode 23 stable string ids (e.g. `'trn_seed_01' … 'trn_seed_23'`) so the seed is deterministic and re-running inserts the SAME ids (the ON CONFLICT on lower(name) prevents dupes regardless). Escape `'Ben O''Connor'`. created_at uses `datetime('now')`.

    Example shape:
    ```sql
    INSERT INTO trainers (id, name, home_location, active, created_at) VALUES
      ('trn_seed_01', 'Matty Wiseman', NULL, 1, datetime('now')),
      ('trn_seed_02', 'Eleanor Perlman', NULL, 1, datetime('now')),
      ... (all 23, with 'trn_seed_12','Ben O''Connor' escaped) ...
      ('trn_seed_23', 'Vicky Faiers', NULL, 1, datetime('now'))
    ON CONFLICT DO NOTHING
    ```
    Re-deploy safety: the unique lower(name) index + ON CONFLICT DO NOTHING means a second boot inserts zero rows.

Then update schema.ts:
  - Add `export const trainers = table("trainers", { id: text("id").primaryKey(), name: text("name").notNull(), homeLocation: text("home_location"), active: integer("active", { mode: "boolean" }).notNull().default(true), createdAt: text("created_at").notNull().default(now()) });` placed near the class scheduling section (after classOccurrences ~216).
  - Add two columns to the existing `classOccurrences` table def (additive — do NOT reorder existing columns): `location: text("location"),` and `trainerId: text("trainer_id"),` (both nullable, no .notNull). Place alongside instructorUserId/room.
  - If a `uniqueIndex` Drizzle export is desired for the trainers index, you MAY add it, but it is optional (the index is enforced by the migration; Drizzle defs are not the migration source here). Do NOT add it if it pulls in a new import that breaks tsc — the migration is authoritative.

Confirm `text`, `integer`, `now`, `table` are already imported at the top of schema.ts (they are) — add no new imports unless strictly needed.
  </action>
  <verify>
    <automated>cd apps/staff-web && pnpm typecheck</automated>
  </verify>
  <done>db.ts has migration blocks v22 (trainers table), v23 (unique lower(name) index), v24 (location column), v25 (trainer_id column), v26 (23-row idempotent seed with ON CONFLICT DO NOTHING). schema.ts exports `trainers` and classOccurrences has nullable `location` + `trainerId`. `pnpm typecheck` exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trainer actions (list/create/update) + extend create-class-occurrence + two-exposure docs</name>
  <files>apps/staff-web/actions/list-trainers.ts, apps/staff-web/actions/create-trainer.ts, apps/staff-web/actions/update-trainer.ts, apps/staff-web/actions/create-class-occurrence.ts, apps/staff-web/server/plugins/agent-chat.ts, apps/staff-web/AGENTS.md</files>
  <action>
Create three new actions in apps/staff-web/actions/, copying the defineAction shape from list-classes.ts (GET) and create-class-definition.ts / update-class-definition.ts (POST mutations). Every gym-table query/insert/update carries `// guard:allow-unscoped — single-tenant gym tables`.

1. **list-trainers.ts** (`http: { method: "GET" }`): schema `z.object({})` (no params). Select id, name, homeLocation from `schema.trainers` where `eq(schema.trainers.active, true)` ordered by `asc(schema.trainers.name)`. Return `[{ id, name, homeLocation }]`. Description: "List active trainers (id, name, homeLocation) for the studio roster. Use to populate a trainer picker or answer who teaches."

2. **create-trainer.ts** (POST mutation — NO http key): schema `z.object({ name: z.string().min(1).max(120), homeLocation: z.string().max(120).optional() })`. Logic (dedupe by name per the member-upsert lesson):
   - Look up an existing trainer by `lower(name)` match: `where(sql\`lower(${schema.trainers.name}) = lower(${input.name})\`)`. (Import `sql` from drizzle-orm.)
   - If a match exists: if it is inactive, reactivate it (`update … set active=true, homeLocation if provided`) and return `{ id, name }`; if already active, return `{ id, name }` (idempotent no-op — optionally update homeLocation if provided).
   - If no match: insert `{ id: \`trn_${nanoid()}\`, name, homeLocation: homeLocation ?? null, active: true, createdAt: now }` and return `{ id, name }`.
   This reactivate-or-create path mirrors the documented member-upsert dual-key reconciliation lesson (avoid blind ON CONFLICT that fights the unique index). Returns `{ id, name }`.

3. **update-trainer.ts** (POST mutation — NO http key): schema `z.object({ id: z.string().min(1), name: z.string().min(1).max(120).optional(), homeLocation: z.string().max(120).optional(), active: z.boolean().optional() })`. Resolve the trainer by id (return `{ error: "TRAINER_NOT_FOUND" }` if absent). Build a partial update object from supplied fields ONLY (mirror update-class-definition.ts's `updates` pattern); empty patch → `{ updated: false, reason: "no changes" }`. `active: false` deactivates (NO hard delete — there is no delete action). Return `{ updated: true }`. If a name change would collide with another active trainer's lower(name), return `{ error: "NAME_IN_USE" }`.

4. **Extend create-class-occurrence.ts**: add to the Zod schema `trainerId: z.string().optional()` and `location: z.string().max(120).optional()`. In the insert `.values({...})`, add `trainerId: input.trainerId ?? null,` and `location: input.location ?? null,`. Keep room/instructorUserId/notes/status untouched. Update the action `description` string to mention "optional trainerId + location". Do not change the return shape (still `{ id, startsAt, endsAt, capacity }`) — the UI revalidates.

5. **agent-chat.ts** Schedule section (after line 57, alongside the other schedule bullets): add bullets:
   - `- list-trainers — list active trainers ({}) → [{id, name, homeLocation}]. Use to find who can teach a class.`
   - `- create-trainer — add a trainer to the roster ({name, homeLocation?}). Dedupes by name (reactivates a deactivated same-name trainer). Returns {id, name}.`
   - `- update-trainer — edit a trainer's name / home location, or deactivate them ({id, name?, homeLocation?, active?}). active:false deactivates (no hard delete). Returns {updated:true} | {updated:false, reason} | {error:"TRAINER_NOT_FOUND"|"NAME_IN_USE"}.`
   - Update the existing create-class-occurrence bullet (line 54) to include the new optional params: `({definitionId, startsAt, capacity?, room?, trainerId?, location?})`.

6. **apps/staff-web/AGENTS.md**: add three rows to the "Agent Actions (LLM tools)" table (Tier blank `—` style, matching the other mutations) for list-trainers (Tier 1), create-trainer, update-trainer. Update the existing create-class-occurrence row to mention the trainerId/location params. Add a new two-exposure callout near the existing AE2 schedule callout (~line 172): note that list-trainers/create-trainer/update-trainer are two-exposed (action file + agent-chat.ts Schedule section + this table), all DIRECT (no propose-action gate), carry guard:allow-unscoped, and that create-class-occurrence now also persists optional trainerId + location.

Run `npx prettier --write` on the new/edited .ts files per AGENTS.md.
  </action>
  <verify>
    <automated>cd apps/staff-web && pnpm typecheck</automated>
  </verify>
  <done>Three trainer actions exist (list-trainers GET; create-trainer + update-trainer POST mutations) with guard:allow-unscoped and the reactivate-or-create dedupe. create-class-occurrence accepts + persists optional trainerId + location with room/instructorUserId/notes preserved. agent-chat.ts Schedule section names all three new actions and the updated occurrence params. AGENTS.md table + two-exposure callout updated. `pnpm typecheck` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Schedule UI — Trainer + Location selects in NewClassDialog and a Manage trainers dialog</name>
  <files>apps/staff-web/app/components/gymos/NewClassDialog.tsx, apps/staff-web/app/components/gymos/ManageTrainersDialog.tsx, apps/staff-web/app/routes/gymos.schedule.tsx</files>
  <action>
Keep clean/minimal per the template-UX rules (progressive disclosure; no clutter on the core screen). shadcn primitives + Tabler icons + optimistic UX only — no custom dropdowns/modals, no emojis-as-icons, no browser dialogs.

1. **gymos.schedule.tsx loader**: add Query E (mirroring Query D / classTypes) — select `{ id, name, homeLocation }` from `schema.trainers` where `eq(schema.trainers.active, true)` ordered by `asc(schema.trainers.name)`, carrying `// guard:allow-unscoped — single-tenant gym tables`. Add `trainers` to the loader return object. (Import `schema.trainers` is already available via the existing `schema` import.)

2. **NewClassDialog.tsx**: extend the component props to accept `trainers: { id: string; name: string; homeLocation: string | null }[]`. Add two new pieces of form state: `trainerId` (default "" = none) and `location` (default "" = none). Add two new `<Select>` blocks in the dialog body (after Room, or after Capacity — keep ordering bsport-like activity→location→teacher: put Location after Capacity and Trainer after Location):
   - Location Select: options `Norwich`, `Wymondham`, plus a "— none —" item (use a sentinel value like `"__none__"` mapped to undefined on submit, since Radix Select cannot use empty-string values). Label "Location (optional)".
   - Trainer Select: map over `trainers` to SelectItems (value = trainer.id, label = trainer.name), plus a "— none —" sentinel item. Label "Trainer (optional)".
   On submit (`onCreate`): include `trainerId: trainerId && trainerId !== "__none__" ? trainerId : undefined` and `location: location && location !== "__none__" ? location : undefined` in the `createOcc.mutateAsync({...})` payload. Reset both in `resetForm()`. Preserve the existing optimistic close-then-revalidate flow exactly.

3. **ManageTrainersDialog.tsx** (NEW): a minimal trainers-management surface. Use `Dialog` (not Sheet — matches the screen's existing dialog idiom). Props: `trainers: {id,name,homeLocation:string|null}[]` (the active roster from the loader). A `<DialogTrigger asChild>` wrapping a `<Button variant="outline" size="sm">` with a Tabler icon (`IconUsers` or `IconUserPlus`) labelled "Trainers" or "Manage trainers". Inside:
   - A list of current trainers (name + home location), each with an inline edit affordance and a "Deactivate" action.
   - An "Add trainer" row: a name `<Input>` + an optional home-location `<Input>` (or a Norwich/Wymondham Select) + an Add `<Button>`.
   - Wire `useActionMutation("create-trainer")`, `useActionMutation("update-trainer")`, and `useRevalidator()`. On add/edit/deactivate: optimistic toast + `mutateAsync` + `revalidator.revalidate()` (mirror NewClassDialog's pattern). Deactivate calls `update-trainer` with `{ id, active: false }`. Edit calls `update-trainer` with the changed fields.
   - Keep it compact and progressive: a simple scrollable list, no extra toolbars/badges. Use shadcn AlertDialog ONLY if you add a destructive-confirm on deactivate (optional — deactivate is reversible, so a plain button is acceptable; do NOT use window.confirm).
   - Tabler icons only; no emojis.

4. **gymos.schedule.tsx header** (the `flex items-center gap-2` div ~line 346, where `<NewClassDialog … />` mounts): pass `trainers={data.trainers}` to `<NewClassDialog>` and add `<ManageTrainersDialog trainers={data.trainers} />` next to it. Import the new component.

Run `npx prettier --write` on all three files.
  </action>
  <verify>
    <automated>cd apps/staff-web && pnpm typecheck</automated>
  </verify>
  <done>The New Class dialog has a Trainer Select (from loader trainers, "— none —" allowed) and a Location Select (Norwich/Wymondham, "— none —" allowed); existing fields + optimistic close-then-revalidate preserved; selected trainerId/location flow into create-class-occurrence. A "Manage trainers" button in the Schedule header opens a Dialog listing trainers and supporting add/edit/deactivate via the actions with optimistic revalidation, Tabler icons, shadcn primitives. `pnpm typecheck` exits 0.</done>
</task>

</tasks>

<verification>
- `cd apps/staff-web && pnpm typecheck` exits 0 after every task.
- db.ts migrations are strictly additive (only CREATE TABLE/INDEX IF NOT EXISTS, ALTER … ADD COLUMN IF NOT EXISTS, INSERT … ON CONFLICT DO NOTHING). No DROP/RENAME/TRUNCATE, no drizzle-kit push, no standalone .sql edits.
- Seed is idempotent: re-running v26 inserts zero rows (unique lower(name) index + ON CONFLICT DO NOTHING). 23 distinct trainers; "Ben O''Connor" escaped.
- Three trainer actions are two-exposed (action file + agent-chat.ts Schedule section + AGENTS.md table). All gym-table queries carry guard:allow-unscoped.
- create-class-occurrence still returns `{ id, startsAt, endsAt, capacity }` and preserves room/instructorUserId/notes; trainerId + location persist when supplied.
- No new packages/core edits (no changeset needed). No git branch ops.
- shadcn primitives + Tabler icons + optimistic UX throughout; no custom dropdowns, no emojis-as-icons, no window.confirm.
</verification>

<success_criteria>
- A `trainers` table + unique lower(name) index exist; class_occurrences has nullable location + trainer_id; 23 HUSTLE trainers seeded idempotently on Vercel boot.
- list-trainers / create-trainer / update-trainer work, are two-exposed, deactivate-not-delete, dedupe by name.
- New Class dialog records trainer + location; Manage trainers dialog manages the roster — both optimistic, clean, minimal.
- Phase 2 (recurrence + worker) and Phase 3 (timetable population) are unblocked but NOT touched here.
</success_criteria>

<output>
After completion, create `.planning/quick/260622-lp3-recurring-staff-phase-1-trainers-roster-/260622-lp3-SUMMARY.md`.
</output>
