import { SettingsPage } from "@/pages/SettingsPage";

export function meta() {
  return [{ title: "Settings — Recruiting" }];
}

export default function SettingsRoute() {
  return <SettingsPage />;
}
